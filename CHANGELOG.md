# Changelog

本项目所有值得记录的变更都会写在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

版本号与条目**由 `scripts/changeset.py version` 从 `.changeset/` 碎片生成**，
不要手改本文件里由它维护的段落。更早的历史见 git log 与 `PROJECT_MEMORY.md`。

## 0.2.0

### Minor Changes

- Web / 桌面宿主端点加访问令牌；同步 L5 规格漂移
  
  **破坏性（对程序化客户端）**：`neo serve` / `neo desktop` 的 `/api/*` 端点现在都需要
  访问令牌，裸 curl 调用会得到 401。用启动时打印的完整 URL（`#token=…`）打开页面即可，
  或显式带上 `?token=` / `X-Neo-Token` 头。
  
  为什么是必要的：只绑 `127.0.0.1` 限制的是网络来源、不是谁能访问。本机任意进程都能枚举
  回环端口；更现实的是浏览器里的任意网页 —— 端点全是简单请求（不触发 CORS 预检），
  恶意页面可跨源 `POST /api/turn` 且**请求会生效**（CSRF）。令牌把这两条同时堵死。
  
  其它改动：
  - `--addr` 绑非回环地址时显式警告（令牌拦得住未授权调用，但流量是明文 HTTP）。
  - 同步 `L5-host.md` 的两处规格漂移：桌面实际是"开回环端口复用 Web 宿主"（非自定义协议
    不开端口）；`neo-host-web` 实际是"手写 HTTP + SSE"（非 axum + WebSocket）。
- 新增 neo-text（宿主中立文本语义）；Tone 去终端专有变体，TUI 改为复用
- 新增 neo-host-egui 桌面原生 GUI 宿主（neo desktop 默认走它）；neo-text 加 palette/blocks
- 桌面 GUI 补 D11 执行模式切换 + 模型 picker + 高风险档位常驻提示
- 桌面 GUI 补 D9 命令面板（Cmd+K 覆盖式 / 搜索 / 键盘导航 / 9 条命令）
- 桌面 GUI 补 D1 会话栏；SessionControl 下沉 neo-session（两宿主共用）
- 桌面 GUI 补 D8 命令台（Op::Shell 走沙箱不经模型）；修 shell 调用 id 重复 + 焦点竞争
- UI 栈奠基：kit/render/behavior/ui 四层 + 共享 neo-driver + 开源边界门禁
- neo-host-gpui：GPUI 桌面宿主可用（真窗口 + NEO 主题 + 中文）
- neo desktop --gpui 可试用；T6 覆盖 GPUI 宿主；CI 补 Linux GUI 构建前置
- gpui 宿主补 D1/D7/D8/D9/D11；消除两处状态重复
- gpui 补 D3 思考块折叠；变更条自绘（渲染缝首个真实消费者）
- gpui 补 D6 轮耗时（可注入时间的纯逻辑，不破坏回放确定性）
- gpui 宿主：D4 工具调用分组（同轮连续调用折叠成组）
- gpui 宿主接入真实文本输入框（此前只能靠环境变量喂任务）
- gpui 宿主：D3 思考轨迹搜索（大小写不敏感、命中自动展开并高亮）
- scripts/make-app.sh：打最小 .app，解开 GUI 键盘自动化验证（回车提交由此有机器证据）
- gpui 宿主：D7 补齐目标设定入口与暂停/恢复/清除
- 转录区补显示引用解析结果与项目指令来源（此前只有 TUI 有）
- IME 回归用例：preedit 删除后重输 / 连续合成 / 测试基建自证
- D1 会话栏补状态圆点与改动行数（从会话日志派生，非新元数据）
- 可提取性守卫（空仓编译演练进每次门禁）+ 5 个可开源 crate 的 README/LICENSE/CHANGELOG
- neo-ui 首个自研组件 RichText + 组件展示台（storybook）
- 许可证合规守卫（进门禁）+ deny.toml（供 CI 用完整能力）
- 可开源集切到 Apache-2.0 + 第三方许可证清单（自动生成 + 门禁校验时效）
- gpui 转录区自动跟随（只在底部才跟随；真机验过"往上翻不被拽回"）
- gpui 模型选择器：列出全部模型（含说明与桩标记），取代左右盲切
- gpui 帮助面板（快捷键 + 全部命令，从共享表读）
- gpui 侧栏可折叠（状态栏开关，与 /sessions 同一 Action）
- gpui 转正：`neo desktop` 默认桌面 UI 换成 gpui（`--egui` 保留回退）
- 渲染缝第二个消费者：用量条形图（含 usage 演示桩）
- 渲染缝第三个消费者（分段进度条）+ 修两处只有真机能发现的布局缺陷
- storybook 补两个自绘组件条目（用量条形图 / 分段进度）
- gpui composer 改为多行：Enter 提交、Shift+Enter 换行（并修掉回车留下换行的缺陷）
  
  缺口表里"gpui 宿主无多行输入（`Shift+Enter` 什么都不做）"已补：输入框从单行
  `InputState` 换成 `TextareaState`，`submit_on_enter(true)` + `auto_grow(1, 6)`。
  
  **顺带抓到一个真缺陷**（端到端实测，读组件文档看不出来）：平台的 Enter 会被
  当作文本再送一遍（macOS 平台层给 Enter 的 `key_char` 就是 `"\n"`），而组件在
  "提交"分支里 `cx.propagate()` —— 于是**回车既提交、又留下一个换行**，输入框
  看着没清干净。修法是在 `composer_input` 里 `stop_propagation`，提交事件在那之前
  已派发，故不受影响。
  
  真机证据：`type` 两行 + `shift+return` 后，无障碍树里 `任务输入 = first\nsecond`；
  按 `return` 后输入框清空且无残留换行，会话日志为
  `{"begin_turn":{"text":"first\nsecond"}}` + `user_submitted`。
  
  4 条无头回归用例（`tests/composer_multiline.rs`）钉住多行语义；牙齿已验证 ——
  去掉 `submit_on_enter` 或去掉 `stop_propagation`，对应用例立刻变红。
- 渲染缝补上第二个后端（无头）—— 从"信仰"变成"设计"，并登记为 SPI
  
  `neo-ui-render` 长期只有一个 `GpuiBackend`，crate 头部自己写着"只有一个实现的
  抽象是信仰"。本轮补上 `HeadlessBackend`（无 GPU、可跑 CI），并用**同一份用例**
  （`tests/conformance.rs`，6 条）跑两个后端：度量逐字一致、颜色序列逐项相等、
  矩形逐一同位、能力边界诚实。
  
  两个后端的能力矩阵**故意相反**（文字：gpui 画不出 / 无头画得出）—— 这本身就是
  `supported()` 该属于后端而非中立层的证明。新增 `unsupported_ops` 把"场景里有
  后端画不出的指令"从静默消失变成可断言的事实；`StrokeRect` 从"静默不画"改为
  真的画（零线宽除外）。
  
  **这条缝因此第一次满足本仓的 SPI 门槛**（契约 + ≥2 后端 + conformance），
  已登记进 `check_spi_conformance.py`（SPI 7 → 8，未超上限 8）。
  
  诚实边界：无头后端是**对照物 / 测试替身**，不是第二个生产渲染路径；
  方案 Phase 4 要的 VelloBackend 仍未做（触发条件：主应用稳定 ≥6 个月 +
  ≥3 个自绘组件 + 专职人力）。storybook 补了变更条条目与"两个后端"条目
  （列出无头后端真的导出的 SVG）。
  
  **conformance 当场抓出两个缺陷**：① 我给无头后端漏了 `PopClip` 的颜色占位，
  导致两个后端对同一契约有两种实现；② 该占位被我自己写的 `debug_assert` 二次抓住。
  两个都已修 —— 这正是"第二个实现"的价值。
- 自绘 diff 行背景带：补上"改动落在哪几段"的形状（渲染缝第 4 个消费者）
  
  两个 GUI 宿主的 diff 正文此前都是**逐行彩色文字**（绿字=新增、红字=删除），
  但底色与其它内容一样。逐行彩色只标注了单行语义，给不出"改动落在哪几段"的形状 ——
  而看 diff 的第一个问题恰是那个形状。真实 diff 查看器（GitHub / Zed / ZCode）
  都有**行背景带**，这是让 diff 能扫读的关键层。新增 `neo-ui-render::diff_backdrop`
  （P2 自绘 DiffView 的第一层），gpui 宿主接上。
  
  分工是**缝画底、宿主画字**：底带是矩形填充（缝的强项），文字留在 element 树
  （保住 gpui 的文本整形、字形缓存与 element diff）。对齐**由构造保证**而非调间距 ——
  底带的 y 用 `i * lh`、文本容器设 `.line_height(lh)` 且每行 `whitespace_nowrap()`，
  `lh` 取自 `Window::line_height()`，两边同源。
  
  真机验证（按窗口 ID 截图，不抢焦点）：红底带 130pt 覆盖 5 个删除行 = 每行 26pt，
  与行高一致；hunk 头淡蓝、新增行绿、上下文行无底。
  
  7 条单测钉住对齐契约（第 i 条带子落在 `[i*lh,(i+1)*lh)`）、上下文行不画也不占位、
  三色可区分且 alpha<128（不抢文字对比度）、裁剪配平且裁到自身总高、5000 行仍线性。
  该场景已进 conformance 的跨后端用例集。
- diff 未改区块折叠：行为层新增可折叠逻辑（对当前 diff 形状**休眠**，据实标注）
  
  新增 `neo-ui-behavior::fold`：把长 diff 里成片的未改上下文收成一行"⋯ 未改 N 行
  （点击展开）"把手。三条语义都钉了测试：① 只折 `Context`，`Header`（文件头）与
  `Meta`（"…另有 N 处"截断说明）**绝不折** —— 折了会把「这段属于哪个文件」
  「这个 diff 被截断过」藏掉；② 折了必须**真的净省行数**（折 3 行多一个把手是负收益）；
  ③ 紧邻改动的上下文保留 3 行（理解改动需要它）。另钉住"折叠只隐藏、绝不重排"。
  
  为支撑①，`DiffBand` 从"改动色 / 其它"补成**完整分类**（新增 `Header` / `Meta` /
  `Fold`）—— 曾经的 `_ => Context` 会把结构行混进可折的上下文。宿主侧分类+
  折叠抽成纯函数 `diff_display_rows`，并断言"底带必须与显示行同长"（折叠改变了
  显示行数，底带沿用原行数就会错位）。
  
  **⚠️ 诚实边界：对我们自己生成的 diff，折叠是休眠的。** 实测
  `neo-capability::diff::unified_diff` 每 hunk 只带 `CONTEXT = 3` 行上下文，
  最长连续上下文就是 3 行 < 阈值 6，故**永不触发**。已用测试钉住这个事实
  （`our_own_diff_shape_does_not_fold`），防止有人为了让功能"看起来在工作"而
  调低阈值（那是负收益）。它会在"上下文半径调大 / 喂进完整上下文 diff /
  并排视图"任一情况出现时立刻生效，不需改本模块。
  
  真机验证（**按窗口 ID 截图，不抢用户焦点**）：全文件写入型 diff（30 删除行）
  底带逐行对齐、无折叠行 —— 与"休眠"的判断一致。宿主接线另用 3 条无头测试证明
  （人工喂长上下文时会真的折、展开态全部铺开且保留把手、底带始终同长）。
- diff 行内强调：把真正变化的字再点亮一次（用更浓的底，而不是粗体）
  
  只按行着色时，一行 200 字符里改了一个词，整行都是绿的 —— 用户还得自己逐字找。
  新增 `neo-driver::transcript::inline_emphasis`：按 unified diff 的"Del 块紧跟 Add 块"
  形状按序配对，字符级比对出真正变化的片段（字节区间）。判定放共享层，
  两个 GUI 宿主共用（否则同一份 diff 在两处强调的位置不同）。
  
  **三处实测抓到的坑**（都不是编译错误）：
  1. **`similar::from_chars` 给的是 char 下标、不是字节偏移**（实测 `-中文旧内容`：
     6 char / 16 字节）。直接当字节用会切在字符中间甚至 panic，故建 char→byte 映射。
  2. **`\ No newline at end of file` 会打断配对** —— 我们自己的 `apply_patch` 常产出它，
     它在 `diff_line_kind` 里是 `Meta`，夹在 Del/Add 之间 → 行内强调在**整文件重写**
     （最常见的一类）上永不生效，屏幕上只表现为"没有强调"。已改为跳过该标记。
  3. **粗体在中文上等于没做**：CJK 字形普遍无真粗体字面，`FontWeight::BOLD` 真机看不出
     差别。改用"更浓的同色系底"（29 → 89，差 60，像素可断言），与字体无关。
  
  另修一个渲染缺陷：强调片段最初直接塞进**纵向** flex，导致每段各占一行
  （真机看到"行首 `-` 单独一行、内容另起一行"）—— 片段必须用横向容器并排。
  
  验证：19 条新测试（共享层 15 + 宿主 4，含真实字符串用例与"越界/倒序区间不 panic"），
  真机（按窗口 ID 截图、不抢焦点）实测强调底与行底差 60 个色阶。
- D12 文件树：工作区文件索引 + 面板 + 点文件加 `@引用`（并补上引用格式化）
  
  parity 清单里唯一还是 ❌ 的项（D12）做了第一层：**文件树**。
  
  - **`neo-platform::file_index`**（L0，新）：有界的工作区扫描。遵守
    `.gitignore`/`.ignore`、跳过隐藏文件、**绝不跟随符号链接**（终止性要求，
    不是效率优化）、条目/深度/时间三重上限并**如实上报 `truncated`**。
    跨平台可复用（TUI 将来用同一份），避免"什么该忽略"出现第二套实现。
  - **`neo-protocol::format_file_ref`**（解析的逆，此前**只有解析没有格式化**）：
    路径含空格时必须 `@"my file.txt"`，否则下次解析会把它切成两个词、静默丢掉引用。
    已用往返用例钉住（含空格/两种引号/全角空格/含 `#` 的路径），并验证过牙齿。
  - **gpui 宿主**：`/files` 命令 + 状态栏开关 + 覆盖式面板；点文件**追加**
    引用到输入框（不覆盖用户已写内容）。egui 侧如实提示"仅 gpui 可用"，
    不留"列着却点了没反应"的假功能。
  
  **实测抓到的两个坑**：
  1. `ignore` 的 `require_git` **默认 true** —— 非 git 目录里 `.gitignore`
     完全失效（本仓的临时目录测试就是这么红起来的）。文件树必须显式设 false。
  2. `NEO_GUI_PANEL=files`（启动即打开）走构造函数、**不经过 `run_action`**，
     于是惰性扫描从未触发、面板永远停在"正在扫描"。改为让"面板可见 ⇒ 索引就绪"
     成为自足条件。
  
  真机验证（按窗口 ID 截图，未抢焦点）：树正确渲染，`debug.log` 与
  `target/debug/big.bin` 被正确排除、`.gitignore` 自身不可见。
  扫描准确性另与 `git ls-files` 交叉核对：本仓 342 个文件 vs git 跟踪的非隐藏
  341 个，唯一差异是当时未跟踪的新文件 —— 既无泄漏也未过度排除。
  
  **诚实边界**：不做文件监听（会话中途新建的文件需点"刷新"）；不画可折叠树
  （层级由路径表达）；点击是"加引用"而非"打开文件"（内置浏览器仍未做）。
- 审批预览的 diff 上下文 3 → 10（并让渲染层的折叠真正生效）
  
  用户拍板：把 `neo-capability::diff::CONTEXT` 从 3 提到 10。
  
  **为什么改**：3 是**未经审视的 unix 默认**（原注释只说"保留的上下文行数"，
  没有任何理由）。它对"补丁邮件逐行读"够用，但对**审批预览**不够 ——
  用户要据此判断改动对不对，3 行常常交代不清（前面是什么函数、变量从哪来）。
  改它有两个独立好处：① 审批时看到更多上下文；② **让折叠未改区块真正生效** ——
  折叠阈值是 6，而 3 行上下文永远达不到，于是那个功能实现了却永不触发
  （§4.64(az) 记的正是这件事）。
  
  **顺带**：半径变大也会让相距 < 2×半径 的改动**合并进同一个 hunk**，
  `@@` 头数量反而变少（可读性提升，不只是变长）。
  
  **新增不变量测试**（这是本轮最重要的产出）：此前 `CONTEXT` 与折叠阈值之间的
  耦合**没有任何东西守着** —— 谁把 CONTEXT 调回 ≤6，折叠会再次静默失效，而界面
  只表现为"没有折叠把手"，不报错。故加断言：
  `fold_threshold_stays_below_the_context_radius`（参数关系）+
  `a_real_approval_preview_actually_folds`（用真实生产者端到端验折叠真的发生）。
  已验牙齿：把 CONTEXT 改回 3 会立刻红并给出可操作的报错。
  
  **顺带修两处"钉死实现细节"的测试**：`hunk_header_tracks_...` 与 TUI 的跨 crate
  往返用例都把"两个改动相距 20 行"写死，半径变 10 后它们被合并成一个 hunk 而失败。
  改为**由 CONTEXT 推出距离**（`10 + 2*CONTEXT`），断言"关系"而非"绝对数字" ——
  这样以后再调半径不会再打断它们，而它们要守的回归（两侧起始行号必须错开）仍在。
  
  **新增 `--provider edit` 演示桩**：改大文件里的一行，产出**带上下文**的 diff。
  其余桩（selftest 整文件重写 / multitool 只有 bash）都产不出这种形状，于是
  "审批时能看到多少上下文"此前**没有可离线复现的观察窗口**。
  
  真机验证（按窗口 ID 截图，未抢焦点）：hunk 头为 `@@ -10,21 +10,21 @@`
  （半径 10 的确切形状）；25 行 diff 折成 14 行显示，`⋯ 未改 4 行`（13–16）与
  `⋯ 未改 7 行`（24–30）与算法逐项对得上；行内强调同时生效。
- D12 内置浏览器：文件预览（点文件看内容，引用成为独立动作）
  
  D12 第二层：**内置浏览器（文件内容预览）**。
  
  - **`neo-platform::file_index::preview_file`**：有界、且**限定在工作区内**的读取。
    三种形态各自如实呈现 —— 文本（逐行 + 行号）/ **二进制**（识别 NUL，不当文本
    渲染成乱码）/ 读不出来（给原因）。非 UTF-8 用 lossy 转换（"能看个大概"好过
    整个文件打不开）；单行长度封顶防一行几 MB 的压缩文件。
  - **gpui 宿主**：面板改为两栏（文件列表 + 预览栏）。**点击文件 = 预览**，
    "加入引用"成为预览栏里的**独立按钮** —— 此前点击只有加引用，意味着"想看内容
    只能先把整个文件注入模型上下文"，既花钱又绕。
  - 新增 `NEO_GUI_PREVIEW=<相对路径>` 启动钩子（与 `NEO_GUI_PANEL` 同族）：
    自绘预览栏收不到合成输入时，脚本化截图验证需要这个入口。
  
  **安全边界**：预览不走沙箱（内容只给人看、不进模型上下文），但必须自己限定
  工作区。**两道独立的闸**（实测确认：关掉 `is_within` 时只有软链那条测试会红）：
  输入含 `..`/绝对路径由入口检查拦；看起来正常的 `link/secret.txt` 只有
  canonicalize 后比前缀才拦得住。`is_within` 在 L1 本地实现（L3 的
  `neo-sandbox-local` 那份不能向上依赖，否则违反架构守卫）。
  
  **长行裁切连踩四次才对**（真机截图发现的，前三版都越界）：
  1. 抄 diff 正文的 `whitespace_nowrap` → 长行横向冲出面板、盖到主界面；
  2. 改折行 → 仍越界（一长串无空白字符**没有折行断点**）；
  3. 误用 `text_ellipsis()` → 它**只设样式**、不含 `overflow_hidden`；
  4. `truncate()`（三者合一）**加上整条 flex 链的 `min_w(0)`** → 成功。
     flex item 的 `min-width` 默认是 `auto`，不给 `min_w(0)` 则 `flex_1` 的
     "可收缩"形同虚设，`overflow_hidden` 无从裁起。
     客观验证：长行最右像素落在面板右边界（1879 = 940 逻辑点 × 2）。
  
  顺带把数据层的"已截断"标注文字去掉 —— 实测它会被省略号裁在可视区外，是死重。
  **数据层管封顶、渲染层管提示**，各做各的不重复。
  
  真机验证（`NEO_GUI_PREVIEW` + 按窗口 ID 截图，全程未抢焦点）：文本逐行带行号、
  二进制显示"6 字节 不按文本显示"、长行以 `…` 在面板边界处裁切。
- 自绘 UI 精细度第一步：渲染缝支持圆角 + 文件列表行改为真正的列表项
  
  用户反馈"自绘组件 UI 比较粗糙、不到直接上生产的标准"。先审计出**可核对的具体项**，
  再改收益最大的两处。
  
  **审计结论（客观可查，不是印象）**：
  | 项 | 实测 |
  |---|---|
  | 圆角 | 渲染缝的 `Op` **没有半径概念**，后端一律 `Corners::default()`（= 全零直角）—— 所有自绘元素都是硬边 |
  | 悬停反馈 | 宿主 22 个 `on_click` 元素，`hover(` 出现 **0 次** —— "能点"却看不出能点 |
  | 字号层级 | `font_weight` / `text_size` 出现 **0 次** —— 标题与正文同字号，没有信息层次 |
  | 列表项形态 | 文件行是裸文字：贴面板边缘、无内边距、无行高、无选中/悬停态 |
  
  **本轮改的两处**：
  
  1. **渲染缝支持圆角**（`Op::FillRect/StrokeRect` 增 `radius`，两个后端都如实传达；
     SVG 导出写 `rx`）。半径**由调用方从设计系统取**（`neo_ui::RADIUS = 6.0`，即
     主题 `radius` medium），不在渲染层写死 —— 与颜色同一条路径。
     **默认 0（直角）是刻意的**：diff 底色带那种整行通铺的面必须直角
     （圆角会让相邻行之间出现缝隙，看起来像"改动行数不对"）。
     组件各自选择：进度段圆角、用量柱顶圆角、变更条与 diff 底带直角；
     圆角一律**先夹到半宽/半高**再入场景 —— gpui 自己夹、SVG 不夹，
     在场景里夹一次才能保证两个后端结果一致。
  
  2. **文件列表行改为真正的列表项**：内边距 + 行高 + 选中底色 + 悬停底色
     （悬停取值跟设计系统的习惯走 `muted` 半透明，不另挑颜色）。
  
  新增两条跨后端契约：半径必须**逐一相同**地传达（且断言"真实场景里确实出现过
  非零半径"，否则是在测全 0）、SVG 必须写出 `rx`。牙齿已验证（丢掉半径→红）。
- UI 精细度第二步：排版层级（data-driven 角色）+ 间距归到设计系统 scale
  
  继续上一轮的"粗糙清单"，本轮做**字号层级**与**间距 scale** —— 这两项对观感的
  贡献比圆角更大。
  
  **审计出的第二个硬数字**：宿主里 `font_weight` / `text_size` 出现 **0 次**，
  且间距里有**离 scale 的原始值**（`.py(px(3.))` —— 设计系统的档位是
  2/4/8/12/16/24/32，没有 3）。
  
  **改法上刻意避免"逐处挑字号"**（那会得到十几种组合，正是拼凑感）：
  新增 `neo_ui::text_role`，按**角色**给样式，且角色是**数据**（`Role { size, bold, tone }`）
  而不是写死在函数里 —— 这让"层级存在"成为**可断言的**：
  若四个角色被改成同一组值，测试会红；写死在函数里就只能靠肉眼发现"界面又变平了"。
  
  四个角色与依据：
  | 角色 | 规格 | 用在 |
  |---|---|---|
  | `SECTION_TITLE` | 14px + SEMIBOLD + Info | 面板标题（文件树、快捷键、命令数） |
  | `BODY` | 16px + 常规 + Text | 正文、文件行、内容 |
  | `META` | 14px + 常规 + Muted | 说明文字、路径、时间戳 |
  | `TINY` | 12px + 常规 + Muted | 行号、角标 |
  
  数值取自设计系统的 `TypographyTokens`（xs=12/sm=14/base=16）；**标题取"小一号"
  而非"大一号"**是工具型界面的惯例（Zed / VS Code）：正文是主角、字号偏小换信息
  密度，标题靠字重与颜色区分。
  
  **层级不只靠字重**：结合 §4.64(ba) 的教训（CJK 常无真粗体字面，bold 可能看不出来），
  标题同时用更亮的语义色、说明同时用弱化色 —— 两条路径叠加，任一条在某字体上失效
  仍可分辨。
  
  间距：5 处离 scale 的原始像素归到 `py_0p5()`（2px）/ `py_1()`（4px），
  宿主里已无 raw `px()` 间距。
  
  新增测试 `text_roles_form_a_real_hierarchy`（断言彼此不同 + 标题不比正文大 +
  标题加粗 + 三种颜色互不相同）。牙齿已验证：把标题抹平成正文立刻红。
  
  **顺带把上一轮标注为"未测"的暗色对比度补上**：实测 `Text` 17.9 / `Muted` 6.1 /
  `Info` 12.6（黑底）与 14.0 / 4.8 / 9.8（面板底），全部 ≥4.5:1（WCAG AA）。
  并固化成守卫（`neo-text` 的 `text_and_muted_stay_readable_on_both_backgrounds`）
  —— 对比度是**最容易被静默破坏**的观感：调暗 `muted` 看起来更雅致，实际让说明
  文字变难读，不报错只费眼。牙齿已验证（调暗到 0x3a 立刻红）。
- UI 精细度第三步：输入框焦点指示 + 面板入场动画（尊重 reduce_motion）
  
  继续"粗糙清单"，本轮做**焦点环**与**过渡动画**两项。
  
  **焦点指示（真机可判定）**：输入区是 `.appearance(false)`（无边框，与转录区
  连续排版一致），于是此前**聚焦与不聚焦长得一模一样**，唯一差别是光标在闪 ——
  而光标会在截图里熄灭（本文件另一处注释就记着这件事）。对以打字为核心的界面，
  "看不出焦点在哪"是实打实的可用性问题。
  
  修法：聚焦时在输入区下方画一条品牌色细线（用底线而非整圈边框，不破坏"无框"观感）。
  焦点状态由 `InputEvent::Focus/Blur` 事件维护。
  
  **真机证据（数值，不是目测）**：聚焦时底部品牌色像素 **1988**，
  焦点移走后 **0**。同时 `NEO_GUI_FOCUS` 钩子报告 `composer_focused` 在
  `true`/`false` 间正确切换。
  
  **面板入场动画**：短促下滑 + 淡入（150ms，与设计系统弹层的
  `DROPDOWN_ENTER_DURATION` 同值；位移 6px 比弹层的 8px 略小）。覆盖式面板
  （文件树 / 帮助 / 模型选择 / 命令面板）都接上。
  
  **只给面板加、不给列表行加** —— 设计系统里 `with_animation` 只用在弹层上
  （dialog / popover / sheet / notification），列表行没有入场动画。跟它的习惯走，
  否则要么显得浮夸、要么与组件库控件不一致（与上一轮同一个判断）。
  
  **自动尊重 `reduce_motion`**：走 gpui 的 `with_animation`，它在系统要求减少动效时
  直接渲染终态、不排动画帧。**没有**自己写 `cx.spawn` + 定时器刷新 ——
  那会让这个系统级设置失效，等于替动效敏感的用户做决定。
  
  一处取证方法的迭代值得记：验证"动画确实走了那个入口"时，前两版判据都失败 ——
  ① `include_str!` + 文本断言**自我命中**（断言里的字面量本身就在文件里）；
  ② 比较 `Debug` 输出，而 `Div` / `impl IntoElement` 都**没有 `Debug`**。
  最后让 `panel_enter` 返回**具体的 `AnimationElement<Div>`**，于是"走了动画入口"
  成为**类型层面的事实**（自己写循环不可能产出这个类型）。
  **能改签名让事情可证，就别绕道去证。**
- D12 可折叠文件树：把"一堵路径墙"变成层级（默认全折叠）
  
  文件树此前渲染**扁平全路径**。在真实仓库上这是"路径墙"：本仓 342 个文件
  排成一列，且每行重复长前缀（`crates/neo-platform/src/`），横向空间几乎全花在
  重复信息上。Zed / VS Code / ZCode 都用缩进树 + 可折叠。
  
  新增 `neo-ui-behavior::tree`（纯逻辑，两个宿主共用）：
  - **默认全折叠**（只显示根级）—— 这是本模块存在的直接理由：默认全展开的话
    342 个文件仍是一堵墙（只是换了缩进），等于没做；
  - 只显示**确实含文件**的目录（目录集合从文件路径派生，不额外收一份）；
  - 同级内**目录在前**，各自按名字排序；
  - 缩进**有上限**（8 级）—— 极深路径会把名字挤出面板右边界，那时缩进反而挤掉内容。
  
  宿主：目录用 ▸/▾ + **直接子项数**（"点开一下会多出几行"可预期）、缩进渲染、
  点目录切换展开/点文件预览；面板加"展开全部 / 全部折叠"一键开关。
  
  **真实仓库实测**（本仓路径清单）：折叠态 **16 行**（6 个根级目录 + 10 个根级文件）、
  全展开 **465 行 = 344 文件 + 121 目录**（内部自洽），目录确实都在文件之前。
  
  展开状态**按路径记**（不按下标）：文件列表会被重扫（点刷新、将来接文件监听），
  下标会整批错位 —— 那时"我展开的目录"会变成别的目录。重扫时按路径保留、
  并清掉已不存在的目录。
  
  13 条纯逻辑测试 + 2 条宿主接线测试（折叠态行数、展开一层不显孙项、
  重扫后按路径保留）。真机截图确认：真实仓库上折叠态是 7 行而不是 342 行。
- D12 文件监听：文件树在磁盘变化时自动更新（事件驱动，无轮询）
  
  补上 D12 最后一块：文件索引此前是**一次性快照**，用户新建/删除文件后树不变
  （得手动点刷新）—— 树显示的是**过期事实**，而用户不知道自己看的是过期的。
  
  - **`neo-platform::file_watch`**：事件驱动 + **容量 1 通道天然合并**。文件系统
    事件会成串到达（保存一次文件、`git checkout`、一次构建能触发成百上千条），
    合并方式**不需要定时器**：通道满了就丢，消费端取走后下一个事件才重新产生信号
    —— 与效率规范一致（等待必须有条件，这里连"等"都没有，只是合并）。
  - **噪声用"扫完再比较"消除**，而不是给事件再实现一套 `.gitignore` 规则
    （那会造成两个来源、必然漂移）：新索引与旧索引相同就什么都不做。
    `.git/` 内部在事件层直接跳过（最大的噪声源，且永远不影响索引）。
  - **启动在后台线程**：实测 FSEvents 的 `watch()` 要 **6.2 秒**（起 CFRunLoop
    线程 + 注册事件流），放渲染路径上会让窗口卡死 6 秒。建好后经通道交回视图安装。
    失败不致命（手动刷新仍是退路），只在 `NEO_GUI_WATCH` 下打印。
  
  **真机验证**（数值 + 截图）：外部创建文件 → `take_pending=true` →
  `rescan: 旧 1 新 2 有变化=true` → 树出现新文件，**全程无用户操作**。
  
  **顺带修一个既有真缺陷**：唤醒循环里只调 `cx.notify(entity_id)` 是**不够**的 ——
  gpui 自己在 `AsyncApp::refresh` 的注释里写明"直接调用会留下排队的刷新效果，
  **唤不醒已经停下的平台渲染循环**"。这个缺陷**在轮次运行时不显现**（那时 `pump`
  自己 notify、循环一直醒着），只在**空闲时刻**（文件监听正是这种场景）才暴露。
  修法：标脏 + `refresh()` 两步都要。

### Patch Changes

- Web 宿主 HTTP 层补 16 项集成测试；修掉 SSE 空转连接不释放槽位/线程的泄漏
- 桌面 GUI 阶段 B 实测：egui 走通、gpui 因 Metal 工具链退出对比；记录 egui 0.36 API 事实
- diff 换用 similar（去掉自写 LCS），修 hunk 头行号与摘要无界两处缺陷
- markdown 换 pulldown-cmark（修跨折行标记残留 bug）；实测修正阶段 C 的行数收益预估
- 修 GUI 中文豆腐块（加 CJK 字体加载 + 字形自检门禁）；补端到端界面测试
- 修 preview 与 execute 解析不一致（审批 diff 可能指向别的文件）；输入框自动聚焦
- gpui 宿主：补发 Op::GoalAdvance —— 目标此前只有第一个子任务会跑
- 文档：release.yml 注释对齐 gpui 转正；记下删 egui 的三条前置条件
- 文档：写清 Linux 桌面所需的第二组系统库（gpui 走 dlopen，编译不需要、运行缺了会 panic）
- 记忆整理：4.64 小节重排为时间序 + 加会话导读 + 修三处过时陈述
- CI：失败信息改为免认证可读（注解）+ 失败用例逐条重跑打印完整输出
- neo-sandbox：三个平台后端各补测试（此前 Linux/Windows 后端零覆盖）
- 记忆：CI 可观测性教训（读不到日志本身是缺陷）+ 沙箱后端零覆盖缺口
- CI 失败摘要：把内联 grep 换成可回放的 `scripts/ci-failure-digest.sh`
  
  上一版在 workflow 里用 `grep -i 'error|failed'` 发注解，两个硬约束叠加导致
  **真因读不到**：GitHub 每个 step 只保留前 10 条 error 注解，而这条 grep 会把
  `Compiling thiserror`、`test result: ok. 0 failed` 两类噪声排在最前，配额被
  吃光，真正的 `FAILED`/`panic` 行一条都没发出去。新脚本按价值排序（失败用例名 →
  panic 上下文 → 编译错误 → 链接期失败），error/warning 各 10 条，模式行首锚定
  且大小写敏感；并带 `--selftest` 自检，由 `verify.sh` 第 6 段执行 —— 已实测把
  模式改回旧的 `-i` 写法会立刻变红。
- neo-host-egui 端到端用例：验 UI 卡片别拿"平台相关的沙箱能力"当前提
  
  `tool_cards_carry_name_args_and_output` 用 `ExecMode::Default`（→ 沙箱档
  `WorkspaceWrite`）真的跑 `bash echo`。而 `LocalSandbox` 的受限档在 Linux 上
  fail-closed（尚无 Landlock+bwrap 实现，拒绝执行），于是该用例在 Linux CI 上
  稳定失败 —— 命令根本不执行，`exit_code` 是 -1 而非 0。
  
  这正是 Linux CI 长期红的**唯一**失败用例（已用"临时把 `supports()` 的受限档
  改成 false"在本机复现：1 failed / 5 passed，`Some(-1)` vs `Some(0)`）。
  
  修法是改用三平台都支持的 `FullAccess` 档（沙箱档 `DangerFullAccess` 恒可用），
  而不是跳过 Linux —— 用例的主语是界面数据通路（名字/参数/输出），与沙箱档位无关。
  用 `Default` 档的其它用例（如审批前 diff）不受影响：它们在**执行前**就挂起审批。
