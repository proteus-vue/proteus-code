# PROJECT_MEMORY.md — NEO 项目记忆

> 交接文档。记录**为什么这样做**、**踩过什么坑**、**哪些路走不通**。
> 结构看 `README.md`，设计看 `docs/neo-plan/`。这里只写那两处没有的。

---

## 0. 分发与发布（2026-09-14）

从"只能自己 `cargo install --path`"到"别人能装"。本节记录**为什么这样设计**
与踩到的坑。

### (a) 三条路径，各解决一类用户

| 路径 | 用户侧 | 何时用 |
|---|---|---|
| `scripts/install.sh` + GitHub Releases | `curl \| sh`，无需 Node/Rust | 推荐 |
| `npm install -g @proteus-vue/neo-code` | 需 Node | 有 Node 环境时最省事 |
| `cargo install --git ... -p neo-code-cli` | 需 Rust | 全平台 / 自定义 feature |

三者共用一件事：**包名与命令名分离**。crates.io 上 `neo-cli` 与 `neo`
都已被占用（NeoRust SDK / Neocities CLI），故 Cargo 包名取 `neo-code-cli`；
但 **bin 名始终是 `neo`** —— 用法文本、会话文件名、用户肌肉记忆都不必改。
判据同 §1.5 的 `dsh` 那条：改的只是"发布标识"，不是"产品名"。

### (a2) 为什么最终没走 crates.io（2026-09-14 决定）

一度把 crates.io 也做成路径（23 个 crate 补了完整元数据、写了拓扑序发布
脚本、`cargo package` 全部验证通过），随后决定**不发布**：价值有限 ——
`cargo install --git` 已经覆盖"有 Rust 工具链"的用户，而 crates.io 相对它
只省一次 clone，却要求长期维护 23 个包的版本同步。元数据与 `version` 字段
**保留**（它们本身是正确性改进，也让 `--git` 安装更规范），只删掉发布脚本。
取而代之补上 **npm**（见 b2），因为 Node 覆盖面比 Rust 广得多。

### (b) 桌面宿主拆成可选 feature（Linux 用户的真痛点）

`neo-host-desktop`（wry/tao）是**唯一引入平台图形依赖**的宿主：
Linux 上要 `libwebkit2gtk` 才能编译。此前它被无条件链入 `neo`，
于是**只想用 TUI 的 Linux 用户也得装一整套 webkit**。

现在 `neo-code-cli` 有 `default = ["desktop"]`，另可
`--no-default-features` 得到精简版。**实测证据**（`otool -L`）：
默认构建链 WebKit + AppKit 两个框架，精简构建 **0 个** —— 不是"据说更小"，
是链接层面确实没有。精简版下 `neo desktop` 由 `main` 的占位分支给出
明确提示（退出码 2），不是静默失败。

默认**开启** desktop 是为了让门控代码总被编译（不腐烂）；
CI 另跑 `-p neo-code-cli --no-default-features` 验证精简组合仍有产物。

### (b2) npm 包：平台包 + 主包，安装期零脚本

npm 侧是**两个层次**（包名统一在 **`@proteus-vue`** scope 下 —— 发布凭证是
该组织的）：

| 包 | 内容 |
|---|---|
| `@proteus-vue/neo-code` | 只有 `bin/neo.js`（约 2 KB）+ `optionalDependencies` |
| `@proteus-vue/neo-code-darwin-arm64` / `-darwin-x64` / `-linux-x64` | 各自的 `bin/neo` 二进制 + `os`/`cpu` 字段 |

**scoped 包发布必须带 `--access public`**：npm 对 scoped 包默认按 restricted
处理，首次发布不带这个标志会失败。已固化在 `publish-npm.sh` 的
`publish_or_pack`，不要去掉。

另两个 scoped 特有的细节：① 包名含 `/`，**不能直接当工作目录名**（脚本用
`slug()` 转成 `_scope_name`）；② `node_modules` 下确实是嵌套布局
（`@scope/name`），故 dry-run 冒烟时拼 `$nm/$pkg` 是对的，但要先
`mkdir -p "$nm/@scope"`。

npm 按 `os`/`cpu` 只装匹配的平台包；主包的 wrapper 用
`require.resolve('<平台包>/package.json')` 定位真二进制并**原样转发**
（参数、退出码、stdin/stdout/stderr、信号都不改写）。

**为什么不用 postinstall 下载**（这是关键设计决定）：安装期执行脚本、
联网抓可执行文件，正是**供应链注入的典型入口** —— 与 §0 里
"可执行文件就是供应链注入"是同一条判据。平台包方案让 **install 期
不执行任何脚本、不联网**，离线与内网也照常工作。代价是每个平台一个包，
发布时 `scripts/publish-npm.sh` 一并处理。

### (c) 坑：含二进制目标的 crate，`cargo package` 会做完整依赖解析

`cargo package` 对 22 个 lib crate 都成功，**唯独 `neo-code-cli` 失败**，
报 `no matching package named 'neo-capability' found`。一度怀疑是
feature 门控或 dev-dependency，逐一排除后做了对照实验：

```
给 neo-exec（纯 lib、打包正常）临时加一个 [[bin]]
→ 它也失败了（报找不到 neo-agent-loader）
```

根因：**有二进制目标的 crate 在打包时要解析完整依赖图并对齐
`Cargo.lock`**（bin 需要锁文件），纯 lib crate 不需要。而本地所有内部依赖
都是 `path`，registry 里找不到，于是只有含 bin 的那个失败。

**这不是缺陷，是本地打包的必然假象**：按拓扑序发布时前面的依赖已上传，
它就能打包。验证方式——把依赖放进本地目录 registry 再打包，
`neo-code-cli` 立刻成功（`Packaged 6 files`）。

### (c2) 坑：bash 3.2 把多字节字符吃进变量名
macOS 自带的是 **bash 3.2**。写 `"版本 $VERSION）"` 时，它会把全角
`）` 的字节也算作变量名的一部分，于是报 `VERSION）: unbound variable`。
修法是**用花括号界定**：`"版本 ${VERSION}）"`。这类问题只在"变量后紧跟
非 ASCII"时出现，`bash -n` 查不出来（语法合法），只有真跑才暴露 ——
`scripts/` 里所有中文字符串都要注意。

### (d) 许可证三处不一致（发布前必须统一）

`Cargo.toml` 写 MIT、README 写 Apache-2.0、`LICENSE` 正文是 Apache-2.0
但**标题写着"Proteus（普罗透斯）Vue 跨端编译框架"**（像是从别的项目拷来的）。
已统一为 **MIT**，并把 README 协议段改成指向该文件、说明与 DSH（MIT）兼容。

### (d2) npm 的 token 机制在 2025-11 变了（Automation token 已消失）

**现象**：按老教程去 npm 建 "Automation token"，界面上找不到、建不出来。
**原因**：npm 于 **2025 年 11 月移除了全部 legacy/classic token（含 Automation）**，
现在只有 **Granular access token**。这不是操作失误，是机制变更。

两条可用认证路径（`release.yml` 的 `publish-npm` job 两条都支持）：

| 路径 | 怎么做 | 评价 |
|---|---|---|
| ① **OIDC Trusted Publishing** | npmjs.com → 各包 Settings → **Trusted Publisher** → 选 GitHub Actions，填 org/repo/**workflow 文件名**（`release.yml`，区分大小写）；仓库加变量 `NPM_TRUSTED_PUBLISHING=true` | **推荐**，无需任何 secret、无长期凭证 |
| ② Granular token | npmjs.com → Access Tokens → Generate New Token → Packages and scopes 选 **Read and write** → 设过期（至少 1 天后）→ 存成仓库 secret `NPM_TOKEN` | 应急用 |

**注意两条时间线**：npm 计划 **2027-01 起移除 granular token 的直接发布权限**
（届时须走 `npm stage publish` + 人工 `npm stage approve`），所以 ① 才
是长期解，② 只是过渡。

**bootstrap 顺序**（官方文档只写了"在包的 Settings 里配置"，没给未发布包的
流程，所以首次发布仍是鸡生蛋）：先用 ② granular token 把包发出去 →
包在 npm 上存在后，才能进它的 Settings 配 ① 的 Trusted Publisher →
之后就可以撤掉 token。

**Trusted Publishing 的硬性前提**（漏一条都会失败）：npm CLI ≥ 11.5.1、
**Node ≥ 22.14**（故 `publish-npm` job 用 Node 24 而非 20）、GitHub **托管**
runner（自托管不支持）、`id-token: write` 权限、workflow 文件必须在
`.github/workflows/` 且**文件名与 npm 侧填写的完全一致**（大小写敏感）。
另：若同时存在 `NODE_AUTH_TOKEN`，npm 会优先用 token 而绕过 OIDC ——
切到 ① 之后**记得把 secret 删掉**。

### (d3) 发版改成 changeset 流程，但**不用 `@changesets/cli`**

要求是"把发版改成 changeset 方式"。这里有个必须讲清的取舍：

**没有用 `@changesets/cli`**，因为它只认 npm 工作区的 `package.json`，
而本项目的版本真源是 `Cargo.toml` 的 `[workspace.package] version`。
用它等于让工具去管**非权威**的那份版本号，再手写胶水把权威的那份同步过去
—— 本末倒置；还会往纯 Rust 仓库塞一整套 Node 工具链（与项目一贯的零依赖
立场相悖：TUI 零依赖、HTTP 手写、不引 reqwest/axum）。

**只借了 changeset 的"精髓"并自己实现**（`scripts/changeset.py`，纯 Python 标准库）：
每个改动写一条 `.changeset/*.md` 碎片，由机器聚合成版本号与 CHANGELOG。
它是**单一版本工作区**，所以碎片只写 bump 级别、不写包名。

| 角色 | 实现 |
|---|---|
| 碎片 | `.changeset/<时间戳>-<slug>.md`，前区 `bump: patch\|minor\|major` |
| 聚合 | `changeset.py version`：推进版本 → 同步内部依赖 → 刷 Cargo.lock → 写 CHANGELOG → 删碎片 |
| Version PR | `changeset-release.yml`：合并 main 后开/更新 `changeset/release` 分支的 PR |
| 门禁 | `changeset.yml`：产品面（`crates/`、`npm/`）改动必须带碎片，`no-changeset` 标签豁免 |

**两个非显然的实现点**（都是踩过才定的）：

1. **内部依赖版本必须一起改**。crate 之间写的是
   `neo-protocol = { path = "...", version = "0.1.0" }`，而 semver 里
   `^0.1.0` **不匹配** `0.2.0`（0.x 有特殊规则）。只改工作区版本会让
   cargo 直接失败（实测确认，不是推测）。故 `version` 子命令会同步所有
   22 个 crate 清单里"带 path 且版本等于旧值"的行 —— 用这个条件保证
   `serde = "1"`、`wry = "0.46.1"` 之类外部依赖绝不被误伤。

2. **机器人推的 tag 不会触发 release workflow**。GitHub 规定
   "用默认 `GITHUB_TOKEN` 做的事件不再创建 workflow run"，而
   `workflow_dispatch` / `repository_dispatch` 是**唯二的例外**。
   所以 `changeset-release.yml` 推完 tag 后**显式 dispatch** `release.yml`
   —— 确定性做法，且不需要额外 PAT。若靠"tag push 自动触发"会静默卡住。

### (d4) 官网：用同组织的 Proteus 框架构建（dogfooding）

**选型**：官网（`neo.proteus-vue.cn`）用 **Proteus**（`proteus-vue/proteus`）构建
—— 本组织自己写的 Vue 跨端框架。理由：① dogfooding，自己的框架先在自己的站点上跑
（Proteus 官方官网就是这么做的）；② 借它的「页面即文件 + `<route>` 元信息 + 路由表集中管理」
约定，后续加内容页不必重搭架子。
**没有**用普通 Vite 模板手搭 —— 那会丢掉这两个收益。

**可行性先证伪再投入**：真跑 `npm create @proteus-vue/proteus` 生成脚手架 →
`npm install`（79 包，无 404）→ `npm run build:web` 成功。所需包全部已在 npm 发布。
关键发现：**脚手架实际产物与官网文档/示例都不一致** —— 它用的是普通
`vite.config.ts` + `@vitejs/plugin-vue`，**不需要 `@proteus-vue/cli`**
（文档仍写"生成 32 个文件含 vite.config.ts"，实际 18 个）。故以**脚手架产物**为准，
不以文档为准。

**三个代价（如实记录，也写进了 website/README）**：
1. **全线 beta**（`0.1.0`～`0.3.0-beta.0`，多数包只有一个版本）→ **依赖钉精确版本**，不用 `^`。
2. **UI 要自己写**：官网那套语义组件（`@proteus-vue/components` 的 `p-*`）与
   `@proteus-vue/glass` **未发布 npm**，拿不到。框架给的是编译/路由/布局底座，不是组件库。
3. **无 SSG**：产物是纯 SPA，搜索引擎抓不到正文 → 用 `<noscript>` 兜底核心文案
   （**不能**把文案放进 `#app`，Vue 挂载会清空它）。

**踩到的坑（都是 vue-tsc 抓的，说明类型门禁值钱）**：
- `ProteusConfig` 的必填项是 **8 个**（含 `routesOutput` 与 `customRoute`），不是子代理
  从源码读出的 6 个。**判据：以 `node_modules` 里实际安装包的类型定义为准**，
  不以远端源码为准（版本不同）。`customRoute` 在 Web 端无意义但类型必填 → 给空 builders。
- 联合类型 `kind?: 'cmd'|...` 漏了 `'plain'`，vue-tsc 直接拦下。

**刻意不启用框架的 `audit` 规则**：其 `no-web-platform-api` 默认 error，会拦
`window`/`document` 裸调用，而官网必须做复制按钮这类 DOM 操作。那套规则面向
"一套源码编译到小程序"的跨端场景，**对纯 Web 站点不适用** —— 是规则不匹配目标，
不是绕过门禁（本站无 MP 目标）。理由写在 `website/proteus.config.ts` 注释里。

**Web-only 收敛**（相对脚手架）：删 `main.mp.ts`/`shims/mp.d.ts`/`scripts/gen-routes.ts`；
`platform:'web'`；`RouterView.vue` 换成精简版（脚手架那份 378 行是小程序多页转场，
含遮罩层与 `.page` 白底覆盖，单页站用会被样式绑架）。
**保留路由**（`router/` + `auto-routes.ts` 签入仓库）：贴合框架约定，且为 v2 内容页留门。

**验证方式**（无头浏览器不可用时）：构建产物 grep 关键文案 + **用 `@vue/server-renderer`
真渲染首页**，断言 h1/卡片数/架构层数/边界表条数/中文正确 —— 这比"产物里有字符串"强，
它证明组件**真能渲染**、无运行时错误。顺带确认 **SSR 可行**，故"SEO 无解"不准确，
更准确的说法是"SSG 未做，将来可升级"。

### (d5) 官网视觉升级：先能验收，再谈好看

需求是"升级到对标同行优秀的官网效果"。**第一件事不是写 CSS，而是解决"改完怎么知道变好了"**
—— 这台机器上没有无头浏览器，凭想象改视觉等于盲写。发现
`~/Library/Caches/ms-playwright` 里**已有 Chromium**（别的项目装的），
装上轻量 `playwright-core`（约 5 MB，不下载浏览器）即得到截图能力。**这是本轮的前提。**

**"专业观感"拆成了可执行的四条**（写在 `website/src/styles/global.css` 顶部）：
1. 层级来自「底色阶梯 + 辉光」而非堆边框（与 TUI 同一语言）；深色底上阴影不可见，要用同色系光晕
2. 类型尺度拉开（h1 与 h2 差 2 倍以上），否则整页没节奏
3. **每个区块的进入方式必须不同**（辉光 / 数据带 / 卡片网格 / 图解 / 表格 / 大命令）——
   通篇 `h2 + 网格` 就是一张长表格，这是"业余感"的主因
4. 动效克制 + `prefers-reduced-motion`

**结构改动**：单文件 487 行 → 拆成 13 个区块组件（`components/Section*.vue`）；
新增数据带、对比表、FAQ、视觉分层图（原先是张平表）；`FeatureIcon` 内联 SVG 自绘
（不引图标库 —— 依赖全钉 beta，少一个依赖少一处漂移）。

**两轮视觉验收各抓到真问题**（都靠截图，不靠自信）：

第一轮：
- **三级文字对比度 3.7:1，低于 WCAG AA**。深色站在这一档最容易偷懒
  （"反正只是辅助文字"），但 12–13px 上是真读不清 → `--neo-fg-faint` 提到 5.2:1（实测值）。
- **移动端窄屏静默裁切**：终端输出与安装命令被容器边缘切断（`curl … | sh` 只剩半截）。
  命令是给人复制执行的，截断会让人以为内容本来就长这样 → 一律改为换行。
- **移动端对比表第三列（neo 列）落在视口外** —— 那一节的核心恰恰是第三列，
  等于白写 → 窄屏改为堆叠卡片 + `data-label` 标列名。
- FAQ 标题被降了一级字号（同屏对比很显眼）→ 统一。

第二轮：**移动端架构图只剩层名，职责与 crate 名被我 `display:none` 掉了**，
卡片右半空白。这是"为排版删信息"的典型错误 —— **信息只能重排，不能删除** → 改为两行堆叠。

**验收方法本身也踩了坑**：整页截图前**必须先滚动一遍**。滚动揭示靠
`IntersectionObserver`，不滚动则首屏之外全是 `opacity:0`，截出来一片空白
—— 第一次就误判成了 bug（用 `reveal-check.mjs` 数 `.reveal.is-in` 才证伪：
滚动后 28/28 全揭示）。已给 `@media print` 加兜底，并把"先滚动再截"写进
`website/README.md` 的验收步骤。

另：**横向溢出**在每个宽度都实测（`scrollWidth > clientWidth` 在 390/768/1440
都不成立），这比肉眼看可靠。

**未做（诚实）**：只有单页；无设计稿（配色与结构是自拟的，但取色严格来自 TUI 主题）；
无真人视觉验收（只有无头浏览器截图 + 视觉评审，未在真实浏览器里长期使用）。

### (d6) 官网改造：让站点本身就是一屏 TUI

用户反馈"太普通了，大厂他们的都有 TUI 宿主的风格在"。**关键判断：这句话要的不是
"更花哨的落地页"，而是"站点应该是产品本身"** —— 于是从"深色落地页里嵌一个终端截图"
改成"整站按 TUI 的界面语言重做"。

**先把 TUI 的视觉语言完整提取出来**（不靠印象）：读 `crates/neo-host-tui/`
（17198 行），并用 `script` 伪造 tty **真抓了两帧**（信任门 / 主界面）——
这一步很值：图形语言与源码注释里的描述有出入（`┃` 面板条、`·` 星场、`●`/`○` 单选、
底部键位行的实际排布，看图才准）。字形词汇统计（`─│┃█▀▌░●○◆❯▸→↓◂`）也是数出来的。

**移植清单**（每一项都对应 TUI 的一个真实实现，不是"终端风格的装饰"）：

| 站点 | TUI 来源 |
|---|---|
| 星场/点阵/斜纹背景 | `/background`（`appearance.rs` 的确定性 `cell(row,col)`） |
| `███╗ ██╗` 六行块字 | `lib.rs` 的 `LOGO_LARGE`（逐行渐变） |
| `▌` 输入框 + `模式 ⏵ 模型` | 首屏输入框 |
| 键位行 `tab 补全 ctrl+/ …` | 底部键位栏 |
| 底部常驻状态栏 | TUI 最底行（左路径 / 右状态） |
| `┃` 单边条面板 | TUI 分组惯例（不画四角框） |
| **9 套主题实时切换** | `ctrl+t` |
| 外观面板（主题 + 背景） | `ctrl+t` / `/background` |

主题取值**逐色抄自 `theme.rs`**（9 套全抄，含 `Light` 与 `Terminal`），
所以站上切到的是**产品里那套真实主题**。`square` / `light` 两个标志也照搬机制：
Terminal 的 `square=true` 会把全部圆角一起归零 —— 这正是「风格包 vs 换色」的区别，
在站点上点两下就能体会到，比文案有说服力。

**两个必须靠工具才能抓到的 bug**（都是本轮实际踩的）：

1. **写死颜色的变量被删导致主按钮隐形**。重写 `:root` 时删掉了自定义的
   `--neo-primary-strong`，而 `.btn-primary` 的渐变还在引用它 → 渐变失效，
   按钮几乎看不见（截图才发现）。**修法不是补变量，而是改用主题已有的
   `primary → accent`** —— 这两个在 9 套主题里必然存在。
   顺带写了个脚本扫"被引用但未定义的 CSS 变量"，这类问题以后能立刻抓出来。

2. **亮色主题下终端块变成"黑砖"**。终端演示块原先写死深色底
   （`linear-gradient(180deg, var(--neo-panel), #100f16)`），切到 `Light`
   主题时它仍是一块黑 —— **这正是"只测默认主题"会漏掉的问题**。
   修法是整块改走变量 + 用 `--neo-glow-strength` 让亮色主题下把辉光压到近零
   （深色底上的白光是"辉光"，亮色底上会变成"脏印"）。

**验收方式**：9 套主题逐个注入 `localStorage` 后**程序化断言**（变量是否解析、
CTA 渐变是否成立、square/light 标志、横向溢出），再对 Neo / Light / Terminal
三套做视觉验收（含最易出问题的亮色与方角）。三套全 pass，6 组不同宽度/主题的
横向溢出实测均为 0。

### (d7) 背景纹理"完全没效果"：一个遮罩层把画好的东西全擦了

用户反馈"测试背景星场这些完全没效果"。**根因不是算法，是一层遮罩**：
`TuiBackdrop.vue` 里有个 `.veil`（`opacity: 0.92` 的 radial-gradient），渐变末端是
与页面同色的 `rgb(10,10,12)` **不透明** —— canvas 上画好的星点被整片擦掉。
实测证据：canvas 上 **588 个像素有颜色**（确实在画），但屏幕上一点看不见。
**"在画"与"看得见"是两件事** —— 这正是只看代码会漏、必须量像素的地方。

顺带修掉三个同族问题：

1. **密度是算出来的，不是拍出来的**。读了 `appearance.rs` 的 `Background::cell()`
   才发现：Stars 是 `v = h % 1000`（v≤1 亮 / v≤9 暗 → **1%**），Diagonal 是
   相位 1/17 且每 2 行，作者还特意注明 **1/7（14%）会盖过正文**，目标是 2%~5%。
   我最初既没读这些数、也没算覆盖率，写出的点场在网页上等于不可见。
2. **规律图案不该用 canvas**。dots / diagonal 是规律纹理，用 canvas 画既模糊、
   覆盖率又难估（实测 dots 只有 0.9%）。改为 **CSS 渐变**：覆盖率由
   `background-size` / 线宽周期精确决定，且不受 devicePixelRatio 影响、零 JS。
   分工定死：**stars 是确定性伪随机 → canvas；规律纹理 → CSS。**
3. **"改完怎么知道变好了"要能量化**。写了个像素差脚本：分别截取同一块
   只含背景的区域，与 `bg=none` 做逐像素比对，输出"明显不同像素占比"。
   这比肉眼看可靠，也让"调密度"有反馈闭环。最终三种纹理都落在 3%~7%：
   ```
   stars     3.42%   ✅
   dots      3.37%   ✅
   diagonal  6.58%   ✅（此前 1px/6px 时达 15%，超了 TUI 标定的上限）
   ```

**教训**：宣称"对齐某个实现"时，**要去读它的参数，而不是复刻它的结构**。
我第一版写了"对齐 TUI 的确定性算法"，算法结构确实像，但密度/尺寸全是自己估的 ——
结果是"结构对了、效果没有"。

### (d9) 桌面版对标：先弄清对标目标自己是什么技术

用户要求"桌面实现对标 Codex 桌面版和 ZCode 桌面版"。**第一件事不是抄界面，是查清它们是什么**——
结果查出一个会误导整个选型的事实：

| 目标 | GUI 技术 | 证据 |
|---|---|---|
| **ZCode 桌面** | **Electron + React 19 + Radix UI + node-pty** | 本机装有 `dev.zcode.app` v3.11.2；asar 里 `NSPrincipalClass = AtomApplication` |
| **Codex 桌面** | 闭源，**不在 `openai/codex` 仓库内** | `codex-rs` 的 `Cargo.lock` **全文无任何 GUI 依赖**；仓库只有 TUI(`ratatui`) + 安装器/`codex://` 深链 + `app-server` RPC |
| **Zed** | **GPUI（纯 Rust GPU 框架）** | 本机装有 `dev.zed.Zed` |

**两个对标目标都不是 Rust 原生 GUI。** 所以：
- "对标 Codex 桌面版"**只能对标 UX 与功能**，代码层面无从对标（GUI 闭源、仓库里没有）。
- 两者架构都是「GUI 消费一个协议」——而**本项目已是同构形态**（内核 + `Op`/`EventMsg` + `neo-host-web`），
  差的只是"谁来渲染"。
- 于是有个必须讲明的错位：**"用 Rust 原生 GUI 对标两个 Web 技术的 GUI"，其可行性证据是
  Zed/GPUI，不是 Codex/ZCode**。把两件事混在一起会得出错误的可行性结论。

**本机两个应用都在跑，但我看不到界面**：ZCode 的辅助功能与屏幕录制权限未授予
（授权对象是 `~/.zcode/computer-use/ZCode Computer Use.app`），Codex 桌面文档 403。
故**结构层能对标**（ZCode 官方文档写得很细 + Codex 的 TUI 源码可读），**观感层需要真人补图**。
这条如实写进了 `docs/desktop-parity.md` 末尾，不装作已对齐观感。

**对标档案**：新增 `docs/desktop-parity.md`（照 `opencode-parity.md` 的路子——TUI 侧正是靠
一份写下来的范式止住了"打地鼠"）。核心产出是**差距清单 D1–D12**，其中 **D2–D7 全是纯前端工作**：
Markdown 正文 / 思考轨迹 / 工具卡片 / diff 渲染 / 轮摘要 / Goal 面板 —— 对应事件
`AgentMessageDelta` / `ReasoningDelta` / `ToolCallBegin{arguments}` / `PatchProposed{path,diff}` /
`TurnComplete` / `GoalUpdated`，**数据早就流在事件流里，只是那份 184 行页面全扔了**。

最该照抄的一段是 **ZCode 的审批语义**（官方文档明确）：权限门触发 → **当前任务暂停 +
composer 被阻塞**（防止误把下一步排进队列）→ 显示将执行的确切内容 → 三档
Allow / Always Allow / Reject → **高风险或全自动模式下工具栏持续显示风险状态** →
审批按任务作用域绑定。

### (d8) 星场重做：从"小方块点阵"到真正的星芒 + 流星

用户反馈"现在看着完全不像星星矩阵，优化成视觉观感真正的星芒那种效果，还有流星滑落那种"。
原实现是在 16px 网格上 `fillRect` 画小方块 —— 那是"点阵"，不是"星空"。

**星芒做法**（关键在预渲染）：把一颗星画成三层叠加 —— 亮核 → 柔和光晕 →
**十字光芒**。十字用"把径向渐变压成极扁椭圆"实现（中心粗两端细，正是星芒形状），
再 `globalCompositeOperation = 'lighter'` 让交叠处更亮。
星**按主色预渲染成离屏精灵**，每帧只 `drawImage` —— 否则每帧建几十个渐变会明显掉帧。
大小分两档：约 14% 是带星芒的亮星（9~18px），其余是 1~3px 的小光点 ——
**层次感来自这个比例**，全画成星芒反而糊。

**流星**：独立的短生命周期粒子，固定约 28° 斜向右下，间隔 2.6~7s 随机。
氛围感靠"偶尔"，所以刻意做了最小间隔。

**★ 一个靠肉眼会漏掉的 bug**：流星的拖尾长度我写成了
`m.x - m.vx * (m.len / 55)` —— `len` 本意是**像素长度**，却被当成速度比例除了，
结果尾巴只有 1~4 像素，**等于只是一个点，屏幕上完全看不到流星**（截图连拍 90 帧
也未必抓到）。改成按方向单位向量 × 像素长度后正常。
**发现方式**：不是看图，而是写脚本按"很亮像素数的峰值/均值"做尖峰检测
（正常 1.0，有流星 1.63）—— 随机 + 短暂的事件，靠抽样截图碰运气是不可靠的。

**与 TUI 的取舍（要如实说）**：TUI 的星场是**静止**的 —— `cell()` 是确定性哈希，
因为终端每帧全量重画，随机纹理会**闪成噪点**。官网要有星芒与流星就必须动，
所以折中：**位置/大小用固定种子**（每次加载星图一致、不噪），
**只有亮度随时间呼吸**（闪烁，相位各异以免整屏同步明灭）。
这条差异是需求驱动的，不是"忘了 TUI 的教训"。

**性能与降级**：粒子数按面积算并设上限（≤240）；DPR 封顶 2；标签页不可见时
暂停 rAF；`prefers-reduced-motion` 下只渲染一帧静态星场（不闪、不出流星）。

### (e) 诚实边界

- **macOS 桌面有 `.app` 打包脚本**：`scripts/make-app.sh`（最小 Info.plist +
  `CFBundleIdentifier` + ad-hoc 签名）。它不只是打包工具 —— 还是 **GUI 键盘
  自动化验证的前提**：直接跑裸二进制时进程 `bundle_id` 为 null，WindowServer
  投不进合成键盘事件，于是"打字/回车"这类路径**无法自动化验证**。
  **仍未做**：图标、Apple 签名与公证。
- **Linux 预编译产物是精简版**（不含桌面），因为动态链接 webkit 会让
  纯 TUI 用户被迫装它。要 Linux 桌面窗口得 `cargo install` 带默认 feature，
  **并且先装两组系统库**：webview 需要 webkit2gtk（构建期 pkg-config），
  gpui 需要 libxkbcommon/libwayland/libX11（**运行期 dlopen** —— 编译不需要
  它们，但运行时缺了直接 panic）。见 §8"Linux 桌面宿主的运行时库"。
- 预编译 / npm 产物只覆盖 macOS（双架构）+ Linux x86_64。其它平台走 `cargo install`。
- **npm 包尚未真正发布**（首次发布仍需一个 granular token 做 bootstrap，
  见 d2）：验证止于 `scripts/publish-npm.sh --dry-run` + **真实
  `npm install -g` 端到端跑通**（wrapper 输出 `neo 0.1.0`）。
- **crates.io 未发布**（决定见 a2）：元数据齐备，随时可发。

---

## 0.1 收尾快照(2026-09-13)
一天弧线:从"流式思考不实时"的用户反馈出发,打通**真流式全链路**(provider SSE → 内核分帧 → 驱动线程),再把 TUI 交互细节整体对齐 opencode/mimo code(18 个提交,617 测试,门禁全绿,真机智谱 glm-4.6 全程回归)。

- **流式主战役**:provider 真 SSE(智谱/DeepSeek/任意 OpenAI 兼容网关共用一条实现)、内核 `Op::Pump` 按时间片分帧(80ms/256 条上限)、内核挪到驱动线程(工具执行/首字延迟期间界面零冻结)。真机验证签名:会话日志从"1 条大增量 + 1 次 pump"变为"52 条小增量 + 几十次 pump"。
- **TUI 交互对齐**:思考块逐块折叠展开(鼠标点头部,下标与显隐无关)、动画挂在块头部(非右下角)、轮耗时与状态栏已用时、工具行命令摘要、侧栏(Session 区/占用条/会话累计)、输入块现代风格(左强调条+面板底,去旧线框)、主题实时预览 + 两个新风格主题(Light 亮色 / Terminal CRT 方角)——主题从"换色"升级为"换风格包"。
- **抓到的深坑**(详见 4.58/4.59):多步轮次早期思考被误判为"进行中";审批应答后 driving 标志未恢复导致轮次卡死;主渲染帧用默认 ThoughtView 使点击失效;行内宽字符+满宽侧栏导致整行折行错位。
- **工程**:测试 key 在 `~/.neo/provider_keys.json`(0600);`cargo install --path` 会绕过 Cargo.lock 重解析(edition2024 冲突),发布用 `cargo build --release` + 手动替换(先 `rm` 再 `cp`,否则 macOS 签名 SIGKILL)。
- **下一步**(按价值):工具耗时进事件 vs 确定性铁律的架构决策;思考块键盘操作(需转录光标概念);审批卡片 diff 视图复用全屏 diff。

---

---

## 1. 项目是什么，为什么改了方向

**NEO**：用 **Rust** 重写内核的编程 Agent，四宿主（TUI / Desktop / Web / Exec）共享同一内核。

### 方向变更记录（重要）

本项目**最初**是「proteus-code」：一个 TypeScript 写的 **Electron 桌面应用**，
以 **DSH（DeepSeek Harness）插件**的形式接入（不 fork DSH，走官方扩展点）。
那版**可运行**（129 测试、真机验证过液态玻璃界面、Proteus CLI 工具、Codex 式策略引擎）。

**后来改为**：用 Rust 重写内核，按 Proteus 方法论（统一语义收敛 + SPI-First），
**摒弃 Electron 这类过重的壳**。

因此：**旧实现整体在 `legacy/`**（不在主线），作为设计参考与经验来源。
旧版的项目记忆在 `legacy/PROJECT_MEMORY-proteus-code.md` —— **重写时值得先读**，
它记录了哪些问题会随 Rust 重写消失、哪些不会（见本文第 6 节摘要）。

---

## 1.5 crate 命名：全部 `neo-`，以及两处**必须**保留 `dsh`

31 个 crate 一律 `neo-`（`neo-protocol` / `neo-core` / `neo-host-web` …），
不再用 `dsh-` 前缀 —— 内核对 DSH 的借鉴是**思想**，不是包名延续。

两处**刻意不动**（动了反而是错的）：

| 位置 | 为什么保留 `dsh` |
|---|---|
| `legacy/` | 那是真的 DSH 插件代码：它 `import '@deepseek-ai/dsh-*'`、写的是 `dsh-host.d.ts`。这里的 `dsh` 指向上游真实包名，改成 `neo` 会指向不存在的包 |
| `docs/neo-plan/REVIEW-proteus-code.md` | 评审稿的论据是「DSH 现状已有 X」，引的是 DSH 自己的包（`dsh-goal` / `dsh-user-approval` / `dsh-sandbox-policy`…）。改名会篡改证据 |

**判据**：`dsh-X` 出现在「我们自己的代码/规格/门禁」里 = 指我们的 crate，必须叫 `neo-X`；
出现在「引用上游 DSH 的证据」里 = 指真实上游包，必须留 `dsh-X`。
这个区分已由 `scripts/verify.sh` 兜住 —— 架构守卫按 crate 名匹配，改名后仍全绿。

---

## 2. 方法论：为什么是「5 个有名 SPI」，不是「一切皆插件」也不是「写死内核」

**DSH 的病不是插件太多，而是约 90 个 seam 没有名字、没有门禁。**

| | DSH | NEO |
|---|---|---|
| seam 数量 | ~90（无名） | **5（有名）** |
| 有契约吗 | 有（Cordis service） | 有（Rust trait） |
| 有 ≥2 后端吗 | 部分，未强制 | **强制**，CI 校验 |
| 依赖可静态分析吗 | **否**（运行时服务查找） | **是**（crate 依赖图 + 守卫） |

把 90 个无名 seam 收敛成 5 个有名 seam，**同时得到可替换性与可定位性** ——
这正是 DSH 缺失的那一半。**写死内核反而把可替换性一起丢了**，那是另一种极端。

**五个 SPI**：`ModelProvider` / `SandboxBackend` / `SessionPersistence` / `HostBackend` / `Tool`。
每个都必须有：契约 + ≥2 真实后端（含 Mock）+ conformance 契约测试。
由 `check_spi_conformance.py` 强制（它区分 landed 与 planned，不谎报通过）。

**耦合审计的意外发现**：旧实现对 DSH 的耦合只有 **8 个文件**，因为
`ctx.tools` / `ctx.shell` / `ctx.llm` / `ctx.commands` / `ctx.skills`
**都是 DSH 自己定义的 SPI** —— 我们是通过接口接入的。

---

## 3. 为什么内核必须是 Rust（两个目标，非语言偏好）

### 目标一：高性能
- 主循环零拷贝（`ModelRequest` 借用 slice + `mem::take`，消除每步 O(N²) 深拷贝）
- 系统提示词与工具 schema 构造时缓存
- 无 GC 停顿（延迟敏感的流式输出路径）
- **实测**：计数分配器测同一步在历史 20 条 vs 400 条下的额外分配差 < 16 KB

### 目标二：内存安全
- **23 crate 生产代码零 `unsafe`**（`cargo check` 全绿；`neo-core/tests/memory.rs`
  的计数分配器是唯一例外，测试基建所必需）
- `Send`/`Sync` 由编译器强制 → 类型层面无数据竞争
- 无悬垂/双重释放（所有权 + 生命周期）

### 但 Rust **不保证内存有界** —— 这是最容易被跳过的一点

Rust 消除 UB，但**一条 `yes` 或 `find /` 能把进程撑爆**，完全安全的 Rust 照样 OOM。
所以有界性是**内核义务**，必须自己实现并测试：

| 措施 | 位置 |
|---|---|
| 单次工具输出上限（256 KB，内核级，工具不可放宽） | `ToolCtx::exec` |
| 截断不切开 UTF-8 码点（直接 `&s[..n]` 会 panic） | `truncate_utf8` |
| 沙箱层已声明的 `truncated` 必须上抛 | `ToolCtx::exec` |
| 上下文消息上限（4096）：超限**报错要求压缩** | `Kernel::check_context_budget` |
| 步数预算 / 单步调用上限 | `drive_steps` |

**上下文超限为什么报错而不是丢最老消息**：静默丢弃会让模型视角与会话日志不一致，
破坏「模型可见即已落盘」的铁律 —— 那比报错危险得多。

---

## 4. 内核实现要点（踩过的坑与设计决定）

### 4.1 契约缺陷：`ModelProvider::complete() -> String` 无法表达工具调用

**原设计**（继承自计划原型）是 `fn complete(&self, prompt: &str) -> String`。
按 SPI-First 这是 **AP-05**（接口表达不了所需行为 = 假 SPI）：
工具调用是 agent 的本质，一个只返回字符串的接口根本描述不了它。

**改为** `fn stream(&self, &ModelRequest) -> ModelStream`（流式增量）。
同时把 `ModelRequest.messages` 从 `Vec` 改为 **借用 slice**（见性能一节）。

### 4.2 沙箱必须是结构保证，不是各工具的自觉

**原设计**的 `Tool::execute(&self, args) -> ToolOutput` 让工具自己去执行进程 ——
安全边界就成了"每个工具作者都要记得"的约定。

**改为** `execute(&self, args, ctx: &ToolCtx)`，`ctx.exec()` 是**唯一**执行入口且已绑定沙箱。
工具**在类型层面就没有绕开沙箱的入口**。

### 4.3 闸门按工具名字符串判断是脆弱的

**原设计**用 `if call == "bash"` 判类别。危害很具体：**新增工具就漏判、改名就失效**。

**改为** `Tool::call_kind(args)` 由工具自己声明语义类别。
`bash` 按命令内容分类，且**写标志优先于只读首词**
（`echo hi > f` 首词是 `echo` 但它在写），识别不出时**保守判 Write**
—— 因为把写误判为读会绕过审批，代价不对称。

### 4.4 审批挂起必须保存「整批调用 + 位置」

只存单个待审批调用的话，模型会看到"半个步骤"、工具结果顺序也会错乱。
所以 `PendingApproval { calls: Vec<ToolInvocation>, index }` 保存整批与位置，
批准后从 `index + 1` 继续执行同一步剩余调用。

### 4.5 同名 trait 会误导（已消除）

`neo-sandbox`（L1）与 `neo-core`（L2）原本都定义了 `SandboxBackend`，
但职责不同：前者「把命令包裹成受限形式」，后者「在某档语义下能否执行」。
合并会迫使 L1 依赖 L2 的语义类型，破坏依赖方向。**前者改名 `CommandWrapper`。**

### 4.6 确定性是回放的前提

审批 id 由 `(turn, step, index)` **派生**，不用随机/时钟。
测试断言：同一 Op 序列两次运行得到**完全相同**的事件序列。
系统提示词字节稳定（工具用 `BTreeMap` 有序，不含时间/随机）。

---

## 4.7 真机测试抓到的三个 bug（都是"只在 mock 里对"的典型）

沙箱是真机验证的（真的调 `sandbox-exec` 起真进程），一次就抓出三个只有真跑才暴露的 bug：

| # | Bug | 根因 | 修法 |
|---|---|---|---|
| 1 | 6 个测试跑了 **240 秒**才失败 | **管道死锁**：先 `try_wait` 等进程退出、再读管道。管道缓冲区（macOS 约 64 KB）写满后子进程**阻塞在写**、永不退出 | 必须**并发读取** stdout/stderr（两个读线程）。这是 OS 语义的必需品，不是"加深调试链" |
| 2 | `workspace-write` 能用写工作区外的文件 | Profile 里放开了整个 `/tmp`，于是"工作区外但在 /tmp"也被允许 | **不放开 /tmp**（与 Codex 立场一致），只放开显式声明的可写根 |
| 3 | 工作区内的写也被拒 | macOS 上 `/tmp` 是指向 `/private/tmp` 的符号链接，而 Seatbelt 的 `subpath` 匹配**真实路径** | 注入前 `canonicalize()` |

另外把输出上限从"内核事后截断"改为 **SPI 契约的一部分**（`execute(…, limit_bytes)`）：
实现必须**边读边限**。事后截断意味着内存早已被吃掉，再截断毫无意义。
且达到上限后**立即返回**（不继续 drain）——否则 `yes` 这类永不结束的输出源
会让读线程永不返回。

**教训**：安全边界与资源边界都必须**真机验证**。一个只在 mock 里"生效"的沙箱
等于没有沙箱；一个只在 mock 里"有界"的输出路径等于没有上限。

---

## 4.8 TUI 宿主：真终端才暴露的两个问题

TUI 用 pty 驱动验证（**轮询就绪信号**再送按键，不用固定 sleep —— 否则按键会在
切原始模式前被行规程吃掉，测试反而挂到超时）。

| # | 问题 | 根因 | 修法 |
|---|---|---|---|
| 1 | 在 pty 里跑仍报"标准输入不是终端" | **Rust 的 `Command::output()` 会关闭子进程 stdin**，`stty` 因此认为 fd 0 不是终端而失败；而父进程的 `is_terminal()` 明明是 true | 所有 `stty` 调用必须 `stdin(Stdio::inherit())` |
| 2 | 错误信息误导 | 原实现把"stty 调用失败"和"输入不是终端"混成同一句 `None` | `RawMode::enter()` 返回**具体原因**（三种失败补救方式不同） |

**教训**：错误信息必须区分原因。把两种不同的失败合并成一个 `None`，
会让排查方向完全错（我一开始真去怀疑 pty 测试方式，而不是自己的代码）。

另外 TUI 的 `interactive_prompt: true` 能力声明原本是**假的** ——
它能显示"需要审批"却不会问用户，内核会一直挂起。补齐交互后能力声明才成立：
同一任务、同一档位，应答 `y` 则文件写入、应答 `n` 则不写（真终端验证）。

---

## 4.9 Web 宿主（第三个真实宿主）：四处容易做错的取舍

### (a) 内核不能交给 HTTP 线程 —— 它不是 `Sync`
最省事的写法是 `Arc<Mutex<Kernel>>`，但"持锁跑完一轮模型调用"会把所有请求串行化，
那不是并发，是排队。正确做法是**内核独占一个工作线程**，HTTP 只发 `Op`、收事件：
```
浏览器 ──POST /api/turn──▶ HTTP 线程 ──mpsc(Op)──▶ 内核线程
浏览器 ◀──SSE /api/events── HTTP 线程 ◀─broadcast(Event)─┘
```
这样内核保持单线程语义（无需 `Sync`、无锁竞争），多个浏览器标签又能同时订阅。

### (b) 事件流不重放 —— 先订阅再提交是硬顺序
`Broadcast` 是**有界**的（队列 256），慢订阅者会被丢弃而不是无限缓冲。
副作用是：**提交任务前必须先建立 SSE 连接**，否则这一轮的事件发给了零个订阅者，
永远看不到。首次冒烟测试就是这样——`/api/events` 只收到 `subscribed` 一行，
因为 curl 建连晚于提交。这不是 bug，是有界性的必然结果，但必须写进用法。

### (c) serde 默认外部标签，前端取值很别扭
`EventMsg` 序列化是 `{"agent_message_delta":{"delta":"x"}}`，前端得写 `m[k].delta`。
宿主加了一层 `wire_event()` 归一成 `{"kind":"agent_message_delta","delta":"x"}`。
这属于**渲染适配**（宿主职责），事实语义仍由协议层 `facts_of` 定义 —— 不能因为
要迎合前端就把事实抽取也搬进宿主，那会破坏 T6 的可比性。

### (d) 引用解析曾有两个副本，已收敛到 L0
`neo-host-web` 和 `neo-host-desktop` 各写了一份 `parse_refs`，且返回类型不同
（一个 `(char, String)`、一个 `ContextRef`）。这是典型的**语义漂移**：
同一个 `@` 在两个宿主里语义可能分叉。引用符号是界面约定，但"这是个文件引用"
是协议语义，因此收敛到 `neo-protocol::parse_refs`，宿主只 `pub use`。
判据很简单：**同一件事在两个宿主里各写一遍 = 它站错了层。**

---

## 4.10 TUI 的两个「用户第一次用就会撞上」的问题

### (a) `neo` 不在 PATH —— 用法文本与交付形态不一致
文档写 `neo tui`，但 `cargo build` 只产出 `target/debug/neo`，那个目录不在 `PATH` 上。
用户直接敲 `neo` 得到 `command not found`。修法是明确的：`cargo install --path
crates/neo-cli --locked` 装到 `~/.cargo/bin`。这只是"没说清"，不是代码错。

### (b) 裸选项被当成子命令 —— 真 bug
用法里写 `neo [选项]`，但 `main` 只匹配 `args.first()` 的字符串：
`neo --provider mock` 落到 `Some(other) => 未知子命令：--provider`，而 `neo`（无参）
却正常进 TUI。也就是**有无参数是两条不同路径、结论相反**。
修法：首参以 `-` 开头时等价于 `neo tui`，并在报错信息里点明
"若想启动 TUI，请用 neo tui <选项> 或直接 neo <选项>"。

### 教训
TUI 只能在**真 pty** 里验证 —— 管道里 `stty` 拿不到终端，本就不是终端。
用 `pty.fork()` 驱动、按输出子串轮询（不固定 sleep），可验证
「启动 → 提交 → 审批 y/n → 落盘 → 完成」全链。已在两种入口（`neo tui` 与裸选项）各跑一遍。

---

## 4.11 TUI 欢迎首屏：一个"自动滚到底"埋下的裁切陷阱

渲染逻辑是"只显示末尾 N 行"（自动滚到底）——对话增长时这是对的。
但**空对话时它会把首屏从顶部裁掉**：用户看到的是"少了 logo 的半截欢迎页"，
而不是一条错误信息。这个 bug 不会报错、不会 panic，只会静默地难看。

修法不是加 `sleep` 或特殊分支，而是**让首屏自己保证放得下**：
按「奢 → 简」四档试排（词标→单行、副标题→无、留白→紧凑），
选第一个 `len() <= 可视行数` 的档位。测试直接断言大小两种终端下都能渲染。

两条副产物：
- 词标（ANSI Shadow）六行**必须等宽** —— 手改最容易某行差一个字符，终端里立刻看出来。
  已加测试断言各行字符数一致。
- 定长文案（副标题、快捷键）也要按宽度截断，否则窄终端里提示语会超宽换行、打乱布局。
  而键值行必须**先截断再着色** —— 着色后含 ANSI，再按宽度截会切进转义序列。

顺带一处验证方法教训：用 pty 抓 TUI 输出时，若**逐块** `decode('utf-8','replace')`，
多字节字符可能被块边界切开，显示成 `U+FFFD`，看起来像渲染 bug。
正确做法是累积字节、最后一次性解码。我差点据此去"修"一个不存在的乱码。

---

## 4.12 TUI 视觉对标 opencode（以及为此补的一条协议事实）

**对标来源是本地克隆的源码，不是博客文章。** `git clone` opencode（SSH，HTTPS 在本环境被封）
后直接读 `packages/tui/src/`：默认暗色主题在 `theme/assets/opencode.json`，
版式在 `routes/session/index.tsx`（消息块）与 `component/prompt/index.tsx`（输入框）。要点：

| 观察自源码 | NEO 的落地 |
|---|---|
| 调色板是暖主色 `#fab283` + 冷强调 `#9d7cd8`，不是 16 色 ANSI | 引入 `Color`/`Pal`/`ColorMode`：truecolor(38;2) → 256 色(38;5) → 16 色 → 无色，按 `COLORTERM`/`TERM` 探测 |
| prompt 只有**左边框** `┃`，不是四边框 | 输入行 `┃ ` + 正文；用户消息也用竖条（`accent` 色）作"我的话"的归属 |
| 助手正文**不加框**，纯文本 | 助手正文顶格直排，不套盒子（套框会让长回答看起来像引用块） |
| 工具调用是树状：`↳ tool`，完成 `✓` / 失败 `✗` | `✓`/`✗` + 名字 + 压暗的 `exit N`；成功/失败行宽一致，不抖动 |
| footer 左右分栏：左目录、右状态（LSP/MCP/权限） | 状态栏左侧提示、右侧 `模型 · 档位`；窄终端右侧自动让位（不折行） |
| 权限提示用 `△` | 审批用 `△`，竖条转警告色 |
| `/details`、`/thinking` 等靠 dialog | 未做（无 dialog 组件，诚实缺口） |

**为对标补的一条协议事实**：opencode 的转录里能看到"用户说了什么"，而我们之前只有
助手与工具。这不是缺个界面元素，而是**缺一条协议事件** —— 用户自己的话本就是转录里
必须可见的事实。于是在 L0 加 `EventMsg::UserSubmitted` 与 `Fact::UserSaid`，由内核统一回显。
注意是**内核发、不是各宿主自己记**：宿主各记会分叉（T6 就散了）。这条同时提升了
exec/Web 的输出（三宿主都显示用户消息），并更新了协议 golden 与 Python 参照实现。

**两个容易踩的实现坑**：
1. `NO_COLOR` 必须是"零转义"，不能只把颜色置空 —— 否则输出里仍残留 `ESC[0m`，
   重定向到文件时污染内容。所以 `Pal::reset` 在无色模式下也是空串。
2. 键值行要**先按显示宽度截断、后着色**；反过来（先着色再截）会按字节切进转义序列，
   终端里出现半个 SGR 导致后续文字全部变色。

---

## 4.13 TUI 对标 MiMo Code：网格模型与"信任"这道门

### (a) 为什么改成"先铺格子再输出"
上一版是"拼字符串"，做不了星场 —— 星场要出现在**居中内容的左右两侧**，
而"内容 + 补星星"的顺序下，左侧空白已算进内容宽度，星星永远进不去左边。
改成网格（`Grid`）后，"内容覆盖"与"背景填充"分成两步：
先 `put` 内容，再 `fill_stars` 只给 `Tone::None`（从未写入）的格子铺星。
输入框内部要显式 `blank`，否则星会渗进框里，看起来像框"漏了"。

### (b) 星场必须**确定性**
TUI 每帧重画整屏。若用随机数，每帧星位都变，用户看到的是满屏噪点在闪 ——
比没有背景还糟。改成**位置哈希**（`(row,col,cols)` → splitmix64）后，
同一格永远得到同一结果，多帧完全静止。星场只用**半角**字符：
宽字符会让整行右移一列，一列错位整屏就歪了。

### (c) 信任：沙箱之外的另一半
沙箱回答"最多能做什么"（硬边界，OS 级）；信任回答"这个目录是不是你让我动的"
（知情同意）。二者互补：沙箱挡不住"我在错误目录里合法地改文件"，
信任挡不住"恶意代码越权"。

信任库落在 `$NEO_HOME/trusted.json`（默认 `~/.neo/trusted.json`），**不落项目里**：
   1. 落项目要进 .gitignore，且每个新克隆都得重问；
   2. 更要命的是**可被仓库自己伪造** —— 恶意仓库带一个"已信任"文件就绕过整道门。
解析失败一律当"未信任"（fail-closed）：宁可多问一次，也不默认放行。
`--mode full` 跳过此门 —— 那本身就是"我承担风险"的显式选择，再问没有增量信息。

### (d) 一个被测试抓住的**假失败**（值得记住）
`rendering_is_deterministic_across_frames` 间歇性失败（20 次里挂 2 次）。
第一反应是"星场在闪"，但星位是位置哈希，**结构上不可能闪**。
真正原因是颜色档位从进程级 env 读取，并行的另一个用例在两次渲染之间
改掉了 `COLORTERM`，于是两次的颜色转义不同 —— 是测试自己制造的抖动。
修法：该用例显式锁定环境（`with_env`，持锁），并额外断言"剥掉颜色后星位一致"。
教训：**先怀疑测量工具，再怀疑被测对象**；确定性的东西"偶尔失败"，
多半是环境串了，不是逻辑错了。

---

## 4.14 退出后欢迎页还在：不是"没清屏"，是**画错了地方**

现象：退出 TUI 后，欢迎页留在终端里，shell 提示符上方还挂着我们的界面。

直觉上会去加"退出前清屏"（`ESC[2J`）。但那是治标 —— 清屏只是把**当前屏幕**擦掉，
下次启动、滚动回看、或任何重绘都可能再露出来。真正的问题是：
**我们一直在用户的正常屏幕上画**。

正确做法是内核的**备用屏幕缓冲**（alternate screen）：
- `ESC[?1049h` 进入：终端保存当前屏幕，切到第二块屏；
- `ESC[?1049l` 退出：整块还原到启动前的样子。

于是我们的界面从不进入正常屏幕的 scrollback，退出后用户看到的就是启动前的内容。
vim / less / htop 以及 opencode 都是这个机制（opencode 走 `@opentui/core` 的
`renderer.destroy()`，底层同样切回备用屏）。

实现要点（三条都踩过）：
1. **幂等**：`leave()` 会被 Drop、显式退出、panic 钩子多处调用；
   重复写 `?1049l` 会让部分终端闪一下。
2. **先恢复光标再退出备用屏**（`?25h` 在 `?1049l` 之前）。顺序反了，
   光标会在原屏幕上保持隐藏 —— 用户回到 shell 发现看不到光标。
3. **panic 钩子也要退出备用屏**。只还原 stty 不够，panic 后终端会一直停在我们这里。

`AltScreen` 用 Drop 保证所有返回路径（含 `?` 早退）都还原，
显式 `leave()` 用于需要"先还终端再打印摘要"的场合。

---

## 4.15 交互对齐 opencode：@// 弹窗、!shell、多主题

### (a) `@` 之前"没反应"——因为它只在 Tab 时插入最佳匹配
上一版 `@` 的唯一行为是"按 Tab 补全成最匹配的一个文件"。用户看不到有哪些候选，
输错一个字母就被替换成别的东西。opencode 的做法是**输入即弹列表**：实时过滤、
↑↓ 选择、Enter 确认。差别是"能用"与"可用"。
实现上新增 `popup.rs`（只管选择状态，不碰 ANSI，可脱离终端单测）
与网格里的 `draw_popup`。

### (b) 弹窗必须**不透明**
第一版弹窗只覆盖"我们写了字符"的格子，其余格子保留底下的 logo/正文/星场 ——
真机截图里能看到 logo 碎片从弹窗行里透出来。修法是先把弹窗占据的**整个矩形**
`blank` 成空格并标记已写入。这是网格模型的一个通用教训：
**"画了什么"与"没画的地方"要分开决定**，否则背景会渗上来。

### (c) `!shell` 用 `Op::Shell` 走同一条执行路径，而不是宿主机自己 fork
`!cmd` 的正确做法不是让 TUI 直接 `Command::new`（那会绕过沙箱），
而是在协议层加 `Op::Shell`，由内核构造 `ToolInvocation` 走 `execute_one` ——
沙箱、输出上限、截断标记、落盘全部自动一致。
**踩到的坑**：内核构造参数用的键（`command`）与 BashTool 读取的键（`cmd`）
不一致，工具拿到 `None` 直接 `exit -1`。编译期无法发现，端到端跑 `!echo` 才暴露。
已加回归断言钉住"命令确实到达了工具"。

### (d) 主题做成可切换，而不是写死一套色
写死颜色等于替用户决定对比度够不够。给出 6 套（opencode / nord / gruvbox /
rosepine / tokyonight / catppuccin，色值取自 opencode 主题文件的 dark 分支），
`ctrl+t` 循环、`/theme` 选择、选择落盘到 `~/.neo/theme`。
加主题时**不需要**手工维护 256 色/16 色映射表 —— 从主题 RGB 现场降级。

### (e) 一个被测试抓住的"假失败"（与 §4.14 同类）
`memory.rs` 的 `a_step_does_not_deep_copy_the_whole_history` 并行跑时随机失败，
报"疑似每步深拷贝历史"。单独串行跑必过。
真因是计数分配器是**全局**的，而该文件只有**一个**用例持锁 ——
其它用例并行分配照样污染计数。修法是让文件内**每个**用例都持同一把锁。
教训同 §4.14：**先怀疑测量工具，再怀疑被测对象**。
这个用例本身就在防"O(N²) 深拷贝"，用它来误报深拷贝问题，尤其容易带偏。

---

## 4.16 审批不能是"盲批"：diff 预览与任务清单

### (a) 发现了一个"定义了却从没发过"的事件
审计对齐度时 grep 协议层，发现 `EventMsg::PatchProposed` 定义了、
`neo-exec` 也写了渲染分支，但**内核从来没发出过它**。
后果是审批时只显示"写入类调用需确认" —— 用户不知道改哪个文件、改了什么，
实质是盲批。opencode 的权限弹窗会渲染 diff。

修法分三处，**职责边界是重点**：
- 工具侧 `Tool::preview(args) -> Option<(path, diff)>`：只有工具知道
  自己的参数怎么变成改动，内核不该解析工具特有的参数格式；
- 内核在 `GateDecision::Ask` 之前调 `preview`，发出 `PatchProposed`；
- 宿主渲染。`Fact::PatchPreview` 与 `ApprovalNeeded` **分开建模**：
  一个是"需要你决定"，一个是"你要决定的内容"，合并会让网络类审批无法表达。

### (b) diff 生成必须内存有界
朴素 LCS 的 DP 是 O(n×m)：两个 4000 行文件要 1600 万格，足以拖垮进程。
`neo-capability/src/diff.rs` 三层防护：
1. 先剥公共前缀/后缀 —— 常见情形（大文件小改动）根本不进 DP；
2. 中间段乘积超限就不对齐，退化为"N 删/M 增"；
3. hunk 数上限 + 每 hunk 只留 3 行上下文。
超限一律如实标注"以上为节选" —— 静默截断会让用户以为"就这么点改动"。

### (c) 任务清单：又一个"工具声明、内核转发"
opencode 有 todo 面板（`[✓]/[•]/[ ]`）。我们本来没有 todo 概念，本轮补上。
关键设计是 `Tool::report(args) -> Vec<EventMsg>`，与 `preview` **对称**：
preview 是"执行前给用户看什么"，report 是"执行后向宿主声明什么"。
都由工具决定，内核只转发 —— 内核不需要知道 `todowrite` 的 items 长什么样。
清单用**整表替换**而非增量：模型每次给完整清单，宿主不必维护差量状态，
也就不会出现"某步增量丢了导致清单错乱"。

### (d) 诚实缺口（为什么这些**没有**对齐）
opencode 有 21 个 dialog 组件。逐个核对后，**大部分对我们不适用**，
因为它们选择的对象我们还没有：

| opencode dialog | 为什么没做 |
|---|---|
| dialog-model / dialog-provider / dialog-variant | 多模型切换需多 provider 注册表 + 变体，我们只有 single provider |
| dialog-session-list / rename / move | 多会话管理（列表/改名/迁移）我们没有 |
| dialog-agent / dialog-skill | 多 Agent / Skill 注册表我们没有 |
| dialog-mcp / dialog-debug / dialog-console-org | MCP、调试台、组织账号我们没有 |
| dialog-workspace-* | 多工作区/worktree 我们没有 |
| dialog-stash / dialog-tag / dialog-timeline / undo / redo / share | 依赖 git 快照、分享服务、时间线回退 |

**做了一个**：`dialog-help` / `dialog-status` 对应的 `/help` `/keys` `/status`
信息屏（已实现）。以及 `dialog-theme-list` 对应的主题选择（已实现）。

判断标准很简单：**dialog 是"选择器"，得先有可选项**。
没有多模型却做模型选择器，只会得到一个永远只有一项的假对话框。
这比不做更糟 —— 用户会以为功能坏了。

其余已知未对齐（真实缺口，非"不适用"）：
- **markdown 语法高亮 / 代码块着色**：我们按纯文本渲染助手回复
- **鼠标支持**（点击/选择/滚轮）：我们只处理键盘
- **输入框内的内联高亮**（`@file` 显示为彩色 chip）：opencode 用 extmark
- **vim 编辑模式 / 多行输入框**：我们是单行输入
- **`/undo` `/redo`**：需要 git 快照集成

---

## 4.17 右侧面板 / Markdown 高亮 / 两次"顺序错了"的 bug

### (a) 侧栏：三块面板，只放我们有数据的
对齐 opencode 的 sidebar 插件集（Context / Todo / Modified Files / MCP / LSP），
但**只做前三块** —— 我们没有 MCP 与 LSP，做了就是永远空的面板。
- Context：最近一轮的 in/out token + 上下文占用率。**上限未知时不显示百分比**
  （`context_limit == 0` → 显示"上限未知"），宁可不给也不编数字。
- Todo：模型自述进度的镜像（正文里也有一份）。
- Modified Files：路径从**左侧**截断（`…/module.rs`），保留文件名 ——
  文件名的识别价值远高于前面的目录层级。

布局上有个必须处理的冲突：输入框/弹窗/状态行原本按**整屏**宽度居中，
加了侧栏后它们的右边框会横穿侧栏竖线（真机截图可见）。
修法是引入 `body_cols()`，把正文区与所有居中计算都收敛到它。
另外侧栏的显示判据（`cols >= 96`）放在**渲染层**而不是调用方 ——
判据依赖 cols，就该在知道 cols 的地方做，否则任何别的调用点都能绕过它。

### (b) Markdown 高亮：词法级，不用 tree-sitter
opencode 用 tree-sitter WASM（每语言一个 .wasm + 在线拉 query）。
那对本项目是**方向相反**的选择：引入 WASM 运行时 + 网络依赖 + 数十 MB，
把"调试链要浅"整个推翻。这里做词法级（注释/字符串/数字/关键字/类型），
不跨行持有状态，单行超 4096 字节直接不高亮（内存有界）。
它不是语法分析 —— 分不清 `foo(bar)` 里哪个是函数名。但终端里 90% 的可读性
提升来自"字符串和注释不再是一片同色"，词法级就能拿到。

### (c) 两个"顺序 / 口径错了"的 bug（都属于只有真机才暴露）
1. **改动统计算在了执行之后**。第一版让工具在 `report()` 里事后算 diff ——
   执行完文件内容已等于目标，diff 为空，侧栏"已修改文件"恒空。
   更糟的是**假工具测不出它**：我的 `PreviewTool` 无条件返回预览，
   测试全绿而真机为空。修法：内核在**执行前**取 preview（审批预览用的就是它），
   执行成功后用它统计。并补了一个用**真实 ApplyPatchTool + 会落盘的沙箱**
   的用例 —— 假工具掩盖的 bug 必须用真工具钉住。
   （顺带：测试沙箱的 `write_file` 是桩，不落盘；用它验"文件变了"是假失败。）
2. **byte/char 下标混用**。Markdown 的强调解析用 `String::find` 拿字节偏移
   当字符数用，遇到中文（多字节）会整段跳过 —— `**重点** 与 *次要*` 里的
   "与"直接消失。字面量下标运算必须统一口径，已全部改为 char 索引并加断言。
   同一个模块里还有一处：逐 span 换行会让每个 span 都从同一列开始、
   互相覆盖，代码块在终端里变成碎片。改为"先整体高亮、再整行排版"。

---

## 4.18 离线可验证性：三个桩 provider 的分工，以及又一次"可达性"疏漏

### (a) 没有 API key 也要能把功能走一遍
问"mock 能验证各个功能吗"时我做了一次逐项实测，结论是**分三个桩 provider**，
因为它们覆盖的链路不同：

| provider | 产出 | 覆盖 |
|---|---|---|
| `mock` | 一句固定文本 | 界面与交互全览（弹窗/命令/面板/主题/历史/侧栏/信任） |
| `selftest` | 调一次 `apply_patch` | 审批拦截 → **审批前 diff** → 批准 → 真实落盘 → 侧栏改动统计 |
| `demo` | Markdown + `todowrite` | Markdown 高亮、正文与侧栏的任务清单 |

27 项 pty 逐项核对全过。**离线验不到**的是：真实推理质量、模型是否真的会
自己调 `todowrite`、`/compact`（未实现）。这三项必须真实模型。

### (b) 又一次"定义了却不可达"
`Action::Status` 定义了、`status_text()` 也写了，但**忘记把它注册成命令** ——
`/status` 永远"无匹配"。之前 `PatchProposed` 也是同类问题（定义了从没发出）。
两次都是"契约/枚举建好了，接线漏了"，而编译器**不会**提醒。

修法是把可达性做成**库内不变量**而不是测试：
- `COMMANDS` 抽成 const 表（数据与取用分离）；
- `audit_action_reachability()` 读它；
- `registry()` 里 `debug_assert!` —— 于是**任何 debug 构建**（含开发时直接
  跑起来）都会检查，而不只是 `cargo test` 时。
- `Action` 分成 `COMMAND_ACTIONS`（必须有命令）与 `INTERNAL_ONLY_ACTIONS`
  （只由界面产生，如 `SetTheme` 只能从主题列表选，不该有 `/settheme`）。

实现这个审计时踩了一个小坑：审计函数最初调 `registry()`，而 `registry()`
又调审计 → **无限递归**，测试里表现为 stack overflow。表格必须是数据
（const），取用才是函数。

### (c) 信息屏的滚屏方向错了
`/help` 变长后，它沿用了对话的"显示末尾 N 行"逻辑 —— 结果把标题裁掉、
只留中间。**对话要从最新看起，文档要从第一行看起**，两者方向相反。
改为从头显示 + 如实标注"还有 N 行"。

---

## 4.19 真机"退出残留"的真因：窗口 resize 后我们从不重绘

用户第二次报"退出还是有问题"（截图：顶部/右侧残留一条侧栏 + 大片空白）。
备用屏机制本身是对的（实测 `?1049h` 与 `?1049l` 各一次、退出后 0 字节残留），
所以问题不在退出，而在**进入之后就没跟上终端状态**。

### 定位过程（值得记：两次都被错误假设带偏）
1. 先怀疑顺序：`raw.restore()` 与 `alt.leave()` 谁先？→ 实测字节流完全正确。
2. 再怀疑行超宽导致折行滚屏：写了个"任何尺寸/任何内容下单行都不得超宽"
   的断言矩阵（含侧栏 + 长路径 + emoji + 中文），**全过**。
3. 最后测"改变窗口尺寸会不会触发重绘" → **不会，0 字节输出**。真因找到。

`read_key` 是阻塞读，主循环因此被钉死在 `read` 上 —— 而**终端不会为 resize
产生输入**。于是改窗口后画面停留在旧尺寸的布局：侧栏留在旧位置、正文空出
一大块。用户看到的"退出残留"其实是**进入后不久就已错位的画面**。

### 修法：带超时读 + dirty 重绘
- `read_key_timeout(stdin, tenths)` 用 `stty min 0 time N` 让 read 到点返回；
  返回 `None` 表示**无输入**（不是 EOF——交互下退出走 Ctrl+C 的字节）。
- 主循环每 0.1 秒醒一次，**只在尺寸变化时**置 dirty 重绘。
  第一版没有 dirty，结果 10Hz 空转：启动就刷了 824KB 输出、终端持续抖屏。
- 信任对话框与信息屏同样处理；信任对话框的按键处理抽成 `handle_trust_key`，
  让"等待输入"与"渲染"两条路径共用同一套语义。
- 退出序列加一步 `2J`：给"把备用屏内容物化进 scrollback"的终端兜底
  （部分模拟器/内嵌终端切换缓冲时不丢弃而是保留）。

### 教训
`?1049l` 只保证"切回原屏幕"，不保证"用户没看到过错位画面"。
**UI 必须跟上终端尺寸变化** —— 这是 TUI 的基本义务，与备用屏无关。
阻塞读的 TUI 默认就不满足它，而这个缺陷只在真机拖窗口时才显形，
pty 里按键驱动的测试永远发现不了。

---

## 4.20 默认主题改为 NEO 自己的紫

用户反馈"高亮颜色是橙色，能换成我们自己的紫色吗"。橙色来自上一轮"对标
opencode"时**照搬了它的调色板**（`primary = #fab283` 暖橙）——
那是 opencode 的品牌色，不是我们的。

关键点：改的不只是一个色值，而是**哪个语义槽承载品牌色**。
`primary` 承担的是最常出现的强调（代码里的函数名、列表圆点、弹窗选中标记、
logo 渐变起点）——这些位置的颜色**就是**"这个产品的颜色"。
`accent` 只用在标题、用户消息竖条这类结构性位置。

于是：
- `ThemeName::Neo`（新，默认）：`primary = #a78bfa`（violet 400），
  `accent = #e879f9`（fuchsia 400）——同色系，logo 渐变从紫到品红自然过渡。
  `muted` 也改用略带紫调的灰（`#8a8a94`），让整体同族。
- `ThemeName::OpenCode`（保留，改名由"默认"变"参考"）：opencode 官方暖橙。
  保留理由是可对照，但不再默认。`parse("default")` 现在指向 Neo。

顺手加的两条测试用**色相族**而不是硬编码色值来断言默认主题是紫
（蓝/红高、绿低），这样以后微调具体色值不会误报。

---

## 4.21 多行输入：从 `String` 换成真正的编辑器

用户问"还差哪些没做"，排在第一位的就是**输入框只有单行**。
之前 `input` 是 `String`，所有编辑都是 `push`/`pop` —— 只能改末尾。
贴一段代码进去就没法改中间、没法换行、没法删整行。

新增 `editor.rs`（`Editor`），要点：
- **全部按字符索引**（`col` 是字符下标）。中文一行 10 字、字节长度 30，
  混用口径会让光标停在半个汉字上，或在 `String::insert` 时 panic。
  换算只允许在 `byte_index()` 一处发生 —— 散落各处一定有人忘。
- **撤销栈有界**（`MAX_UNDO=100`）。存的是文本快照，无界就是内存泄漏。
  连续打字合并为一个撤销单元（逐字符撤销很烦）。
- 词边界按**空白**划分（与 shell/readline 一致），中文不按单字拆。
  代价是 `中文测试ok` 无空白时会被整段删掉 —— 已写进模块文档的边界。

布局随之改动：输入框高度**动态**（1..6 行），所以
`CHROME_ROWS` 常量不够用了，改为 `chrome_rows(input_rows)` 函数。
凡是"正文可用行数"的地方都必须走它，否则多行输入会盖住正文。

### 顺带修掉的两个真 bug
1. **输入行不截断**：网格的 `put` 只受 body/sidebar 边界约束，**不知道
   "框"的右边界**，所以长输入会盖掉右边框。已按框内宽度截断。
   这类 bug 是"多行测试"顺手抓出来的 —— 单行时输入短，看不出来。
2. **ESC 序列只读 3 字节**：`ESC [ 3 ~`（Delete）、`ESC [ 1 ~`（Home）
   是 4 字节序列，之前被截断成前缀而永远匹配不上。改为读到终止字节
   （`~` 或字母）为止。

---

## 4.22 转录滚动与搜索：follow 语义 + 行号必须单一事实源

长会话要看前面的内容，之前只有"显示末尾 N 行"，翻不回去。

### (a) 关键语义是 follow，不是"永远滚到底"
新内容到达时该不该自动滚到底？两种极端都错：
- 永远滚到底 → 用户往上翻看时被新内容**拽回底部**，没法读；
- 永不滚到底 → 得手动追新内容。
`view.rs` 引入 `follow`：在底部时跟随；一旦主动上翻就停止跟随，
回到 offset==0 再恢复。`window()` 还会按渲染时的 total **夹取偏移**。

### (b) 夹取偏移不是洁癖，是防"整屏空白"
滚动时的 total 来自估算、渲染时的 total 来自实际 —— Markdown 换行数
本来就难精确预估。不夹的话偏移可能指到内容之外，视口算出 `start==end`
→ **整屏空白**，用户会以为程序挂了。测试就是这么抓到的。

### (c) 行号必须单一事实源（否则搜索高亮标错行）
第一版 `searchable_lines` 自己按 Markdown 渲染一遍来取文本，与
`fact_lines` 的换行结果不一致 → 命中行号与实际渲染错位，高亮标在
无关的行上。修法：把 `fact_lines` 从方法抽成**自由函数**（渲染、
搜索、行数估算都调它），两个辅助函数退化成一行委派。
顺便：搜索文本不能用 `format!("{f:?}")` ——那会把枚举名纳入匹配，
用户搜 "Assistant" 会命中每一行（屏幕上根本没这个词）。

### (d) 提示行的位置要按"正文区最后一行"算
滚动提示原本放在"已显示内容的下一行"，贴底布局下会落到正文区之外，
提示永远看不见。改为固定在 `body-1`。

---

## 4.23 鼠标支持：三个"只在真机才暴露"的实现错误

新增 `mouse.rs`（SGR 解析）+ `MouseMode`（RAII）+ 渲染产出 `Regions` 做命中测试。

### (a) 为什么用 SGR 而不是老协议
老 X10 模式把坐标编码成单字节（值 = 32+n），**列号 >223 就溢出**。
用户实测用过 260 列终端，所以必须用 SGR 扩展（`?1006h`）：坐标是十进制
文本，多宽都行，且能区分按下/释放。

### (b) 命中区域由**渲染产出**，不是输入时另算
`render_with_regions()` 顺手记下转录区/输入框/侧栏/弹窗候选项的矩形。
另算一遍等于把布局逻辑写两遍，两边一漂移鼠标就点错地方，而且很难归因。

### (c) 三个真机才暴露的错误
1. **`last_regions` 声明在循环内**：`dirty == false` 的迭代不重绘，
   而鼠标事件到达时正好是那一轮 → 区域是空的，**点击/滚轮全部无效**。
   必须跨迭代保留。测试直接构造区域所以测不出，只有 pty 端到端才发现。
2. **`seq.len() == 3` 判断鼠标序列**：上一个循环已经读走最多 3 个字节，
   到判断处可能已是 4 字节（`ESC [ <` + 第一个参数）。写成 `==` 让判断
   永远不成立 → 鼠标彻底失效。改为 `>=`。
3. **矮终端下弹窗装不下就干脆不画**：用户按 `/` 什么都没出现，看起来像
   功能坏了。改为**收缩可见行数**；连一行候选都放不下时至少画标题框。

### (d) 一个交互陷阱：收起侧栏后鼠标没法再展开
点击侧栏收起后，侧栏区域随之消失 —— 鼠标用户**再也点不开**（只能回去按
键盘）。修法：收起状态在最右一格画一个 `‹` 把手，点它展开。
这类"操作后入口消失"的陷阱在只能用键盘测试时发现不了。

---

## 4.24 全屏 diff 查看器：三个建模错误

之前审批时把 diff 前 200 行打进对话流。对大改动完全不够：读不完、
跳不到想看的文件、没法只看某个 hunk。新增 `diffview.rs`（解析 + 导航）
与全屏查看器（文件树 / 统一与双列视图 / hunk 与文件跳转）。

### (a) 三个建模错误（都是测试抓出来的）
1. **hunk 被记了两遍**：建占位时 push 一次、收口时又 push 一次。
   表现是 `hunks.len() == 4`（实际 2）。
2. **`File` 没有自己的起始行**：想用 `hunks[0].start - 1` 反推文件头，
   但文件头有**两行**（`---` / `+++`），而且没有 hunk 的文件根本推不出来。
   改为显式记 `line_start`。
3. **删除行错误地保留了"新行号"**：unified diff 里删除行只属于旧文件、
   新增行只属于新文件。这个字段错了会让双列视图把行号放错边。

### (b) 只有两种视图，不做"严格左右配对"
双列视图是**把删除与新增分列**，不是按 hunk 做行对行配对。后者需要
更复杂的对齐算法，而宽终端下"删一边、增一边"已经够读。这是有意的
简化，边界写在模块文档里。

### (c) 又一个"insert 静默失败"
本轮有两次 `str.replace` 的锚点文本已经变了，替换没生效但**脚本不报错**，
表现为"功能加了却没生效"。教训：这类批量改动用 `assert` 校验锚点存在，
或按行号插入并立刻 grep 确认。后来改成按行索引插入 + `grep` 复验。

---

## 4.25 挂上 ai-efficiency-rules：把效率从「自觉」变成「机器可判」

用户新增了 `docs/ai-efficiency-rules/` 技能并要求以后项目都挂着它。

### (a) 不只是放个目录，而是四件事都做
1. **装到用户级**（`~/.agents/skills/ai-efficiency-rules`）——技能要能被
   自动发现才叫"挂着"；只躺在 `docs/` 里只是一个文件夹。
2. **工作区级用 symlink**（`.agents/skills/ai-efficiency-rules →
   ../../docs/ai-efficiency-rules`）——指向同一份文件，**避免两份副本漂移**
   （技能改了一处、另一处还是旧的，比不装更糟）。
3. **AGENTS.md 里声明为强制技能**——`AGENTS.md` 每轮都会进上下文，
   而技能的触发依赖 description 匹配；声明之后"总是适用"才成立。
4. **接进门禁**（`scripts/verify.sh` 第 4 段）——先前这些规则只写在 AGENTS.md
   里靠自觉，而本项目已经**因为固定 sleep 浪费过一整轮时间**。
   现在 error 级（固定盲等 / 重复拉取 / 无退出轮询）会卡门禁。

### (b) 脚本实测发现的真 bug：失败被缓存成成功
`cache_fetch.sh` 用 `[[ -d $dest/.git ]]` 判断缓存命中。问题是**失败的拉取
也会留下 .git**：`git clone --branch` 失败后脚本会 `git init` 再 fetch，
fetch 再失败时 `rm -rf` 已经来不及（`||` 分支里 init 在前），目录里就有了 .git。
后果：下次调用报 `HIT(cache)` 并返回一个**空仓库**。
**缓存把失败缓存成成功，比没有缓存更糟** —— 调用方拿到"成功"的路径、
拿不到内容、且不会再重试。

修法：
- 引入完成标记 `.fetch-ok`，**只有 checkout 成功后才写**；
- 命中判断改为看标记，不看 `.git`；
- 有目录无标记 = 上次残留 → 清掉重来；
- 所有失败路径统一走 `fail()`：清理目录 + 非 0 退出。

已双向验证：失败两次仍失败（不再 HIT）；成功一次后第二次 0.02s 命中且内容完整。

### (c) 审计脚本会"自我命中"
`audit_efficiency.py --path docs/ai-efficiency-rules` 会把**规则表里用于说明的
违规样例**当成真实违规（规则描述行含 `git clone`、建议文本含 `node_modules`）。
所以门禁只扫 `crates/` 与 `scripts/`，不扫该技能目录自身 —— 否则门禁会
因为"文档里写了什么叫违规"而永远失败。这是**通过排除范围**解决的误报。

---

## 4.26 which-key 与 /undo：可发现性 + 一个必须说清的边界

### (a) which-key：把"想不起来"这件事解决掉
已有 20 多个键位，但只静态列在 `/keys` 里 —— 用到时想不起来。
新增 `ctrl+/` 弹出"当前上下文能按什么"的卡片。

两个设计决定：
1. **不用 leader 键**（opencode 用 `ctrl+x` 两段式）。本项目已有大量单段
   `ctrl+`/`alt+` 组合，再引入 leader 会让同一个键在不同上下文含义不同，
   反而难记。改为**显式请求**（`ctrl+/` = "我忘了能按什么"）。
2. **只显示此刻真实可用的键**。弹窗打开时输入框的编辑键会被弹窗先吃掉，
   那些键"不可用"就不该提示 —— 提示按不到的键比不提示更糟。
   `Context` 枚举 + `detect()` 表达这个优先级，且用测试钉住
   "同一上下文里同一个键不得出现两次"（这条真抓到了 `ctrl+b` 写了两处）。

放**右下角**而不是居中：用户按它时通常还要继续打字，居中会盖住刚看的内容。
任意键即关闭（否则会一直挂在那里挡正文，像界面卡了）。

### (b) `/undo`：回退对话，**不还原文件**
内核新增 `Op::Rewind`。实现要点：
- 轮次边界用**用户消息**判定，不是助手消息 —— 一轮可能含多个助手消息
  （工具调用），只有用户消息才是轮的起点。只删用户消息会留下无主的助手发言。
- 回退时清掉挂起的审批：那批调用已无意义，否则内核会停在
  "等一个已经不存在的轮次"的状态。

**最重要的边界**：磁盘上的改动**不会**被撤销（我们没有 git 快照）。
所以 `Rewound` 事件带 `files_kept`，界面显示"注意：磁盘上 N 个文件的改动
未被撤销"。让人以为"undo 连文件一起回去了"是最危险的那种错觉 ——
这一条在协议注释、渲染、测试里各强调一次。

另：`KernelError` 新增 `RewindTooFar` 而不是借用 `Unimplemented` ——
一个说"这个能力还没有"，一个说"你的参数不成立"，混用会让用户以为功能缺失。

### (c) 一个"看起来没生效"的假象
which-key 加完后 pty 测试说卡片没出现，我差点去改渲染逻辑（单测是通过的）。
真因是**装了旧二进制**：`cargo install` 发生在移动 match arm **之前**，
跑的是改动前的版本。加临时 eprintln 一看就清楚了
（`key=WhichKey → groups=4 → 卡片已画`）。
教训：pty 验证前先确认二进制是新的；"单测过但端到端不过"时，
先怀疑二进制/环境，再怀疑逻辑。

---

## 4.27 工具输出与推理：补上"事件里有、界面没有"的两处

### (a) 又一次"定义了却没人显示"
`ReasoningDelta` 在协议层定义了，但 `facts_of` **从未把它转成 Fact** ——
推理过程从模型一路传进内核，然后在进界面之前被丢掉。
同时发现另一个更严重的：**工具输出根本没进事件流**。
`ToolCallEnd` 只带 `id` 与 `exit_code`，用户看到的永远是
`✓ bash exit 0` —— 命令打印了什么完全看不到。

这两处与之前的 `PatchProposed` 是同一类问题：**契约/事件建好了，
但没有接到用户能看到的地方**。这已经是第三次，所以在 PROJECT_MEMORY
里单独立了一节记录这个模式。

修法：
- `ToolCallEnd` 带上 `stdout` / `stderr` / `truncated`（内核已按上限截断）；
- `Fact::ToolFinished` 对应扩展；
- `Fact::AssistantThought` 新增，与 `AssistantSaid` **分开** ——
  混成一个 Fact 就无法独立控制显隐（`/thinking` 就做不到）。

### (b) 三处刻意的取舍
1. **默认折叠工具输出**：输出常常很长，铺开会淹没对话。
   但折叠时给出"N 行输出（/details 展开）"，否则用户不知道藏了东西。
2. **失败时强制展示**：出错还把原因藏起来，用户只能靠猜。
   这一条写成了测试（`failed_tool_output_is_shown_even_when_collapsed`）。
3. **"被截断"与"还有更多行"说法必须不同**：前者是内核截的（展开也看不到全部），
   后者只是界面折叠（展开就有）。混用会让用户以为展开能看到全部。

### (c) 协议变更的连锁成本（记录以便下次估时）
给 `ToolCallEnd` 加三个字段，波及所有构造点：内核执行路径、
审批拒绝路径、`neo-exec` 渲染、`neo-mock` conformance、`neo-host-web` 测试。
`cargo test --workspace --no-run` 一次就能把所有遗漏列全（类型系统的价值），
但要逐个补。协议字段是**公共契约**，加字段的成本是"构造点数 × 一处"，
所以宁可一次加对，也不要分几次加。

---

## 4.28 真实模型验证：一条命令就暴露两个"mock 测不出"的 bug

用户提供了有效 key，终于能跑真实模型。第一次调用就直接失败，
并且一次暴露两个只在真实 API 上出现的问题。

### (a) chunked 传输编码没解 —— 所有真实响应都解析失败
报错 `响应不是合法 JSON：trailing characters at line 1 column 2`。
真因：HTTP/1.1 的 **`Transfer-Encoding: chunked`**，响应体是
`1d1\r\n{...}\r\n0\r\n\r\n`。直接喂给 serde_json，它把长度前缀 `1d1`
读成数字 `1`，后面 `d` 就成了"多余字符"（正是 column 2）。

**为什么一直没有暴露**：之前的 HTTP 帧格式测试用 httpbin，那个服务用
`Content-Length`。**测试选的服务器决定了哪条路径被覆盖** —— 选了个
不用 chunked 的，于是这条最重要的路径从未被测过。
补了 `decode_chunked()`（含块扩展、trailer、大写十六进制、上限保护），
并加了一条**正向对照**：不解码时 JSON 解析必然失败、解码后成功 ——
这样"解码是必需的"这件事被测试固定下来，而不是靠注释说明。

### (b) 工具结果只发 stdout —— 拒绝/失败对模型不可见
只发 `stdout`，`stderr` 被丢掉。后果实测可见：模型被审批拒绝后说
**"工具返回空、无报错"**，然后请求重试 —— 它完全不知道是自己被拒了。
而这正是"沙箱拦住了它"这个事实最需要被模型理解的时刻。

修法：工具结果带上 stderr、非零退出码、截断标记。
修完同一个场景，模型说 **"rejected by the user"** —— 表述准确了。

### (c) 实测确认的四件事（都是真机，不是 mock）
1. 真实推理可用：`1+1 等于 2`，真实 token 计数（669 in / 8 out）。
2. 真实工具循环可用：模型自己 `ls -la` 并正确解读输出（还知道 `.`/`..` 不算条目）。
3. 真实落盘可用：`world` → `earth`，文件内容确实变了。
4. **真实沙箱拦得住真实模型**：让它写 `/tmp/neo_escape_test.txt`（工作区外），
   它被拒绝、并自己推理出"这是真实边界而非路径笔误"（还注意到 `/tmp` 是
   `/private/tmp` 的符号链接）。它还明确拒绝绕过（不试 shell 重定向/符号链接/`..`）。

### 教训
mock/stub 能验证"机制对不对"，但**验证不了"真实对端的协议细节"**。
两次失败都不是逻辑错，而是"对端的实际行为与我的假设不同"：
- 服务器用 chunked（我的测试服务器不用）；
- 模型需要 stderr 才能理解失败（我的桩不关心）。

这类问题的唯一解法就是**尽早接一次真实对端** —— 它比再多写 100 个桩都有效。
本项目此前把"真实模型待验"标注了很久，而实际上一条命令就暴露了两个 bug，
说明这个待验项的价值被低估了。

---

## 4.29 ctrl+z 挂起 + 剪贴板 SPI（第 6 个 seam）

### (a) ctrl+z 挂起：机制可验，真停止受环境限制
把终端完整还原（光标/鼠标/备用屏）后给**自己**发 SIGTSTP，恢复时重进一遍
（终端可能被 shell 改过）。键位重排：`ctrl+z` 让给挂起（终端惯例肌肉记忆最强），
撤销退到 `ctrl+_`（readline 惯例），which-key 退到 `ctrl+o`。

**验证边界（诚实记录）**：本环境的进程 `tpgid=0` —— **没有控制终端**，
而 POSIX 规定 SIGTSTP 对无控制终端/孤儿进程组**被内核丢弃**。
这不是代码问题：对照实验显示连 `sleep 60` 直接 `kill -TSTP` 都停在 `S`。
可验证的是**机制**：还原 → 发信号 → 恢复重进三步都执行且顺序正确
（`?25h` / `?1006l` / `?1049l` 之后 `?1049h` / `?1006h`，再重绘）。
真实终端里信号会生效 —— 这一条我标注为"环境受限，未在真实终端验证"。

### (b) 剪贴板：为什么值得成为第 6 个 SPI
直接 `Command::new("pbcopy")` 会把 macOS 写死在调用点：Linux/Windows 静默失效
（用户以为复制成功了），测试也无法注入假实现。所以定义 `Clipboard` 契据：
- `SystemClipboard`：按平台选命令（macOS `pbcopy`、Windows `clip`、
  Linux 按序试 `wl-copy` → `xclip` → `xsel`，Wayland 优先）；
- `NoopClipboard`：headless 用，**返回明确原因**。

**核心契约**：`copy` 的返回值必须真实反映结果。不能"返回 Ok 但其实没写进去" ——
那会让用户粘贴出旧内容且完全不知道哪步错了。conformance 测试用
"能力探测式断言"钉住这点：自称可用就必须成功，自称不可用就必须 Err。

已登记进门禁（SPI 总数 5 → 6，仍在 MAX_SPI=8 内），契约指纹入库。

### (c) 真机验证（真实模型 → 真实剪贴板）
`/copy` 命令取最近一条**答复**（不含推理）写入系统剪贴板。
端到端实测：真实模型答"中国的首都是北京。" → `/copy` 报"已复制 9 个字符"
→ `pbpaste` 读到的确实是 `中国的首都是北京。`。
注：`/copy` 对**失败**也如实报（找不到剪贴板命令时显示"复制失败：…"），
不显示"已复制"然后让用户粘出旧内容。

---

## 4.30 ctrl+p 面板分组化 + 设置界面：只做能改的

用户指出 ctrl+p 比 opencode 薄太多（那边有推荐/会话/提示框/系统四组、
每项带键位）。核对后确实如此，本轮补齐。

### (a) 面板：分组 + 键位 + 模糊
- 命令表加 `Category`（推荐/会话/显示/系统）与 `keybinding` 字段；
- 面板按组渲染小标题，右侧显示对应键位 —— 让"命令"与"键盘"对上号
  （看到 `/theme` 右边写着 `ctrl+t`，下次就直接按键）；
- 过滤从**子串**换成**模糊**（子序列）：用户记得的是大概形状
  （"th" → theme/themes），精确子串会一个都不匹配。

### (b) 设置界面：假控件比缺控件更糟
`/settings` 列出四节。**关键决定**：只把内核真的支持运行时修改的项做成
可点击（主题/侧栏/工具输出/推理/回退/新对话/查看 diff），
其余（服务商、档位、上下文上限、会话 ID）**明确标为只读并给出原因**，
右侧灰字直接写出为什么不能改：

```
服务商    mock        运行中不可切换；用 --provider 启动参数指定
上下文上限 64000 tokens 由模型目录决定，本版本未接模型元数据
```

理由：一个点了没反应的假按钮会让用户以为功能坏了，比不做更糟 ——
这与之前"选择器没有可选项就不做"是同一条原则。
光标也只在**可操作行**之间移动（停在只读行上会让人以为能改）。

### (c) 实测抓出的真 bug：分组标题撑破了弹窗
分组标题占一行，但高度计算没算进去 → 内容溢出框外，其下界面的文字
**透上来**（实测看到 `/undo` 行前面挂着欢迎页的 `v0.1`）。
修法：把标题行数纳入高度，并用不动点收敛（先按无标题估 → 按实际标题数
收缩可见项，直到装得下）。补了一条测试专门断言"卡片左右边界之间不得有
框外内容" —— 这类"背景透上来"的 bug 只能靠量列发现，肉眼看容易忽略。

---

## 4.31 提醒 SPI（第 7 个 seam）：降级语义才是重点

### (a) 为什么值得成为 seam
与剪贴板同理（避免把 macOS 写死在调用点）。但提醒有个更关键的差别：
**它是最不该失败却很爱失败的能力** —— 常在无 GUI、无音频设备、SSH、
容器里被调用。所以本 SPI 最要紧的契约不是"能响铃"，而是
**失败时必须安静地不做**，绝不能崩、卡住，或把错误抛上去中断任务。

### (b) 与 NoopClipboard 有意的语义差别
- `NoopClipboard::copy` **返回 Err** —— 剪贴板不可用意味着用户操作没生效，
  必须报错（否则用户粘出旧内容还不知道哪步错了）。
- `NoopNotify::notify` **返回 Ok** —— 提醒不可用只意味着"少了个便利"，
  报错会中断主流程，那正是本 SPI 要避免的事。

同一个"noop 后端"，两种返回值，区别在**失败对用户意味着什么**。
这条写进了 conformance 用例的注释里，免得后人"统一"掉。

### (c) 默认关闭
对齐 opencode 的 `attention.enabled` 默认 false —— 没人喜欢工具自己响。
要就显式开（`/notify` 或 `NEO_TUI_NOTIFY=1`）。声音与通知**分开控制**
（专注时可能只想要视觉或只要声音）。

### (d) 实测
用假 `osascript`/`afplay` 拦截调用并记录，验证两条路径：
- 开启时：`display notification "完成（0 in / 0 out）" with title "Neo · 完成"`
  + 播放对应音色；
- 默认（未设环境变量）：调用记录为空（确实没响）。

另加了一条**注入测试**：通知文案含 `; touch /tmp/xxx` 时不得被执行 ——
我们走 argv 传参而非 `sh -c` 拼接，所以是安全的。这条断言防的是
"以后有人图省事改成 shell 拼接"。

**去重**是另一个必要设计：facts 每帧重新扫描，若不去重同一个"完成"会响个不停。
用 `HashSet` 记住已提醒的 key（有界，超 64 清空）。

---

## 4.32 背景纹理与 Logo 样式：装饰不得进入语义

### (a) 命名上不说谎
终端没有位图通道（少数图像协议支持面窄且不可移植），所以"背景图片"
在本项目里实际是**确定性字符纹理**。模块与命令都叫 `Background` 而不是
"图片"，避免"我放了张 png 却显示成点阵"的困惑。想放真图的诚实通道是
`NEO_TUI_BG_FILE`：用户给一个字符画文件（自行把图转字符，工具链成熟），
我们不做图像解码、不引依赖 —— 这条边界写在模块文档里。

### (b) 铁律：装饰不进语义
纹理不进 `Fact`、不影响 T6 宿主等价、只填"从未被内容写入"的格子
（复用 `Grid::fill_background` 的 `Tone::None` 判定），因此**永远盖不住正文**。
测试对四种纹理逐一断言"输入框与品牌名仍可见"。

### (c) 三种纹理都在 2%~5% 密度区间
第一版斜纹用相位 1/7 = 14%，测试直接报"密度不合理" —— 背景会盖过正文观感。
改为 1/17 且每 2 行考虑一次。这条阈值来自实测，不是拍脑袋。

### (d) Logo 由样式决定，不是"够宽就画大的"
之前是"终端够宽画 6 行大词标，否则退单行"。那让用户**无法选择** ——
宽终端上想去掉大 logo 也做不到。改为四种样式（大/小/极简/隐藏）显式可选，
`Large` 在窄终端仍会自动降级到小词标（不是不画）。
顺带把 `welcome_degrades_on_small_terminals` 的断言改准：原来断言"没有 █"
（小词标也用 █），现在断言"没有大词标特有的框线字符 ╗╔"。

### (e) 一个测试自身的坑（记下来免得重犯）
"纯色背景不该有 `·`"这条断言最初写成整段 `contains('·')` —— 但欢迎页
副标题里本来就有 `·`（"Rust 内核 · TUI / Web / Exec"），那是**内容**不是背景。
改为只检查"整行只有背景字符"的行。**断言要针对被测对象，不要把内容误判成背景。**

---

## 4.33 补齐后端：模型注册表 + 运行时切换（第四次"声明了没接线"）

用户要求"缺的后端能力先补齐，再做 TUI"。审计后发现的最大后端缺口：
`SessionPatch.model` 这个字段**从协议层就开始存在**，被 `ConfigureSession`
接受、写进 `cfg.model`、然后**从未被使用** —— 因为 `Kernel` 持有一个固定的
`Box<dyn ModelProvider>`，构造后不可更换。

这是同类问题第四次（前三次：`PatchProposed`、`Action::Status`、`/next`）。
后果是设置页只能把"服务商"标成只读、写"运行中不可切换"。

### (a) 设计：注册表放 L2，不是 L3
`models::ModelRegistry`（名字 → provider + 元信息 + 当前项）放在 **L2 core**：
它是内核自身的状态（主循环要按名字取 provider），放 L3 会让 core 依赖 L3
（违反只能向下）。provider 本身仍由 L3 实现并注入，依赖方向不变。

### (b) 三条严格性（都用测试钉住）
1. **注册名必须等于 provider 自报名** —— 否则列表显示 A、日志记 B，很困惑。
   这条当场就抓到了真问题：`ScriptedProvider` 所有变体都自报 `"mock"`，
   于是 `demo` / `selftest` 根本注册不进去。修法是给它加 `label` 字段与
   `with_name()`，一个桩可以自报不同名字。
2. **空注册表 / 默认项不存在** 在构造期就拒绝 —— 内核不能在没有模型的情况下
   被造出来，不能等到第一次请求才失败。
3. **切换失败必须列出可用名字**：`未知模型 x（可选：a, b, c）`。
   只说"失败了"用户还得自己猜。

### (c) 切换的正确性用"回答来自谁"证明，不看配置字符串
新增的用例建了两个自报名不同的桩（`small` / `big`），各回一句带名字的话；
断言**切换前回答来自 small、切换后来自 big**。只断言 `cfg.model` 变了
是不够的 —— 那正是修复前就"看起来对"的部分。

### (d) CLI 的模型构成（诚实标注）
`build_models` 注册：真实 `deepseek`（仅在有 key 时；**缺 key 不静默换成 mock**，
那会让用户以为在跟真模型聊）+ 三个桩（`mock` / `demo` / `selftest`，标
`production=false`，列表里带「（桩）」后缀）。列表用 `●` 标当前项。

### (e) 一个守卫测试提醒了我"能力变了要改断言"
`help_and_keys_advertise_only_real_keys` 里有一条黑名单
`["/models", "/agents", ...]`（"未实现的功能不得出现在文案里"）。
本轮把 `/models` 实现之后，这条断言**立刻失败** —— 守卫的作用正是提醒：
能力从无变有时，要把名字从黑名单挪出来，别留下过时的断言。
顺手把它改成正反两向：黑名单保留真正未实现的，另加"已实现的必须出现"。

---

## 4.34 多会话：先补上"日志无法重建历史"这个更根本的洞

用户要求"缺的后端能力先补齐"。多会话的前置条件是**从日志重建对话历史** ——
而审计发现它根本做不到：`ToolCallBegin` 只有 `id`/`name`，**没有 `arguments`**。

### (a) 这不只是"多会话做不了"，而是违反项目自己的约束
AGENTS.md 写着「**模型可见即已落日志**：凡进入模型请求的内容都要能从会话日志重建」。
模型请求里的 assistant 消息带完整 `tool_calls`（含 arguments），
而日志里没有 —— **日志重建不出模型实际看到的东西**，回放与审计都是不完整的。
所以这个字段是补洞，不只是多会话的副产品。

### (b) 分层判断：会话库 (L3) 与会话持久化 (L2 SPI) 是两件事
- `SessionPersistence` 管**一个**会话的追加写（append-only 契约）。
- `SessionStore`（新 crate，L3）管**一批**会话：列举 / 新建 / 删除 / 标题。
合成一个 trait 会让"单会话写入"的实现被迫知道目录布局 —— 把上层策略
塞进了下层机制。

### (c) 三个设计决定
1. **会话 ID = 文件名**（去扩展名）。文件系统即索引，删除会话 = 删除文件，
   不会出现"索引说存在但文件没了"。代价是 ID 必须是安全文件名 ——
   由 `sanitize_id` 强制（挡路径穿越：`../etc/passwd` → `etc_passwd`
   之类的安全形式）。这条有专门测试。
2. **标题取自第一条用户消息**，不额外维护标题文件（那会引入第二个真相源）。
   取不到就退回 ID —— 诚实显示"不知道叫什么"，而不是编一个"未命名会话"。
3. **单个坏文件不中断列举**：一个损坏的 .jsonl 不该让用户看不到其它会话；
   它以"id 兜底标题 + 0 条记录"出现，仍可被删除。

### (d) 切换不重建 Kernel，只换会话
`Kernel::switch_session(id, persistence)`：换 id、换持久化目标、重建历史。
模型注册表 / 工具集 / 沙箱都是同一个，重建整个内核会丢掉这些装配，
也让调用方重复一遍构造代码（两处容易不一致）。
宿主侧：`SessionControl` 契据由 CLI 实现（只有 CLI 知道内核怎么装配），
TUI 只调契据 —— 与 `submit` 闭包同一个思路，宿主仍不依赖 L2。

### (e) 门禁两次抓到"能力变了要同步改断言/注册"
1. `NewSession`（旧的"清空转录"）被 `NewSessionReal` 取代后，
   可达性守卫立刻报"NewSession 没有任何命令可达" —— 改为登记到
   `INTERNAL_ONLY_ACTIONS`（设置页仍可触发"清空转录"）。
2. `/sessions` 实现后，help 黑名单里的它必须移除（否则断言失败）。
3. 新增 crate `neo-session-store` 没登记进架构守卫的 LAYER 表 →
   A1 报"依赖了未知 crate"。**这是好事**：漏登记会被抓住而不是被忽略。

---

## 4.35 L4 编排：Goal 状态机 + 上下文压缩（三个"只在端到端才暴露"的错）

用户要求"后端缺的能力先补齐"。协议层早就定义了 `Compact` / `GoalSet` / `GoalPause` /
`GoalResume`，编排 crate 是空的 —— 于是这条链路名义上存在、实际不可用。

### (a) `Compactor` 是 L2 seam，策略放 L4
内核需要"什么时候压缩、压到什么程度"，但这属于**编排策略**，不是内核机制。
所以 L2 只定义 `trait Compactor { fn plan(&self, messages) -> Option<(String, usize)> }`
与 `NoCompactor`（永不压缩），具体策略 `PolicyCompactor` 放在 L4。
依赖方向 L4→L2 是向下，架构守卫通过；反过来（内核引用编排）会被 A1 直接拒。

### (b) 三个 bug —— 每一个都是"看着对，跑起来错"
1. **`trigger_at: 64` 让手动 `/compact` 静默失效**。语义被写成了"达到 64 条才自动压缩"，
   而 40 条的正常会话压缩请求被直接拒绝，用户看不到任何反馈。
   改为 `trigger_at: 8` 并明确语义是"**低于此值不值得压缩**"，不是自动触发线。
2. **`removed = before - keep_from` 算错了量**。`before` 是压缩前的总条数，
   `keep_from` 是保留尾部起点，两者之差是**保留数**，不是移除数。
   正确是 `removed = keep_from`。这个错会让状态栏显示"压缩了 N 条"里的 N 完全不对，
   但压缩本身仍然工作 —— 所以光看"能不能压缩"的测试抓不住。
3. **`/command` effect 路径（TUI 站点 4860）静默吞掉 `Effect::CompactSubmit`**。
   它落进了旧的 `Effect::None` catch-all，命令解析成功、状态机也生成了 effect，
   但没有任何分派 —— 表现为"敲了 `/compact` 什么都不发生"。
   这是第五次"声明了没接线"，根因同前：新 effect 变体加进枚举，
   消费侧漏了分支，而 Rust 的 `match` 在 catch-all 存在时不会报错。

### (c) 回放的正确姿势：只 drain 前缀
`rebuild_from_log` 处理 `ContextCompacted` 时，首版写成了 `rebuilt.clear()` ——
把**保留下来的尾部也一起清掉**，回放出的历史比真实历史短。
正确的语义是"把被替换的前缀换成摘要"：`drain(0..n)` 后再 `insert(0, summary)`。
这类错误只在"压缩后再回放同一个会话"时暴露，单测压缩本身看不出来。

### (d) 验证结果
`/compact` 在离线桩上端到端跑通：状态栏显示 `⇩ 上下文已压缩（8 条 → 摘要）`，
日志含 `context_compacted` 记录（带 `removed_messages` 与 `summary`），
回放重建出的历史与内存态一致。协议层 `Op::Compact` 从"已定义"变成"可达且生效"。

---

## 4.36 上下文引用：`@file` / `$skill` 从"解析了但没人用"到真正注入

用户说"后端缺的能力先补齐"。审计引用链路时发现：`parse_refs` 把 `@` / `#` / `/` / `$`
都解析成了 `ContextRef`，但**解析结果根本没进请求**——
TUI 提交时写的是 `refs: Vec::new()`，内核把引用压缩成一句
`[refs: 3]`（只有个数）。也就是说 `@src/main.rs` 发出去的是一串路径文本，
模型拿不到文件内容。这就是第六次"声明了没接线"，但这次连用户可见行为都是假的。

### (a) 解决顺序：先解析、再拼装；解析失败不阻断
一条引用的路径打错，不该让整个提问发不出去。所以解析失败只记一条摘要
（"📄 x.txt 读取被拒：…"），本轮照常提交。把"引用失败"升级成"提交失败"
是更糟的取舍：用户会以为应用坏了。

### (b) 注入内容必须落盘，且回放**不得重解析**
`block` 是模型可见内容，按 AGENTS.md「模型可见即已落日志」必须进事件流。
反过来，回放时**不能重新读文件**——文件内容早变了，重读会得到与当时不同的字节，
重建出的历史与真实请求不符。所以内容"当时定格、随日志走"：
`RefsResolved { summary, block }` 事件，回放时把 block 拼到刚推入的用户消息后。
有专门的回放往返测试（`ref_roundtrip_replays_into_identical_history`）。

### (c) 第一个 bug：只在有 block 时发事件 → 失败静默
`$nope` 找不到技能时没有 block，于是整个 `RefsResolved` 事件被跳过——
用户唯一能得到的反馈（"未找到，可用的是 X"）消失了。
改为**有摘要就发**（哪怕 block 为空）。测试先抓到这条：断言取不到事件直接 panic。

### (d) shell 注入是安全项，不是风格项
读文件用 `cat -- '<path>'`，路径来自用户输入（不可信）。
单引号转义独立成 `shell_quote()`（POSIX：`'` → `'\''`），
因为转义逻辑散落各处时漏掉一处的后果是把用户输入当命令执行。
有单独测试用 `a.txt; rm -rf /` 与 `it's.txt` 验证两条规则。

### (e) 分层：数据在 L2，发现（扫盘）在 L3
`Skill` / `SkillRegistry` 是 L2 数据（内核只认名字+正文，不知道目录布局）；
`neo-skill-loader`（新 crate，L3）负责扫 `SKILL.md`、解析 frontmatter。
加载发生在**装配点**（`build_kernel`）一次，而不是每次 `$skill` 都扫盘 ——
引用是热路径。代价（会话中途新增技能需重启）写进了 README 的诚实边界。

### (f) 两个"看目录就懂、写代码才会踩"的细节
1. **兜底名必须用文件夹名**：所有技能文件都叫 `SKILL.md`，用它当兜底名字会让
   多个无 frontmatter 的技能全部同名而互相覆盖（"加载了 2 个"实际只有 1 个）。
2. **递归深度固定 2**：技能目录里一旦有 `node_modules`，无限递归会把整棵
   依赖树扫一遍，并把依赖包里的 `SKILL.md` 当技能加载进来。有测试守住这条。

### (g) 未闭合的 frontmatter 不予采信
`---\nname: x\n正文（没有结束 ---）`：首版采信了 name=x，测试断言兜底名时失败。
半截 frontmatter 是残缺文件，采信其中解析到一半的 name 会让技能以错误的名字注册
（`$真名` 找不到、`$半个名字` 反而命中）。改为只有闭合才采信。

### (h) 验证
`neo exec --provider mock "@sample.txt#2-3 看看"` → `[refs] 📄 sample.txt:2-3 已注入`，
会话日志含 `refs_resolved` 且能搜到注入的正文（beta/gamma）。
`$ai-efficiency-rules` → `🧩 已注入（5801 字节）`；`$nope` → `未找到（可用：ai-efficiency-rules）`。
全门禁通过：451 测试、零 warning、架构/协议/会话/配置/SPI/效率守卫全绿。

---

## 4.37 项目指令（AGENTS.md）：契约早就写好了，但没人去读

承接"后端缺的能力先补齐"。查上下文装配时发现：`docs/neo-plan/02-架构设计/
上下文策略-fresh-by-default.md` 里把 AGENTS.md 级联合同写得很完整
（`~/.neo/AGENTS.override.md` → `~/.neo/AGENTS.md` → 仓库 → 子目录，32 KiB 上限），
`neo-config` 里甚至有 `AGENTS_MAX_BYTES = 32 * 1024` 这个常量 ——
**但全仓库没有任何代码读 AGENTS.md，那个常量是死代码**。
对一个编码 agent 来说这是核心上下文缺失：模型不知道项目的约定。

### (a) 系统提示词的组成也是"模型可见"，也要落日志
指令每轮都进模型请求。只落会话消息而不落它，审计时无法回答
"模型当时到底被要求遵守什么约定"—— 而这恰恰是解释模型行为的关键。
新增 `EventMsg::InstructionsLoaded { sources, block, truncated }`。

两个细节：
1. **只落一次**（`instructions_logged` 标志）。它每轮都进请求，逐轮落盘只会放大日志。
2. **回放不重读盘**：AGENTS.md 之后可能被改，重读会得到与当时不同的提示词。
   回放时用日志里的 block 重拼提示词，测试断言回放后 `system_prompt()` 与原内核**逐字节相同**。

### (b) 仓库范围必须由 git 根界定，否则会捡到无关文件
若无边界地向上遍历，会一路捡到 `$HOME/AGENTS.md` 之类与项目无关的东西。
契约把"个人基线"明确放在 `~/.neo/AGENTS.md`，所以仓库级级联从**最近的含 `.git`
的祖先**开始；找不到 git 根就只取 cwd —— 宁可不级联，也不越界。
有专门测试：无 `.git` 时上级目录的 AGENTS.md 不得被加载。

### (c) 截断：诚实边界，且不能切开 UTF-8
规划里写的是"超限让模型生成握手摘要"，那是更大的一步、**当前未实现**。
这里做诚实截断：截到上限、`truncated = true`、并在注入文本里明说
"内容已按 32 KiB 上限截断，未展示的部分不生效"。
悄悄丢掉一半约定比截断更危险 —— 模型会以为它看到了全部。
截断用 `truncate_utf8`（回退到字符边界）：中文 AGENTS.md 是常态，
按字节硬切会 panic 或产生乱码。有 `"约".repeat(MAX_BYTES)` 的测试守这条。

### (d) 分层：数据在 L2，发现（级联/扫盘）在 L3
`Instructions` 是 L2 数据；`neo-instructions`（新 crate，L3）负责算级联顺序与合并。
与 `Skill`/`SkillRegistry` 完全同构 —— 换指令来源（远程、内联）不需要动内核。

### (e) 优先级显式标注，不靠位置暗示
注入文本里每个文件都带 `<!-- 来源：路径 · 最高优先级/个人基线/仓库约定/目录级联 -->`，
header 明说"冲突时以标注最高优先级的为准"。
位置（谁先谁后）在提示词里的"显著性"是经验性的，标注是确定的。

### (f) 验证
从仓库根 `neo exec --provider mock "读一下 @AGENTS.md"` →
`[instructions] 1 个文件并入系统提示词（4549 字节）`，
且 `[refs] 📄 AGENTS.md 已注入` 同时生效（两条链路互不干扰）。
全门禁通过：460 测试、零 warning、架构/协议/会话/配置/SPI/效率守卫全绿。

---

## 4.38 设置页五个"没反应"：一次都出在"状态改了但界面没跟上"

用户报了一批症状：选主题回车没反应、选模型回车没反应、点新对话只有底部提示、
点背景只换名字画面不变、"选择完退出后发现之前选择的对话才出现"。
逐个查下来是**五个独立原因**，但都指向同一类错误：**状态下去了，表现层没接上**。

### (a) 弹窗被"刷新设置页"顶掉了 —— 主题/模型/会话三项都失效
Picker 动作的处理是 `settings_state = None; popup_state = Some(弹窗)`，
但紧跟其后还有一句无条件的"刷新设置页上的值"（`open_settings`）——
它看到 slot 是 None 就**又打开了设置页**，于是弹窗刚建好就被设置页盖住。
用户看到的是"回车后什么都没变"。
修法：只在仍停在设置页时才刷新（`if settings_state.is_some()`）。

这里有个通用教训：**"动作"与"动作后的收尾"混在一个函数里时，
收尾必须能判断"这一步到底要不要收尾"**。无条件收尾会撤销动作本身的意图。

### (b) 背景画在内容之后 —— 一个格子都填不上
`fill_background` 只填 `Tone::None`（空）格、不覆盖已有内容。设置页原来是
**先画内容再填纹理**，于是整屏都已有内容、纹理无处可填 ——
"点了背景只换了名字、画面没变化"。
修法：把 `fill_background` 挪到 `draw_settings` 之前（主界面几处一直都是这个顺序）。
测试用"星场 vs 无纹理的整屏输出必须不同"来守，比断言某处调用了它更结实。

### (c) "新对话"只清屏幕，没真新建会话
设置页那一行绑的是旧的 `NewSession`（`events.clear()`），而 `/new` 早已改成
真新建会话（`NewSessionReal` → 换 session id + 落新日志文件）。
只清转录的后果最危险：**用户以为开了新对话，内核却还记得全部历史**，
下一句提问仍然带着旧上下文。
修法：设置页改绑 `NewSessionReal`，并且**复用 `handle_session_effect`**，
不复制一份逻辑 —— 两处各写一遍必然漂移（这正是它当初漂移的原因）。
值文案也从"清空转录"改成"新建会话"，说清它到底做什么。

### (d) 会话 id 显示的是启动快照 —— "脱节"
`SettingsInfo.session` 取自 `About.session`，而 `About` 是 CLI 在启动时注入的
**快照**，切换会话后从不更新。于是切换成功、设置页还显示旧会话。
修法：直接读 `sessions.current()`（实时值），不再走 About。
模型名同理（切换成功后同步 `about.current_model`）。
**快照型数据不要用来显示"会变的状态"** —— 这类脱节会一直存在，
直到有人恰好盯着那两处对比。

### (e) 设置分支不置 `dirty` —— 退出后主界面不重绘
设置视图是"独占输入"分支，自己渲染完就 `continue`，走不到循环底部那句
统一的 `dirty = true`。于是关掉设置（或切换模型/会话）后主界面**不重绘**，
屏幕上留着的还是设置页那一帧 —— 就是用户说的"退出了却发现之前的操作才出现"。
修法：设置分支离开前无条件 `dirty = true`（代价只是一次重绘）。

### (f) 真机验证
- 主题：设置页高亮"主题"→ 回车出主题弹窗 → 选中后写入 `~/.neo/theme`（neo → opencode）。
- 背景：鼠标点"背景"行 → 值 stars → dots → diagonal，且纹理字符出现在画面上。
- 新对话：点击后提示 `已新建会话 s-60342000`，在新会话里发言后
  `s-60342000.jsonl` 出现 —— 确实是**另一个会话文件**，不是清屏。
- 模型：回车出模型弹窗。

全门禁通过：469 测试、零 warning、架构/协议/会话/配置/SPI/效率守卫全绿。

## 4.39 设置页选择器：从"跳出弹窗"改为"行内展开"

用户反馈"打开设置是全屏的，选择后回车又回到了对话框继续选择"，"体验割裂"。
这**不是 bug 而是界面范式不一致**：原实现是"关掉全屏设置页 → 退回聊天画面
弹居中对话框"，用户被迫在两个界面间来回跳。而这恰恰是我之前回答
"全屏还是对话框"那个设计问题时留下的半成品 —— 设置页是全屏，
选择却是对话框，两种范式混在了一起。

改法：内联选择器（`SettingsPicker`）——候选长在被选那一行的**下方**、
缩进与值列对齐，↑↓ 移动、enter 确认、esc 收起，全程留在同一页。
"选设置行"与"选值"成为同一页里的两个层次，而不是两个界面。
候选与动作复用 `popup::Item` / `ItemAction`，不另写一套 ——
主题/模型/会话在设置页里的行为必须与 `/theme`、`/model`、`/sessions` 完全一致。

嵌套后状态多了一层，带出一个真实坑：`picker_apply` 原放在**读键之后**，
而选择器 Enter 后 `continue`；下一轮 `read_key_timeout` 无按键返回 None 就继续，
于是那段**永远等不到执行**（表现为"候选里回车了但不生效"）。
修法：状态在上一轮就置好了，就应在下一轮**读任何输入之前**落实。

真机验证（pty 44×150）：打开设置 → 回车（主题）→ 候选就地展开在「主题」下方，
仍是设置页、右侧无聊天侧栏 → 选下一项确认 → 值变 opencode、候选无残留、
`~/.neo/theme` 落盘。

**这一节与 4.38 合起来说明一件事**：设置页那几个"没反应"，本质都是
"状态改了但表现层没接上"；而这一节是"表现层接上了，但范式不统一"。
功能对 ≠ 体验对，后者要单独当需求做。

## 4.40 服务商注册表：把密钥的存放位置当成安全设计来做

用户此前希望设置页有「服务商管理」。当时我判定"没有后端支撑"并记入诚实清单 ——
一个 provider 完全由环境变量决定（`DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL`），
多服务商配不了。这次补上 L3 `neo-providers`。

### (a) 密钥放哪是安全设计，不是存储细节
最省事的做法是让用户把 key 写进配置 JSON —— 那样一次 `git add .` 就泄露了。
所以注册表里存的是 **`api_key_env`（环境变量名）而不是密钥本身**：
注册表进版本库也安全，密钥仍留在环境里。这条取舍刻意写进模块文档 ——
将来有人想"优化"成直接存 key 时，会先看到这段理由。

### (b) 项目级的 providers.json 必须**明确拒绝**，不能静默忽略
契约（`check_config_layers.py` C2）说「安全敏感键只允许在用户级设置，
项目级忽略」。但"静默忽略"在这里是错的体验：用户会很自然地建一个
项目级 `providers.json`，然后对着"配了却不生效"完全想不通。
所以显式报错并说清原因与正确路径。真机验证：项目级文件存在时打印
`拒绝读取项目级 …：服务商密钥属安全敏感配置，只允许放在用户级 …`。

### (c) 敏感键判定从"黑名单"改成"白名单"
原来的 `SENSITIVE = {model_provider, profile, notify}` 是黑名单：
**将来新增的敏感键默认放行**。改成白名单 `PROJECT_ALLOWED`（未知即敏感）——
漏掉一个只会更保守（项目级不生效，用户仍可在用户级设）。
门禁加了 C5：用一个没人见过的键名验"项目级被拒、用户级可用"。
`neo-config` 里同时给出 `key_allowed_in()`，让契约在实现里可调用、在门禁里可验证。

### (d) 注册表校验抓出一个真实 bug：provider 自报名硬编码
`ModelRegistry::new` 会校验"注册名 == provider 自报名"，
而 `DeepSeekProvider::name()` 硬编码返回 `"deepseek"` ——
于是以别名注册一个自建网关时直接失败：
`注册名 my-gateway 与 provider 自报名 deepseek 不一致`。
同一个 OpenAI 兼容客户端可指向**任意**网关，名字必须跟着用途走。
加 `label` 字段（与 `ScriptedProvider` 同一个模式）+ `with_label()`。
**这个 bug 是校验规则自己抓出来的** —— 如果当初图省事把校验去掉，
它会变成一个更晚、更隐蔽的故障。

### (e) 又一个"快照当实时值"的坑
设置页「模型」行取的是 `about.model`（启动快照），切换后不更新 ——
与之前 `session` 踩的是同一个坑。改为 `about.current_model`。
这是同类错误的**第三次**出现（会话 id、模型名），已把它写进纪律：
界面显示"会变的状态"时，一律用实时值，不用启动快照。

### (f) 真机验证
- 用户级注册表 → `my-gateway` 出现在模型列表（5 个可选），描述正确
- 选中 `my-gateway` → 反馈"已切换到 my-gateway"，设置页模型行同步更新
- 缺 `MY_GATEWAY_KEY` → 该条跳过并提示环境变量名（不给"切过去就连不上"的选项）
- 项目级 `providers.json` → 明确拒绝并说明原因
- `neo exec --provider my-gateway` → 真的用该网关（自报名已改为注册名）

全门禁通过：477 测试、零 warning、架构/协议/会话/配置/SPI/效率守卫全绿。

## 4.41 光标停在汉字中间：字符下标 ≠ 显示列

用户截图：输入「你是谁」三个字，光标停在第**二**个字上。

根因是一处坐标单位混用：`editor.cursor()` 的列是**字符下标**（中文按 1 个数），
渲染时却直接把它当**终端列偏移**用（`left + 3 + ccol`）。汉字占 **2 列**，
于是 3 个字算成 3 列、实际 6 列 —— 光标落在第 3 列，正好是第二个字「是」的中间。

修法：取光标前的子串算 `width::display_width`，再夹到输入框内宽
（行被截断时字符偏移可能超出可视区，不夹会让光标跑到右边框外）。

**值得记的是：编辑器那个"列 = 字符数"是刻意的、且有测试守着**
（`cursor_column_is_character_count_not_bytes`：两个汉字 → 列号 2 而不是 6）。
编辑逻辑用字符下标是对的（与字节/宽度解耦）；错的是**渲染层没做换算**。
所以修的是渲染，不是编辑器 —— 若当时"顺手"把 editor 的列改成显示宽度，
反而会破坏编辑逻辑（移动/删除按宽度算会错位）。

测试从渲染输出里解析**硬件光标序列**（`ESC[r;cH`）来验，而不是断言内部变量：
- 三个全角字应比三个半角字多占 3 列；
- 每多一个全角字应 +2 列。
把修复临时改回去，测试立刻失败（left 0 / right 3）—— 确认它真能抓住这个 bug。

真机验证：空输入 col=18 → 输入「你是谁」col=24（+6，正确）→ 再输 1 个半角 col=25。

**同类教训第二次出现**：4.38 是"状态改了界面没跟上"，这次是"坐标单位不一致"。
共同点是**两个模块各自正确、接口处没对齐**。单位/坐标系跨模块传递时，
必须在**交界处**显式换算并写测试，而不是指望两边"看起来一样"。

## 4.42 服务商管理：密钥与配置分文件，权限是唯一的安全控制

用户指出"只有模型选择，不能管理服务商"。4.40 补的是注册表（只读），
这次补**增删改** —— 而"改"躲不开一个设计问题：密钥写哪。

### (a) 密钥与配置**分两个文件**
`~/.neo/providers.json` 是**配置**（名字/URL/模型），不含密钥；
密钥单独在 `~/.neo/provider_keys.json`，权限 **0600**。
理由：配置文件是用户会分享、会复制进版本库的东西；密钥必须在一个
"语义上就叫密钥"的文件里，看一眼就知道不能外传。
保存时用 `OpenOptions::mode(0o600)` **创建即收紧**，不留"先 0644 再 chmod"
的窗口。测试直接断言文件权限 `== 0o600` —— 明文密钥文件的权限是这条路径上
唯一真正的安全控制，必须可验证。

### (b) 表单里的密钥打码，且点数与长度无关
打码显示 `••••••••`（固定 8 点）：**不泄露密钥长度**。
编辑已有服务商时密钥字段留空 = 不改动已存的 —— 让用户重新粘贴一遍
既不安全也没必要（而回显明文更糟：终端会被共享/录屏）。

### (c) `base_url` 必须规范化
用户会自然写 `https://api.deepseek.com/v1`（OpenAI 兼容生态的写法），
但底层 HTTP 客户端用 `openssl s_client -connect host:443` 建连，**要裸主机**。
不规范化的话，填的 URL 看着完全正确、实际连不上 —— 这种"看起来对但用不了"
最难排查。`normalize_base_url` 剥 scheme、拆路径，并补 `/chat/completions`。
**不猜**别的（不做"自动补 https"之类），猜错比不猜更糟。

### (d) 又一个"落进 `_` catch-all"的静默无操作
删除做了"候选 → 选中 → 确认"三段，确认条也画出来了、按 `n` 也正确取消，
但按 `y` 没反应：`Effect::RemoveProvider` **没有分支**，被 `_ => {}` 吞掉，
而 Rust 有 catch-all 时不报错。这与之前 `Effect::CompactSubmit` 是同一个坑
（第五次"声明了没接线"的同型）。教训已经写进纪律：新增 Effect 变体后，
必须确认每个消费侧的 match **真的加了分支**，而不是靠"能编译过"判断。

顺带把确认逻辑里的一个隐患修掉：原先想从**提示文案**里解析服务商名
（"删除服务商 X？（连带其密钥）"→ 切名字）。文案一改就不能删了。
改为名字单独放 `pending_delete` 槽，展示文案与逻辑解耦。

### (e) 分层：宿主不碰配置，靠契据
新增 `ProviderControl` 契据（与 `SessionControl` 同构）：宿主只表达
"我要增删改"，真正的读写与 0600 策略在 `neo-providers` 里实现。
收益立刻可见：宿主测试用 `FakeProviders` 就能覆盖表单与提交逻辑，
不需要文件系统（`provider_control_contract_is_exercised_by_the_fake`）。

### (f) 真机验证
- 表单新增 `gw-test`：密钥在屏幕上显示为 `••••••••`，落盘后
  `providers.json` **不含密钥**、`provider_keys.json` 权限 `0600`。
- 用该服务商跑真实链路（**不设任何环境变量**）：`neo exec --provider gw-test`
  正常启动 → 证明密钥库确实被读到并生效。
- 删除：确认条可见；按 `n` 文件不变；按 `y` 条目与密钥一并删除。

全门禁通过：487 测试、零 warning、架构/协议/会话/配置/SPI/效率守卫全绿。

## 4.43 ESC 按了没反应：`stty raw` 把超时模式复位成阻塞

用户报"提示说 esc 取消，但我测的 q 是退出、esc 没反应"。

实测确认：在候选列表里按 **ESC 什么都不发生**（进程活着、界面不动），
按 **q 才关掉候选**。所以那句提示不是"文案写错"，而是**ESC 真的失效了**。

### 根因在 `read_key_timeout` 里一行看似无害的重置
```
set_stty("min 0 time N");   // 超时模式：读不到就返回
read(&mut b);               // 读到首字节
set_stty("raw -echo");      // ← 这里把 min/time 复位成**阻塞**
decode_first(&b, stdin);    // 为转义序列续读 → 卡在 read 上
```
`stty raw` 会把 `min` 复位成 1（阻塞）。于是 `decode_first` 里
"为转义序列再读几字节"的那一步**永久等待**：

- **方向键没事**：`ESC [ A` 三个字节早已在输入缓冲里，续读立即命中；
- **单独 ESC 卡死**：后面根本没有字节，read 一直等 ——
  界面表现为"按了没反应"，而**下一个按键还会被当成序列的续字节吃掉**。

这就是为什么它藏了这么久：所有多字节序列都正常，只有"孤立的 ESC"失败，
而 ESC 恰好是最常见的"取消"键。修法是删掉那行重置，让 `min 0 time N`
一直生效 —— 单独 ESC 会在 0.1 秒后落到 `[0x1b] => Escape`。

### 验证
pty 驱动真机：修前 ESC「picker 仍在」、q「picker 已关」；
修后 ESC 与 q 都能关掉候选。并逐项确认没回归：
方向键移动、鼠标点击执行（隐藏→显示）、输入框左移插入（abXc）、
设置页/diff/主界面按 ESC 均正确且进程不退出。

### 诚实说明测试的边界
补的单元测试（`bare_escape_resolves_without_blocking`）钉的是
`decode_first` 的契约（ESC 后无字节时必须立刻返回 Escape），
**但它抓不住这次的真实 bug** —— 问题在 `read_key_timeout` 的 stty 调用，
不在 `decode_first` 的逻辑里。单元测试进不到 stty；这次的真正验证是 pty 真机。
把它记下来，是为了避免以后看到绿测试就以为这条路径有保护。

### 顺带
「服务商」为空时不再弹一个"（0 项）"的空候选框（用户截图里那个），
改为提示"还没有服务商，用「新增服务商」添加" —— 空候选框只会让人以为坏了。

## 4.44 "重启后生效"是个逃避问题的说法 —— 改成真热加载

用户问："新增服务商后测试还是 401，重启后生效指的是重启什么？"

这个提问同时暴露了两件事：

### (a) 401 与我的改动无关，但用户无从判断
截图里错误是 `api key: EY__ is invalid` —— 而该 key 以 `EY` 开头，
用户填的是 `sk-...`。这说明**请求根本没走新服务商**：输入框下方
`default ▸ deepseek` 表明当前模型仍是 `deepseek`，走的是内置那条
（带一个无效 key）。用户新增了服务商、以为它自动成了当前模型。
这是"新增了但没切过去"的困惑，而不是新增失败。

### (b) "重启后生效"是我不该写的话
它既没说清重启**什么**（进程？终端？应用？），也把"现在不能用"
变成一个听起来合理却让人无从下手的说法 —— 而且它掩盖了一个**可以修**
的限制：注册表在启动时建好就不可变，所以只能靠重启。

正确做法是**让改动立刻生效**：
- `ModelRegistry::add/remove`（校验与 `new` 同一套：注册名 == 自报名；
  **不允许移除当前在用的那个** —— `current_provider()` 不返回 Option，
  删掉它会让内核立刻无 provider 可用，要先切走）；
- `Kernel::add_model/remove_model` 暴露给宿主；
- `ProviderControl` 契据加 `models()` / `current_model()`，宿主据此
  **立刻刷新模型列表**（否则列表里看不到新服务商，用户以为没生效）；
- 新增/删除后消息改为"已可切换，见「模型」"/"已从模型列表移除"；
  **没填密钥时如实说明**为什么它不在模型列表里（否则会被当成新增失败）。

顺带把构造逻辑抽成 `provider_of()`：启动时注册与设置页热加载共用一段
代码，避免一边规范化 base_url、另一边忘了。

真机验证：新增后模型数 **4 → 5 立即变化**、候选里出现新服务商、
切过去成功且模型行同步 —— **全程没有重启**。

### (c) 要记住的教训
当实现里出现"重启后生效"这类提示时，先问一句：**这个限制是真的吗？**
多数时候它只是"我没做热加载"的委婉说法。把可修的限制写成使用说明，
会让用户背负本不该由他承担的成本。

## 4.45 状态行"一直显示 default" + 非 UTF-8 响应：两个都查清了

用户问："为什么一直显示 default？切换自己的服务商报错
`stream did not contain valid UTF-8`。"

### (a) `default` 是**档位**，不是模型
状态行格式是 `档位 ⏵ 模型`，所以 `default ⏵ deepseek` 里 `default` 是
执行档位（沙箱×审批的档），右边才是模型。这部分不是 bug，是显示格式
没有自解释 —— 值得记的是我一开始也差点把它当成"模型没切过去"。

### (b) 真正的 bug：右边的模型名是**启动快照**
`a.model` 在输入框下方与 footer 两处被使用，而它是 CLI 启动时注入的
快照，切换后从不更新。所以"切换成功但状态行仍显示 deepseek"。
改为 `a.current_model`（实时）。

**这是同一个坑的第三次**（会话 id、设置页模型行、状态行）。
纪律已经在前面写过，这次是没执行到位 —— 说明"记下纪律"不等于"做到"，
后续新增显示项时应逐个确认取值来源，而不是等用户再报一次。

### (c) 非 UTF-8 响应：错误信息本身是坏的
`read_to_string` 一旦遇到非 UTF-8 字节就整体失败，报出
`stream did not contain valid UTF-8` —— 用户既看不到状态码、也看不到
服务商回了什么，**完全无从下手**。真实场景里非 UTF-8 很常见：
网关把错误页返回成 gzip/二进制。

两处修：
1. **请求侧**显式 `Accept-Encoding: identity`（还加了 `Accept: application/json`）。
   有些网关不发这个头就会 gzip 响应体，而响应是按文本解析的。
   在请求侧声明比在客户端解压简单，也少一个失败点。
2. **响应侧**改为按字节读 → 先试严格 UTF-8 → 失败则有损解码 + 打印
   **可安全打印的预览**（控制字符替成 `.`，上限 200 字符，保留换行）。
   连 HTTP 状态行都认不出时，直接说"多半是压缩体或二进制错误页"并给预览 ——
   比一句 invalid UTF-8 有用得多。

顺带把请求组装抽成 `build_request()`，这样 `Accept-Encoding: identity`
这类"不发就出问题、但只在真机才显现"的头可以在单测里断言。
真实 HTTP 帧测试（对真实服务器发一次）仍通过，确认加头没破坏帧格式。

### (d) 教训
**错误信息也是产品的一部分。** `stream did not contain valid UTF-8` 是
库的措辞泄漏到了用户面前；换成"响应不是合法 UTF-8（多半是压缩体或二进制
错误页）+ 预览"之后，同样的故障至少能让人知道下一步查什么。

全门禁通过：490 测试、零 warning、各守卫全绿。

## 4.46 审批：从"输入框变黄回 y"到模态对话框 + 真正的"总是允许"

用户三条反馈合在一起指向一个问题：
"交互逻辑有问题，不是对话框形式，和 opencode/mimo 标准差很远，
而且我输入这么多遍 y 了还一直询问。"

### (a) `Decision::AllowAlways` 声明了但被忽略 —— 第六次"没接线"
协议层早就定义了 `AllowAlways`，但内核里它与 `Allow` **走同一分支**：
```rust
Decision::Allow | Decision::AllowAlways => self.execute_one(&call)?,
```
于是用户答 `y` 之后再答一次 `y`，第三次仍会被问 —— 因为每次调用都重新
过一遍门禁，没有会话级记忆。用户"输入这么多遍 y"正是这个原因。

修：内核加 `granted: BTreeSet<CallKind>`（会话内已放行的**类别**）。
`AllowAlways` 记住类别，之后同类调用在门禁判定处直接放行。
两个刻意的边界：
- 只记**类别**不记具体命令：用户点的是"总是允许这类"，记具体命令
  等于把"放行这一次"伪装成"放行这类"，与用户意图不符。
- **沙箱硬边界不受影响**：会话级放行只在"审批"这一层生效，
  只读档下写入仍被 gate 直接拒（有专门测试钉住）。

配套：`CallKind` 加 `Ord`（放进 BTreeSet 用）。

### (b) 审批必须是**对话框**，不是"输入框变黄 + 打字回 y"
之前审批是把输入框边框变黄、提示 y/n，用户要在对话框外打字 ——
观感与 opencode/mimo 的"弹出确认框、↑↓ 选、回车确认"差很远。
现在做成**居中模态卡片**，按"用户判断需要什么"分三层：
1. 要执行什么（工具 + 参数摘要）—— 没有它就是在盲批；
2. 改动预览（unified diff）—— 决定放不放行的关键；
3. 三个选项（↑↓ 选 / 回车确认）。

### (c) 最有价值的一个发现：快捷键与文案不符
卡片选项写的是 `批准一次（y）`，但我第一版把 `y`/`a`/`n` 实现成
**只移动高亮**、仍需回车确认 —— 于是"按 y"看起来没反应（高亮动了、
没提交）。用户那句"输入这么多遍 y 还一直询问"里，有一部分就是这个：
他按的是 y，程序只当"选中"，没批。

改为 `y`/`a`/`n` **一次按键就生效**（与文案一致），回车仍可确认当前高亮。
并把提交路径抽成 `decide_approval()`，让快捷键与回车走同一条 ——
两处各写一遍必然漂移。

### (d) 验证
- 内核单测：`allow_always_stops_re_prompting_for_the_same_kind`
  （三个写调用，答一次 a 全部执行、不再问）；
  `allow_once_still_re_prompts_for_the_next_call`（对照组：y 必须继续问，
  否则"总是允许"与"批准一次"无区别，等于静默放行）；
  `allow_always_never_bypasses_the_sandbox_hard_boundary`（只读档仍拒）。
- 渲染单测：模态卡片有标题、有问题描述（批的是什么）、三个选项、选中标记。
- 真机（pty，selftest 触发真实审批）：卡片显示"要执行：apply_patch
  selftest.txt" + diff 预览 + 三个选项；按 `a`/`y` 文件被写入、
  `✓ apply_patch exit 0`；按 `n` 不写入。

全门禁通过：494 测试、零 warning、各守卫全绿。

## 4.47 视觉层级：底色层是"面"，边框只是"线"

用户："感觉做的太简陋了，没有层级边界感，要完全对齐 mimo code / opencode"。

### (a) 根因是渲染基础设施只有**前景色**
`Grid` 每格只有 `ch` + `tone`，而 `tone` 全部映射成**前景** SGR
（38;2 / 38;5 / 30–37）。也就是说整个界面**画不出"一块有底色的面"** ——
只能画线。而 opencode/mimo 的层级感主要来自**色块**（侧栏底、卡片底、
选中条），不是更多边框。所以再怎么调边框也到不了那种观感。

修：给 `Grid` 加**背景层** `bg: Vec<Option<Bg>>`，与前景正交
（同一底色上可以放 Text/Warning/Muted 各种前景；合进 tone 会组合爆炸）。
`Pal::bg()` 按 TrueColor/256/16 三档降级；`Grid::fill_bg()` 铺矩形；
`lines()` 在前景或背景任一变化时重发 SGR。主题加三档层次：
`bg_panel`（侧栏/设置页底）→ `bg_surface`（模态卡片）→ `bg_selected`（选中条），
七个主题各自配好。

### (b) 审批卡片的高度算错了
原来 `h = body.len() + choices + 4`，多留 4 行：卡片底部挂着一块
**没有左右边框**的空白（用户截图里那块空洞）。改为**由实际内容算高度**，
并让每一行（含内容行）都补左右边框 —— "面"必须闭合。

### (c) 三处同时喊审批
模态卡片 + 输入框变黄 + 状态行"待审批…"三处重复提示，用户反而不知道
在哪答。改为：模态在场时输入框**不抢焦点**（不变黄、不显示 y/n 前缀），
状态行也不重复审批文案 —— 卡片是唯一入口。

### (d) 选中态从"一个箭头"改成"一条底"
列表选中只加 `▸` 太弱（用户说的"简单划线"）。设置页行与审批选项的
选中态现在都铺 `bg_selected` 底色条 —— 与 opencode 的列表高亮一致。

### (e) 验证
真机 pty 抓到**三种不同的背景色序列**（`32;30;42` 面板、`42;39;56` 表面、
`51;47;69` 选中），确认底色真的输出到终端，不是只在代码里存在。
卡片结构：`╭─ 需要审批 ─╮` + 要执行 + 说明 + 分隔线 + 三个选项 + `╰──╯`，
每行左右边框闭合。

全门禁通过：494 测试、零 warning。

## 4.48 "回车像卡死"：整轮同步执行 —— 界面在等网络

用户："选择完回车没任何反应好像卡死了，退出后才显示一堆内容。"

### (a) 根因：`submit(UserTurn)` 在一次调用里跑完**整轮**
`drive_steps()` 是个循环：请求模型 → 执行工具 → 再请求模型 → …直到模型
不再要工具。全部在一次 `submit` 里完成，宿主**只能等它返回**才能拿到事件、
才能重绘。一轮若多次网络往返，界面就冻在那里几十秒；退出时那"一堆内容"
其实是早已生成、只是没机会画出来的事件。

### (b) 拆成可逐帧推进
- `step_once()`：推进一步（一次模型请求 + 它要求的工具执行），
  返回 `More / Suspended / Done`；`drive_steps()` 变成"循环调它 + 收尾"。
- 协议加 `Op::BeginTurn`（只开场：回显/解析引用/入历史，不驱动）
  与 `Op::Pump`（推进一步）。语义与 `UserTurn` 不重叠：
  一个"跑完整轮"，一个"我只推一步，自己控制节奏"。
- TUI 改为 `BeginTurn` + `Pump` 循环，**每步之后重绘一次**
  （显示"运行中…"），到 `TurnComplete` 或审批请求为止。

### (c) 实测
pty 驱动，提交后测**首次输出延迟**：改前要等整轮结束（数秒到数十秒），
改后 **0.01 秒**就有输出。这是"不冻结"的硬证据，不是主观判断。

### (d) 关于截图里右侧那些小方块
排查后确认是**星场背景**（`stars` 纹理的点阵），不是渲染错误 ——
它在宽终端上本就会在某些行出现。与本次卡死无关，但值得说明：
用户把两件事一起报上来时，先分清哪个是 bug、哪个是设计，避免一起乱改。

全门禁通过：494 测试、零 warning。

## 4.49 两个问题：推理链路断了，批准后界面再次冻结

用户报两件事：没有思考过程显示；选择"总是允许"后回车终端退出。

### (a) 推理：不是显示问题，是**第一环就不存在**
协议层有 `EventMsg::ReasoningDelta`、有 `Fact::AssistantThought`，
TUI 早就实现了"思考过程"渲染与 `/thinking` 开关 —— 但
**`ModelDelta` 里根本没有推理变体**。provider 没有任何途径把推理报上来，
整条链路是断的。用户看到的是"永远没有思考过程"。

修：`ModelDelta::Reasoning`（第一环）+ provider 解析 DeepSeek 的
`reasoning_content`（顺序在正文之前，与模型产出顺序一致）+ 内核转成
`ReasoningDelta` 事件并落盘（它同样是模型可见内容）。
顺带让 `demo` 桩也带一段推理 —— 否则这条链路**离线不可验**
（真实模型才有 reasoning_content，测试里永远覆盖不到）。

实测：默认隐藏；`/thinking` 开启后出现推理文字。

### (b) 批准后"退出"：批准把整轮又跑了一遍 → 再次冻结
`Approve` 与 `UserTurn` 是同一个毛病：`finish_step_from` 里会
`drive_steps()`，**在一次 submit 里把整轮剩下的模型往返全跑完**。
于是批准之后界面又冻住几十秒；用户在冻结期间敲的键（回车、q…）
堆在缓冲区里，解冻后被**逐个**处理 —— 其中任何一个被当成退出/提交
就会"终端直接退出"。这是上一轮只修了 UserTurn、**漏了 Approve** 的后果。

修：
- 协议加 `Op::ApproveStep`（只执行本步剩余调用，**不驱动**后续），
  与 `Approve` 语义区分；内核抽出 `resolve_approval(id, decision, drive)`。
- TUI 审批改用 `ApproveStep`，随后自己 `pump_until_boundary()` 逐步推进。
- 把"推进 + 每步重绘"抽成 `pump_until_boundary()`，主提交路径与审批恢复
  **共用同一段** —— 两处各写一遍正是这次漏掉审批路径的原因。

顺带：审批模态在场时，回车是"确认当前选项"，不再同时是"提交空输入" ——
消除了一个误触退出的入口。

### (c) 教训
上一轮修"卡死"时我只改了 `UserTurn` 一条路径，没问"还有哪些路径会
一次跑完整轮"。`Approve` 就是其中之一。**修一类问题时，先把同类路径列全**，
否则用户会在下一条路径上再撞一次 —— 这次就是这样。

全门禁通过：495 测试、零 warning。

## 4.50 三个反馈：对话消失、思考设置不持久、步数预算太低

### (a) 批准后对话消失、只剩"运行中…"
我上一轮加的 `pump_until_boundary()` 逐帧重绘时写了 `facts: &[]` +
`about: Some(...)` —— 而渲染器的逻辑是"**没有任何事实**且有 about 时
显示欢迎首屏"。于是每推进一步，界面就退回欢迎屏：用户的对话凭空消失，
只剩一个"运行中…"（真实截图）。修：传 `facts_of(events)` 的真实转录。
**教训**：构造渲染输入时，"空数组"在这套 API 里不是中性的 ——
它携带"还没有任何事实"这个语义。传空必须是有意的。

### (b) 思考过程设置重启后失效
`ToolDisplay`（展开工具输出 / 显示推理）只在内存里，重启回到默认（都隐藏）。
主题与背景都持久化了，这两个没有 —— 不一致，用户自然觉得"又要重设"。
修：新增 `~/.neo/display` 偏好文件（与外观分开存，语义不同）。
**落盘点放在主循环顶部比对 `display != display_saved`**，而不是在 5 个切换点
各写一次 save —— 循环顶部不可能被 `continue` 跳过，从结构上杜绝"漏存一处"。

### (c) "你是谁"竟然弹工具审批
系统提示词只有"优先用工具核验事实，不要凭记忆断言"。这句对**需要核验仓库
事实**的任务是对的，但模型会把它推广到一切 —— 问身份也先跑工具探查，
于是弹出审批要用户授权。
修：抽出 `base_system_prompt()`，同时说清**什么时候该用工具**和
**什么时候不该**（问候/身份/概念解释直接用已有信息回答）。
**只写单向偏好会让模型过度使用** —— 提示词里的"优先"没有反例就会变成"总是"。

### (d) "了解下这个项目"撞上"超出步数预算（16 步）"
16 太低了：一次真实的项目探索就能用完，表现为干到一半被硬停。
`max_steps` 是**安全阀**（防死循环）而非工作限额，调到 64；
报错文案也改成可操作的（说明已完成的工作仍在、可继续发消息、或用
`--max-steps` 提高），而不是只丢一句"超出预算"。

### (e) 一个需要用户帮忙确认的点
截图 2 里模型在推理中讨论"neo-instructions 两个测试在本环境无法通过
（tmp 目录必然落在 git 仓库内）"—— 那是**真实模型在真实工作**的输出，
说明推理与工具循环都跑通了。至于那两个测试是否真受环境影响，
属于另一个独立问题，需要单独复核（不该和这次三个反馈混在一起改）。

全门禁通过：495 测试、零 warning。

## 4.51 停止打地鼠：拉 opencode 源码，先把范式写成可核对的规格

用户一句很关键的话："我感觉现在的做法是打地鼠，能直接对照 opencode 和
mimo code 的交互范式对齐吗？这样我一个个测试太耽误时间了。"

**他说得对，而且这句诊断比任何一个具体 bug 都重要。** 前面几轮我都在
"用户报一个、我修一个"，根因不是不努力，而是**没有一份写下来的范式**：
每改一处都在凭"我觉得像"判断，于是永远对不齐。

### (a) 先把源码拿到手，而不是靠记忆猜
GitHub 起初连不上（HTTP2 framing error）。排查发现本机代理在 7897 端口
（`curl -x http://127.0.0.1:7897` 通），于是用项目自带的
`docs/ai-efficiency-rules/scripts/cache_fetch.sh` 拉了 `sst/opencode` 的
`packages/tui`（浅层 + 稀疏）。**先拿事实，再动代码** —— 这一步花的几分钟
换掉了后面无数轮猜测。
（MiMo 是 99MB 编译产物、无源码，只能 pty 抓帧对比；opencode 有完整 TS 源码。）

### (b) 写下来才发现真正的差距
`docs/opencode-parity.md` 从源码提取了范式，逐条对照出 11 项差距。
其中最关键、也是最颠覆我原先判断的一条：

**层级来自"底色阶梯 + 单边条"，不是"更多边框"。**
opencode 的底色是一条精确亮度阶梯（`#0a0a0a` step1 → `#141414` step2 →
`#1e1e1e` step3），面板只有**左侧一条 `┃`**（`ui/border.ts` 的
`SplitBorder`），根本没有 `╭╮╰╯` 四角框。我此前一直在四角框的粗细、
对齐上打磨 —— 方向本身错了。

其它从源码读到、此前完全不知道的具体约定：
- 模态有**遮罩**（`RGBA(0,0,0,150)`），垂直位置是**上 1/4**（非居中）。
- 对话框有**固定宽度档**（60/88/116），不是"屏宽减几个"。
- 选项是**横向药丸**，选中项**整块填充背景**；键位提示在底部右侧。
- `←/→` 选择（不是上下）。
- **esc 恒等于最后一个选项**（权限场景 = reject），从不"什么都不做"。
- 权限标题**按类型给图标与措辞**（`→ Edit <path>`、`# Shell command` …）。
- `always` 是**第二段确认**并列出将放行的具体范围，语义是"直到重启"。

### (c) 本轮改了什么（P1–P7、P10、P11）
- 底色阶梯补成五档：backdrop / panel / element / menu / selected
  （真机验证输出 `10;10;12` → `32;30;42` → `38;35;48` → `42;39;54` → `51;47;69`）。
- 审批面板改为 **底色 + 左 `┃`**，去掉四角框；选项改横向药丸、选中整块填充；
  底部独立 element 选项条 + 右侧键位提示。
- 标题按类型化：`△ 需要审批` + `→ 编辑 <path>` / `# Shell 命令` + `$ <cmd>`。
- esc = 拒绝；`←/→`/`h/l` 选择。
- 顺手删掉两个真问题：`ApprovalPrompt.call` 字段（渲染从不使用，死字段）、
  以及内核原始 detail（"写入类调用需确认"）与类型化标题重复的噪音。

### (d) 还没做（写进 parity 清单，不再靠用户撞）
- **P8**：`always` 的二次确认 + 范围列表（当前直接生效，用户不知道授权了什么）。
- **P9**：拒绝时带理由（需协议加 message 字段）。
- 设置页的左侧竖排分类（opencode 的结构性差异，用户提过多次）。

### (e) 防止再打地鼠的机制
parity 文档末尾立了一条**纪律**：新界面必须复用原语（面板/选项条/对话框），
**不得再手画四角框**。因为"每个页面各长一副样子"正是打地鼠的温床 ——
统一到原语上，下一个页面才不会又冒出一批新问题。

全门禁通过：495 测试、零 warning。

## 4.52 "突然退出会话"与"好吓人的花纹"：都查清了

### (a) "好吓人的花纹"是我引入的**真 bug**，不是终端问题
截图里满屏深色小方块。查 `lines()` 的 SGR 发射顺序发现：
`tone(Tone::None)` 返回的是 **reset**，而 reset 会**连同背景一起清掉**。
背景层是我上一轮刚加的，于是空格格子的流程变成
"设背景 → 立刻 reset 抹掉"，**只有星场那些格子留着深色底** ——
每个星点被深底衬成一个方块，整屏就是那个花纹。

修：新增 `fg_sgr()`，`Tone::None` 返回**空串**（"不设前景"）而不是 reset；
reset 统一由 `lines()` 在切换时发。并补回归测试
（`background_survives_tone_none_cells`）。

顺带：模态在场时**不再铺装饰性星场**，改铺与 opencode 一致的平铺遮罩 ——
星场留在底下会让遮罩变成一格格的方块。帧大小也从 16KB 降到 13KB。

### (b) "突然退出会话"：ctrl+c 的语义分层错了
从 opencode 源码查到关键事实：`app.exit` 默认绑 `ctrl+c/ctrl+d/<leader>q`
（`config/keybind.ts:48`），但在**权限对话框内被重绑定为 Reject**
（`permission.tsx` 里 `name:"app.exit"` → `onSelect(escapeKey)`）。即：

| 场景 | ctrl+c |
|---|---|
| 对话框打开 | **拒绝** |
| 运行中 | 中断本轮 |
| 空闲 | 退出 |

我们此前**一律直接退出**（每个分支都是 `Key::Quit => break`）。用户在审批框上
按 ctrl+c 想取消，结果整个会话没了 —— 正是他说的"突然退出会话"。
三处都改了：模态内 = 拒绝；`pump_until_boundary` 里轮询到 ctrl+c = `Op::Interrupt`；
空闲才退出。

**顺带修掉一个更隐蔽的**：运行中被打断前，用户情急连按的键会**攒在缓冲区**里，
回合结束后被逐个处理 —— 于是"退出后还继续动作/残留输入"。
`poll_interrupt` 明确只取中断信号、**其余按键一律丢弃**，不留后患。

### (c) 教训（第三次同类）
"用户在冻结/等待期间敲的键会在之后被处理"这个模式，我已经在上一轮
（Approve 冻结）遇过一次，这次又出现在 ctrl+c 上。
**规律**：只要有一段时间不读输入，就必须明确"这段时间的输入怎么处理"（丢弃？中断？），
不能默认"攒着"—— 攒着的键会在状态变化后被误解释。

## 4.53 参考源码不在仓库里：换机器必须重拉（`scripts/fetch-refs.sh`）

`docs/opencode-parity.md` 是照着 **opencode 真实源码**写的，所有引用都指向
`.cache/ai-external/github/opencode-dev/packages/tui/...`。而 `.cache/` 在
`.gitignore` 里（外部资源缓存不入库）—— 于是**换一台电脑，那份规格就查不到出处了**：
文档里的路径全指向不存在的东西。这不是小事，它会让"对齐"重新退化成
凭印象猜（正是打地鼠的成因）。

固化成一条命令：`scripts/fetch-refs.sh`
- `bash scripts/fetch-refs.sh` 拉全部参考项目
- `bash scripts/fetch-refs.sh --list` 只看缓存状态
- 自动探代理：先看 `$PROXY` 与环境变量，再并发探测常见本地端口
  （7897 打头，其次 7890/7891/1080/…），命中才用

### 注意事项（新机器 / 新网络）

1. **参考源码不入库**（`.cache/` 已被忽略），新电脑必须重跑本脚本；
   不跑则 `docs/opencode-parity.md` 的路径全部失效。
2. **GitHub 直连可能失败**（`Error in the HTTP2 framing layer` / 连接超时，
   git 不读系统代理）。设代理再跑：
   `PROXY=http://127.0.0.1:7897 bash scripts/fetch-refs.sh`
   （本机实测 7897 可用；脚本也会自动探。）
3. **MiMo Code 没有可用源码**：npm 包 `@mimo-ai/cli` 里是 99MB 编译产物
   (`bin/.mimocode`)，**没有 `packages/` 目录**。要对照只能：
   - `npm i -g @mimo-ai/cli`（本机已有 `/Users/kags/.npm-global/bin/mimo`）
   - 然后用 pty 抓帧比对观感（做法见 4.51）
   - **不要**去 clone `XiaomiMiMo/MiMo-Code` 找 TUI 源码 —— 那是另一个仓，
     与 npm 包内容不对应，白费时间。
4. **opencode 的分支是 `dev`**（不是 main）；只稀疏拉 `packages/tui`
   （省时间与体积）。要看别处再加 `--subdir`。
5. `cache_fetch.sh` 在 macOS/BSD `sed` 下会打印一行
   `RE error: repetition-operator operand invalid` —— 那是它推导默认缓存名时
   的报错，**不影响克隆**；`fetch-refs.sh` 已显式传 `--name` 规避。

## 4.54 MCP 客户端：外部工具从"接不进来"到复用整条内核链路

协议层早就留了扩展位，这轮把它接上了：`neo-mcp`（L3）实现 MCP stdio 传输
（JSON-RPC 2.0，**换行分隔**的消息），把外部服务器声明的工具变成
`ToolRegistry` 里的普通 `Tool`。

### (a) 为什么 MCP **不是**第 8 个 SPI
工具的 seam 早就是 `Tool` —— MCP 只是这个 seam 的**另一个实现来源**
（如同 `neo-capability` 提供内置工具）。再立一个"MCP 后端"SPI 会把同一个
事实记两遍，账本必然漂移。接入后外部工具自动获得：提示词注入、语义分类、
审批闸门、输出上限、会话落盘 —— **没有为新协议开任何特殊通道**。

### (b) 安全设计：配置与密钥同一套纪律
- **项目级 `mcp.json` 显式拒绝**（不是静默忽略）：仓库可以指定的可执行
  文件就是供应链注入 —— `git clone` 后跑一次就执行了任意程序。这与
  项目级 `providers.json` 的拒绝（C5）是同一条原则：静默忽略会让用户
  对着"配了却不生效"想不通，报错要说清威胁与正确位置。
- **`call_kind` 只信服务器声明的 `readOnlyHint`**：没有 hint 一律按
  Write（保守判写 —— 把写误判成读会绕过审批，与 bash 分类的代价不对称
  是同一条）。
- `execute` **不走 `ctx.exec` 也没有 `Command::new`**：工具执行是向
  用户配置的服务器进程 stdin 写 JSON-RPC。沙箱管辖"工具发起的本地命令"，
  MCP 服务器是装配时的集成对象（与模型 provider 的 HTTP 端点同类）。
  这条边界写在 crate 文档里，防止后人"顺手"给它加执行入口。

### (c) 有界性是客户端的义务
握手/列工具/调用**各自独立超时**；超时后连接标记为**已污染**（迟到的
响应无法与后续请求配对，继续用只会张冠李戴），之后所有调用立即失败。
stderr 用 4 KiB 环形缓冲收尾（服务器刷屏不会吃穿内存）；stdout 读线程
经有界通道（64 行）搬运并**持续排空** —— 不排空的话管道写满后服务器
阻塞在 stderr 上，就是沙箱真机测试抓过的同类死锁（4.7 #1）。分页循环
上限 100 页 —— 无退出条件的循环是缺陷。

### (d) 验证：桩 + 真进程双层
23 个桩测试覆盖握手/分页/超时/错误映射/名字收敛（`mcp__<server>__<tool>`，
非法字符收敛为下划线）；2 个真进程测试用 `/bin/sh` 起真实服务器验证
线格式与字节边界 —— **桩测不出缓冲与管道语义**，这与沙箱必须真机验证
是同一条纪律。装配接线（`register_mcp_tools`）抽成可测函数：配置路径与
工作区都是参数，测试不碰进程级 `NEO_HOME`（4.13d：并行测试会串环境）。

### (e) 装配取舍与诚实缺口
一台服务器起不来不拖垮应用：告警后继续，缺哪些工具在模型侧可见
（工具列表就是事实）。服务器在装配时启动，改 `mcp.json` 需重启
（与技能同一取舍）。资源（resources）与提示模板（prompts）未接入，
SSE 传输未做 —— 都进了诚实清单。

## 4.55 Goal 编排接线：机制在内核、策略在 L4、回放即重建

`GoalEngine` 状态机（四阶段 + 停止条件）写好很久了，但 `Op::GoalSet` 在内核
一直落到"未实现"。这次接完，M4 的核心验收"kill 进程后重启能续跑"成立。

### (a) 与 Compactor 同一条分界线
内核（L2）定义 `GoalOrchestrator` 契据并只提供**机制**：Goal 系列事件落日志、
子任务轮就是普通 turn（沙箱/审批/上限/落盘全复用，不另开执行通道）、轮次
结束时把控制权交回编排器。拆解/阶段提示词/重试/判停全是 **L4 策略**
（`EngineGoalOrchestrator` 包装既有 `GoalEngine`，零重新实现 —— 两套状态机
必然漂移）。TUI 的泵循环在 TurnComplete 后检查快照，有剩余就提交
`Op::GoalAdvance` 继续泵 —— 一次推进一个完整子任务轮，宿主始终控节奏，
不会回到"一次调用跑完整轮"的冻结老路。

### (b) 回放即重建（kill 续跑的关键）
编排状态不存内存旁路 —— 每次变化发**完整快照**事件（`GoalUpdated`）落日志；
`rebuild_from_log` 把每条事件喂给编排器的 `observe`，取最后一条快照即恢复。
两个由此而来的纪律：快照必须**字段完备**（`consecutive_failures` 就是发现
回放后判停行为会不一致才补进协议的）；清除不是"没有快照"而是显式的
`GoalCleared` 事件 —— 让宿主拿最后一条快照当实时值，就会在清除后
永远显示旧目标（4.38d/4.45b 同一个坑的 Goal 版）。

### (c) 分两步走的审查判定
第一版是确定性的失败代理：轮内出现 Error 事件或非零退出的工具调用 = 失败
→ 回退 Code 重做（≤ max_review_retries 次）。随后补上**模型自评**：
审查轮答复含显式否定词（「审查未通过」等，词汇表与审查提示词同模块成对）
→ 回退重做；显式肯定或沉默 → 结合硬失败信号判定。沉默不判失败是刻意的
不对称 —— 否则含糊的模型会把重试预算烧光，离线桩（固定答复）也永远过不了
审查。挂钟停止条件同理：挂钟会破坏"同一 Op 序列产出同一事件序列"，
四项停止条件生效、一项如实标注未实现。

### (d) pty 冒烟抓到的显示 bug
子任务头显示"子任务 0/2"—— 用了 vec 的 0 基下标当序号。子任务有自己的
1 基 id，显示层必须用它。这条被冒烟脚本的输出断言抓住（单测里没测显示
文案），记下：**用户可见的编号也是契约**。

## 4.56 真实模型回归（智谱 BigModel / glm-4.6）

用测试 key 走通了服务商注册表路径（`providers.json` + 0600 密钥库）——
密钥解析"先密钥库后环境变量"的顺序在真实配置下成立。三项真机验证：

1. **简单对话**：真实回复 + 真实 token 计数（682 in / 15 out）。
2. **工具循环**：模型自主调用 apply_patch，真机落盘，内容逐字符正确。
3. **目标模式（最有价值的一次）**：四阶段完整跑完，其中**审查阶段
   真实触发了一次回退** —— 审查轮里模型的一个工具调用失败（exit -1），
   硬失败代理判定"不通过"→ 回退 Code 重做 → 第二次审查通过 → 复盘 →
   全部完成。全程 8 轮、7 份快照落日志，有界重试吸收了保守判据的
   误伤 —— 设计取舍（宁保守、有上限）在真实对端上被验证而不是
   只在纸面上成立。

### 真机暴露的一个值得记录的边界

审查阶段的硬失败代理比"模型自评"更敏感：模型在审查时用工具核验，
**任何一次非零退出**（哪怕是无害的探测失败）都会把整轮审查判为失败。
真实运行里这表现为一次额外的 Code→Review 循环（预算多花约 40%）。
这不是 bug 而是刻意的保守 —— 但如果未来要省预算，方向是：把
"探测类只读调用的失败"与"修复类写入调用的失败"分开记账。
（**已按此方向改进**：审查轮内只读调用的失败不再判死整轮 ——
写入/网络失败仍是硬信号；两条路径各有内核测试钉住。

### 4.57 桌面窗口交互验证:一个 .app 包引发的工具链课

授权屏幕录制后,用桌面控制对 `neo desktop` 做了真机交互验证,全部通过:
真实点击任务框 + 键入 + 回车 → 任务提交、转录渲染;HTTP API 设定目标 →
页面自动推进四阶段 → 全部完成,**进度在原生窗口里实时渲染**(AX 树里
能读到每一行 🎯 进度与阶段提示词);点关闭钮 → 进程退出、HTTP 服务停止
(关窗即退出)。

三条值得记的:

1. **裸二进制在 macOS 桌面控制生态里是二等公民**:没有 bundle 身份
   (Info.plist),无障碍树不可读、辅助工具无法锚定窗口。包一个最小
   .app(Contents/MacOS + Info.plist 声明 CFBundleIdentifier)后一切
   打通 —— 这本来就是 macOS 桌面应用的正确发布形态,不是为测试而
   妥协。正式打包(图标/签名/公证)进诚实清单。
2. **webview 的合成聚焦键入不可靠**:type 经"合成聚焦"投递,焦点会
   回到上一个 AX 焦点元素而不是视觉光标所在 —— 文本反复落进任务框。
   可靠路径是"点击 → get_app_state 读 focused 标志确认 → 再投递",
   或者像 goal 验证那样走 HTTP API 驱动、窗口只做渲染验证。
3. **AX 树是懒加载的**:首次交互前 webview 不暴露网页元素;交互一次
   之后 textarea/按钮全部出现 —— 验证脚本要先动一下再断言。

### 依赖与工具链钉版(桌面窗口层的配套)

rustc 1.83 下为解析 wry/tao 的依赖树,把 5 个传递依赖钉回了兼容版本
(idna_adapter 1.1.0 / hashbrown 0.15.5 / indexmap 2.7.1 /
num_enum 0.7.3 / dlopen2_derive 0.4.1)。配套把工具链钉在
`rust-toolchain.toml`(1.83.0)—— 否则一次 `cargo update` 会把树
漂移到 edition2024 版本,本工具链无法解析,报错还很难看懂。
升级 rustc 时:先升工具链,再让依赖树自然升级,两步分开走。

### TUI × 真实模型 Goal 冒烟(与一个顺带的正面发现)

TUI 里设定真实目标(智谱)—— 真实网络延迟下泵驱动逐阶段推进:
计划 → 执行 → 审查 → 复盘 → 全部完成,文件落盘,子任务序号正确
(1/1)。脚本等待预算(240s)小于真实模型四阶段实际耗时(思考模式
每轮 30-60s+),超时误报了一次,但会话日志证明目标完整收敛。

**顺带发现**:TUI 的会话文件按工作区共享(s-<id>.jsonl 由 cwd 派生),
同 cwd 重启 TUI 续写同一会话 —— 这正是 M4"kill 进程后重启能续跑"
验收在 TUI 侧的自然表现:今天所有 TUI 启动(审批回归、P9、Goal)
的记录都在同一份日志里,重启后历史完整可重建。附带提醒:同 cwd
**并发**两个 TUI 会写同一文件(append-only 交错),schema 校验的
seq 严格递增可能被破坏 —— 当前单用户单窗口场景下不构成问题,
多窗口并发是将来要做文件锁时的课题。

### 真机重度试用暴露的 harness 缺陷:工具没有参数 schema

给 glm-4.6 一个真实的编码任务(修除零 bug + 补测试 + 跑测试),模型
把编码任务做对了,但 **todowrite 连续十几次调用全部失败**:模型把
整个数组 JSON 序列化成字符串传入(`"items": "[{...}]"`,双重编码)。

根因在 harness:发给 provider 的 function 定义里 `parameters` 是
**硬编码的空 schema** —— 模型拿到零结构信息,只能靠散文描述猜参数
形状。mock 测不出这个(桩不关心 schema),只有真实对端能暴露。

修法(三层):
1. `Tool` 契约新增 `parameters() -> Value`(JSON Schema),四个内置
   工具各自声明精确形状;MCP 工具直接透传服务器声明的 `input_schema`
   —— 同构,一字不改;
2. provider 把真 schema 发给模型(替换硬编码空对象);
3. todowrite **宽容字符串化**:items 是能解析成数组的字符串就接受
   —— 对端是概率系统,输入规范化是 courtesy。顺带修正语义:
   **空数组 = 合法**(整表替换成空 = 清空清单),不再当错误。

复测确认:带 schema 后模型发的是真数组,主任务里 todowrite 全部
成功。

**更正一个我自己的错误判断**:此前认为模型"编造 Python 2.7 理由"。
真机日志证明模型**真的运行过 `python --version` 得到 2.7.18**
(macOS 系统残留的 Python 2),随后添加编码声明与 division 导入都是
对证据的合理适配 —— 它不是幻觉,是模型有据可依的行为。我当时的
判断只看了表面。教训:判定"模型编造"之前,先查它手里有什么证据。

### MCP prompts 的设计决定(不接,记录触发条件)

MCP prompts 是服务器提供的参数化消息模板,语义是"用户挑选并填参后
注入对话"。NEO 里**不接**,理由:

1. 与 `$skill` 高度同构 —— 已有的本地机制就是"用户挑模板 → 注入
   上下文";MCP prompts 只是把模板源换到远程服务器。核心使用场景
   已被覆盖。
2. 接入的真实成本在 UI:斜杠命令表是静态 const 表,动态合并服务端
   prompts + 参数表单 + 新协议 Op(内核转发 get_prompt)约 400+ 行,
   其中绝大部分是交互界面 —— 而 prompts 在 MCP 生态的使用频率低
   (大多数服务器只有 tools/resources)。
3. 触发条件(何时回头接):出现真实的"用户需要调用某个 MCP 服务器
   prompts"的需求时,先在 issue 里对齐交互设计(动态命令 vs 子命令
   vs 表单),再动手 —— 不闷头写。

### CI Linux 测试失败:Notify 不可用分支的契约违反(已修)

Rust CI 首跑在 ubuntu 上失败于 notify conformance:`SystemNotify::notify`
在 `!available()` 时返回 Err("无可用提醒后端"),而契约要求**两种情况
都返回 Ok** —— 不可用时报错会打断主流程(提醒常在 CI/容器/SSH 里被
调用,那里没有 notify-send 是常态)。macOS 上 osascript 恒存在,
本地永远测不到这个分支 —— 又一次"只有真实环境能暴露"。

修法:不可用时安静返回 Ok(与 NoopNotify 同语义)。顺带更正 4.31
的一处记录:conformance 的语义是"自称可用就必须成功,**不可用也
必须 Ok**"(提醒的失败永远不打扰主流程),不是"不可用就必须 Err"。
剪贴板才是不对称的那一半(不可用 → Err,用户操作没生效必须报)。

**修复后发现还有一个同款钉子**:lib 内嵌单元测试
`system_notify_reports_unavailable_platforms_instead_of_pretending`
钉着旧语义(不可用 → unwrap_err)。它在 macOS 上走 available 分支,
**条件分支让本地永远测不到它** —— 这类"平台条件门控的旧断言"
只有跨平台 CI 能抓。已改为与契约一致的静默降级断言。
**CI 复跑全绿(verify + audit 双通过)—— Linux 测试失败排查闭环。**

### Web / Desktop × 真实模型回归（补完四宿主覆盖）

此前 Web/Desktop 只用 mock 验证过交互。补上真实模型（智谱）：

- **Web**：任务经 API 提交，SSE 完整投递真实答复（"Rust 的所有权
  通过编译时规则确保…" —— 质量在线）；顺带 glm-4.6 返回了
  `reasoning_delta`，推理链路在第三个真实对端上成立；审批往返：
  apply_patch → patch_proposed → 拒绝 → `tool_call_end` exit -1 →
  模型答复"看起来调用被拒绝了" —— 模型随后重试写入，闸门再次
  拦截并等待审批（系统安全，文件未写入）。**新 detail 生效**：
  "写入类调用需确认:apply_patch 测试写入"。
- **Desktop**：任务经 API 提交，原生窗口实时渲染真实答复与
  token 计数（914 in / 169 out）。（模型把"NEO"答成了知名区块链
  项目 —— 它不了解本项目命名，预期内；验证目标是管道。）
- 测试 key 预算注意：一次真机 Goal 曾消耗约 15 万 token
  （budget_used），四阶段 × 思考模式很贵，真机 Goal 回归要节制。

**操作纪律再应验**：二进制未重建就跑 e2e → 白跑（4.26 教训第二次
应验）；桌面窗口的任务必须在 webview 的 SSE 连上**之后**提交
（事件流不重放，先到先得不存在）。

### TUI 交互式审批 × 真实模型（P9 契约成立）

pty 驱动真实模型（智谱）走完交互审批:模态 → `n` → 理由段输入
"用户明确要求先不写入任何文件" → 拒绝落空（✗ exit -1、文件不存在）→
追问模型"刚才写入成功了吗、为什么" —— 模型答复**"没有成功，因为
用户明确要求先不写入任何文件"**。理由从输入段 → 内核 → 模型可见
工具结果 → 模型上下文完整流通。这正是 4.28b 的对照实验补完:
没有理由时模型说"工具返回空"并原样重试;有理由时模型精确复述并
停手。测试 key 已留在 `~/.neo/provider_keys.json`（0600），不需要时
删除该文件即可。

## 4.58 真流式全链路：伪流式的诊断与三层修复

用户反馈:"打开思考过程提问,右下角一直'运行中',思考全部完成后内容一次性出现"。诊断发现**三层串联**的问题,单独修任何一层都不够:

### (a) provider 是伪流式(第一环就断)

`DeepSeekProvider::stream()` 阻塞等完整响应体再一次性解析——而且**智谱也走这段代码**:`neo-cli` 的 `provider_of()` 把注册表里每个供应商都构造成同一个实现,换 endpoint/model 而已。修一处全修。改法:`stream:true` + `stream_options.include_usage`,手写 TLS 管道逐字节增量解 chunked 帧 + SSE 事件,`data:[DONE]` 收尾,工具调用分片按 index 聚合。**兼容兜底**:网关无视流式标志回整段 JSON(响应体以 `{` 开头)时退回 `parse_completion`——失去实时性但不出错。断流/空响应显式报错,不把半截回答当完整答案。

### (b) 内核 Pump 粒度是"整步"(第二层)

provider 流式后,内核仍在一步内消费完整条流才把 outbox 交给宿主。修法:在飞流存 `Kernel`(跨 Op::Pump 存活),一次 Pump 消费一个时间片(80ms / 256 条上限),事件即到即发。**确定性不受影响**:事件序列只由 provider 产出顺序决定,时间片只是同一序列的分段方式(T2 断言的是序列不是分段)。流式进行中只接受 Pump/Interrupt,其余 op 报 `TurnInFlight`。

### (c) TUI 主线程被 submit 阻塞(第三层)

泵循环阻塞在 submit(Pump) 里时无法重绘——工具执行(几十秒)与首字延迟(1-3 秒)期间 spinner 静止。修法:**内核挪到驱动线程**(`Kernel: Send` 由 Web 宿主既有代码证明,零锁),TUI 的 Pump 变成"发 op + 最多等 80ms 收事件批",批没到返回空批照常重绘。**越界 Pump 防护**:长步执行期间排队的 Pump,落地时若本轮已到边界直接丢弃且**不回批**(回了会污染通道)——否则会对空闲内核多余起一步真实请求。**审批应答(Approve/ApproveStep)必须把 driving 重新置真**:ApprovalRequest 置假后没人恢复,批准后所有 Pump 被当越界丢弃,轮次卡死在"运行中"(驱动线程回归测试钉住)。

### (d) 验证方法:会话日志当指纹

新代码下 `reasoning_delta` 是几十条小增量、`op:pump` 几十次;旧代码是 1 条大块 + 1 次 pump——看日志立刻分辨二进制新旧。曾误判过一次:用户"还是一样"实为 `~/.cargo/bin/neo` 是旧版(`cargo build/test` 不更新 `cargo install` 装的程序);且 `cargo install --path` 会绕过 Cargo.lock 重解析依赖(edition2024 冲突),发布走 `cargo build --release` + 手动替换。**macOS 坑:直接 `cp` 覆盖已签名二进制会签名失效被 SIGKILL(退出码 137),先 `rm` 再 `cp`。**

---

## 4.59 TUI 交互对齐 opencode/mimo:一串"想当然"的坑

流式打通后逐项对齐交互细节,每项都踩了一个只有真机才暴露的坑:

- **逐块折叠的判据**:"进行中"不能只看"其后没有 TurnComplete"——TurnComplete 整轮才发一次,多步轮次里**同轮早期思考**全部满足该条件,被永远判成 live(全展开、假 spinner、点击无效)。修正:已结束 = 其后有 TurnFinished **或其后还有更新的思考块**(下一步都开始了,上一步必然结束了)。
- **主帧接线**:`Screen.thought_view` 若用默认值,点击切换的状态会在重绘时被渲染回折叠态——点击"看起来没反应"。机械补字段时默认值恰好插进了主渲染帧。
- **主题风格包**:主题从"一组配色"升级为"配色 + 风格包"(light/stars/square/backdrop/base)。Light 亮色主题整屏铺亮底(`Bg::Base`,暗色主题保持终端默认底),模态遮罩用浅灰(黑遮罩在亮底上是刺眼的洞);Terminal CRT 方角边框(角部字形 `Theme::corners()` 从 16 处硬编码收敛)+ 无纹理磷绿单色系。主题不变量测试适配:亮色主题"文字比次要文字**深**"而非"亮"。
- **实时预览**:关闭弹窗的路径散落各处(Esc/退格/输入变化/点击),逐个还原必漏——用"打开时快照 + 主循环顶部守卫"统一:弹窗已关且快照仍在 = 非确认关闭,还原;唯一保留路径是回车(确认臂清快照)。
- **宽字符 + 满宽行 = 整行折行**:行内一个宽字符(键位卡标题里的"内")使 96 格的行视觉宽度到 97,行尾贴着满宽侧栏时最后一列溢出终端行宽,**整行折行全屏错位**。`Grid::lines()` 按视觉列预算收口(宁可少画最后一列)。这是既有缺陷类,任何满宽行内的宽字符都会触发,行位移只是把它暴露了。
- **居中基准**:logo 按整窗宽居中、输入块按正文区宽居中,两条中轴差半个侧栏——截图里"没居中"实为不同轴。`put_centered_styled` 改由调用方传居中基准,欢迎屏统一正文区。
- **信息密度**:侧栏占用条用 `█░`(▰▱ 会被不少字体回退成斜杠,截图实锤),基数按**会话累计**(上下文本来就是累计填充,按最近一轮算是错的),`About.context_limit` 必须取注册表真实值(写死 64k 把智谱 128k 占用条翻倍失真)。

---

## 4.60 Web 宿主 HTTP 层：补测试网时补出一个真缺陷

桌面版计划把"HTTP 层零集成测试"列为**动界面前必须先补**的安全网（新宿主的 `--webview`
路径要复用这条链路）。补的时候暴露了两件事。

### (a) 这层其实很好测 —— 因为 `start()` 早就接受注入

`start(bind, handle_op)` 的 `handle_op: FnMut(Op) -> Vec<EventMsg>` 是调用方注入的，
所以集成测试不需要真内核：起真实监听端口 + 记录 Op 的假内核，HTTP 解析、路由、状态码、
SSE 时序、`current_goal` 跟踪全部按真实路径走，只有"模型怎么回"是假的。**首批 16 项测试
0.01 秒跑完**，不是慢测试。这说明"没写"不是"难写"，是没做。
（§4.61 又加了 7 项鉴权用例，同一文件现共 23 项。）

### (b) 补测试的动作本身抓出一个资源泄漏

第一版测试里我断言"客户端断开后订阅槽位被回收"，**它失败了** —— 一开始以为是我测试写错
（假内核默认回空事件，没有 publish 就没有写失败）。但把"必须有事件流过"这条写进断言后，
暴露的是真问题：**SSE 只在写事件失败时才发现对端断开**。空闲期间断开的连接会一直占着
订阅槽位**和连接线程**，直到下一条事件才归还；并发上限 64，攒满 64 个空闲断开连接，
新连接只会收到 503 —— 本机任何进程都能触发，且端点无鉴权（见诚实清单）。

修法是**事件驱动的断开探测**：SSE 是单向的，客户端除关闭外不再上行数据，所以服务端读侧
返回 0 就是断开的确定信号。无需心跳、无需轮询、不改线格式，符合"等待必须有条件"。
两个必须注意的细节：
- **读超时得清掉**（`set_read_timeout(None)`）：`serve()` 给每个连接设了 15s 读超时，
  不清掉的话空闲但**健康**的连接会被超时唤醒，反被误判成断开、踢掉正在看的页面。
  清不掉就**放弃探测**退回老行为 —— 少一个优化可以，误杀健康连接不行。
- **收尾要 `shutdown` 唤醒探测线程**：否则它阻塞在已死连接上，变成线程泄漏。

### (c) 回归测试必须验证"有牙齿"

修完把 `src/lib.rs` 用 `git stash` 临时还原，确认那条新测试**确实失败**，再恢复。
本项目已经吃过一次"假门禁"的亏（§5），所以"新测试通过了"不等于"新测试能抓到这个 bug"。

---

## 4.61 端点访问令牌：把"只绑回环"这句安慰话换成真的门槛

桌面版计划的第二个前置问题。原来诚实清单写的是"Web 宿主无鉴权，若改成对外监听必须先加鉴权" ——
这句话**把风险说小了两处**。

### (a) "只绑回环"不是访问控制，甚至挡不住浏览器

- 它限制的是**网络来源**，不是**谁能访问**。本机任意进程都能枚举回环端口。
- 更现实的一条：端点全是**简单请求**（`POST` + 纯文本正文，不触发 CORS 预检），所以
  浏览器里的**任意网页**都能 `fetch('http://127.0.0.1:<port>/api/turn', {method:'POST',
  body:'…'})`。它读不到响应，但**请求已经送达并生效** —— 这是 CSRF，用户只要打开一个
  网页就够了。原清单只提"对外监听"，把这条更近的路径漏了。
- 桌面绑随机端口只是**降低可见性**，不是访问控制。

### (b) 设计取舍：令牌放哪里、怎么传

- **必须同时适配 `fetch` 与 `EventSource`**，而 `EventSource` **不能设自定义请求头** ——
  所以 query 参数是浏览器侧唯一的通用方式。程序化客户端另给 `X-Neo-Token` 头。
- **令牌放 fragment（`#token=`）而不是 query**：fragment 不发给服务器、不进 Referer、
  也不进服务端请求日志，令牌只留在本地地址栏。宿主因此打印 `page_url()`（带 fragment）
  而不是裸地址 —— 两个宿主都改成用它了（桌面把它直接喂给 webview，错了就是一张 401 页）。
- **`page_url()` 的路径要显式写成 `/`**：页面用相对路径（`./api/turn`）发请求，而
  `http://host:port#token=` 这种空路径 base 在相对解析上依赖 RFC 3986 的 merge 规则。
  真机 smoke 时先写成不带 `/` 的，虽然浏览器多半也能解析，但显式更稳、也更可读。
- **鉴权要先于路由**：未带令牌访问不存在的路径也返回 401（不是 404），否则 404/401 的
  差别本身就是一个路由枚举器。并且拒绝必须发生在**送到内核之前**（测试断言 `received()`
  为空），而不是响应层的事后拦截。
- **恒定时间比较**：按字节短路会泄漏"猜对了几位"。长度不等直接 false —— 长度不是秘密。

### (c) 真机验证不能只看单测

单测跑通之后，用 `neo serve --provider mock` 起真进程 + `curl` 验证六条路径：
页面 200 / 无令牌 401 / 带令牌 query 200 / 带令牌头 200 / 错令牌 401 / SSE 同样如此。
**这一步抓到了两件事**：一是 `page_url` 缺显式 `/`；二是我的 smoke 脚本把
`-X POST` 当单个参数传给 curl 函数（helper 的引号问题）—— 脚本 bug 与产品 bug 要分清，
前者不能记成后者的功劳。

### (d) 顺手补上的：对外监听的警告

`serve --addr` 本来就可以绑非回环地址。令牌能挡住未授权调用，但那是**明文 HTTP** ——
令牌与会话内容都会在网线上裸奔。加了 `is_loopback_bind()` 判断 + 显式警告。解析不出
主机时按"对外"处理：宁可多警告一次，也不要因为格式没料到而漏掉。

### (e) 覆盖

7 项鉴权集成测试：逐个端点的未带令牌 401（漏掉任何一个，那个端点就是敞开的写入口）、
错令牌的 5 种形态（长度对但值错 / 前缀对长度差一 / 只差最后一位 / 空 / 短）、
不存在的路径不可探测、query 与头两种传递方式、页面接线的源码断言（把令牌从某个 fetch
上摘掉或从 fragment 挪到 query，这个"只在浏览器里才暴露"的错误会被 CI 抓住）。
门禁全绿；把鉴权门 `if !authorized` 改成 `if false && !authorized` 后，三条安全用例
确实失败（验证有牙齿）。

---

## 4.62 桌面 GUI 框架选型：原型实测把 gpui 挡在门外（egui 胜出）

阶段 B 的定位是"**不靠论证，靠原型实测**"——同一份最小需求（窗口 + Markdown + diff +
输入框 + 流式文本）用 egui 与 gpui 各写一遍。实测结果明确：

- **egui 走通**：121 行实现全部需求，编译成功、**真机开出窗口**。
- **gpui 编不过**：卡在 `gpui-0.2.2/build.rs` 的 `compile_metal_shaders()` —— 它无条件调
  `xcrun -sdk macosx metal` 编译 Metal 着色器，失败即 `process::exit(1)`，**无环境变量可跳过**。
  而 `metal` 编译器**只随完整 Xcode.app 分发**（本机只有 CommandLineTools）。
  `xcodebuild -version` → "requires Xcode, but active developer directory is a command
  line tools instance"；`xcrun --find metal` → "unable to find utility"。

### 最有价值的一点：失败原因与预判不同，而且更硬

计划里预判 gpui 的主要障碍是 **MSRV**（Zed 主线钉 1.98.1 > 我们的 1.95），并留了
"若编不过就退出对比"的规则。但**实际失败在更早、更硬的一层：构建期依赖 GPU 着色器
工具链**。这个差别有实际后果 —— **升到 1.98 根本救不了 gpui**，它要的是装完整 Xcode
（~10 GB 级前置）。若按预判去"先升版本再试"，会白绕一大圈才撞上同一堵墙。

**教训**：预判的障碍清单只是"待验证假设"，实测时要**捕获真实的失败点**（`cargo build`
的完整输出里那行 `cargo::error=metal shader compilation failed`），不能只回答
"能不能编过"这个布尔问题 —— 失败在哪一层，决定了有没有绕法。

### 连带修正：依赖声明数 ≠ 闭包数

计划表里写"egui 约 16 条 normal；eframe 约 34 条"，据此觉得体量可控。实测
`cargo tree -e normal` 的**闭包是约 165 个 crate**（相差约 5 倍）。原数字是各自的
**直接依赖**。凡涉及"零多余依赖"这类立场性判断，必须用闭包数。

### egui 0.36 的 API 是一次大改（凭印象写必编不过）

本次 spike 第一版 8 个编译错误全部源于此。三处：

| 常见写法（旧） | 0.36 实际 |
|---|---|
| `App::update(&mut self, ctx, frame)` | **`App::ui(&mut self, ui: &mut Ui, frame)`** |
| `egui::TopBottomPanel::top` / `SidePanel::left` | **`egui::Panel::top` / `Panel::left`**（统一为 `Panel`） |
| `panel.show(ctx, …)` | **`panel.show(ui, …)`** |
| `Event::End(Tag::Heading(..))` | **`Event::End(TagEnd::Heading(..))`**（pulldown-cmark 0.13） |

**方法**：不确定就查本机 registry 源码（`~/.cargo/registry/src/*/eframe-0.36.2/src/epi.rs`
的 `pub trait App`、`egui 0.36.2/src/containers/panel.rs` 的公开构造器），比翻文档与猜 API 快得多。

### 实测数据

| 指标 | egui |
|---|---|
| 编译 | ✅ 成功；真机开窗 |
| 传递依赖（闭包） | 约 165 crate |
| 首次编译（release） | 约 124 秒 |
| 增量编译 | 2 秒 |
| 二进制体积 | 13.4 MB |
| 实现行数 | 121 行 |

原型按计划的定位"一次性 spike，可丢弃"处理：建在**仓库外**的独立 cargo 工程，测完即删
（构建产物 592 MB / 1.1 GB，不入工作区、不污染门禁）。留档的是上面的 API 事实表与数据。

---

## 5. 假通过：门禁与测试各抓到过一次自己

这两次都值得记，因为它们说明"看起来有保护"有多危险：

1. **SPI 门禁首版用字符串搜索判断后端数**，在 `HostBackend` 只有 1 个实现时报了 4 个
   （注释里的 tui/web/exec 被算数）。**假门禁给假保证，比没有门禁更糟。**
   已改为只认 `impl <Trait> for` 语法事实。

2. **上下文上限的测试先写、实现忘加**：测试跑出 FAILED。
   如果没写这条测试，那会是一个"文档声称有保护、实际没有"的洞。

3. **负向用例自己失败过一次**：`TamperingPersistence` 首版只在输入含 `"user"` 时违规，
   而契约用例的输入是 `{"a":1}`，导致负向用例自身失败。
   这恰好**证明负向用例在起作用**（它真的能抓住违规）。已改为无条件违规。

4. **"零 warning"门禁在编译没跑起来时报了假绿**：`verify.sh` 第 2 段原本是
   `cargo check ... 2>&1 | grep -c '^warning'`，只要计数为 0 就报 ✅。但
   `rust-toolchain.toml` 当时被写成了**裸文本 `1.83.0`**（`.toml` 后缀必须是合法
   TOML，纯文本只有无扩展名的 `rust-toolchain` 才支持），rustup 因此拒绝解析
   override，本仓库内每条 `cargo` 命令都在编译前直接失败 —— **没有编译就没有
   warning，计数自然是 0，于是"没编译"被当成了"零 warning"**。
   修法有二，缺一不可：① 钉版文件改回 `[toolchain]` 表；② 门禁加预检，
   `cargo` 不能成功执行时整体判失败，且 warning 计数必须建立在 `cargo check`
   **成功退出**之上。教训与第 1 条同源：**计数类门禁必须先证明"被计数的东西真的
   跑过"**，否则 0 是"干净"还是"没跑"根本分不出来。

5. **npm 发布在"无凭证"时跳过且报绿**：`publish-npm` job 里原本写的是
   "没有凭证就 `::warning` 后跳过"，于是 tag 发布**看起来全绿**，npm 上却
   空空如也。真实踩到：`NPM_TOKEN` 被加到了 **Variables** 页而非 **Secrets**
   页，workflow 读 `secrets.NPM_TOKEN` 得到空值 → 安静跳过 → 无从察觉。
   修法：**跳过不是正常路径，必须响亮失败**。现在该步骤会打印一张
   "两个候选各是什么状态"的诊断表（写进 step summary），并在都不可用时
   `exit 1`，附上"token 是不是加到 Variables 了"等排查项。
   若确实想不发 npm，用仓库变量 `NPM_SKIP_PUBLISH=true` **显式**声明 ——
   把"意外漏配"与"有意不发"区分开。
   教训与第 1、4 条同源：**"条件不满足 → 跳过"如果没有留下可见证据，
   它就和"成功"长得一模一样**。发布链路上的任何跳过都应当是响亮的或显式的，
   不能是静默的。

6. **负向用例会"偶然构造出正确输入"，把门禁变成随机数**（§4.61 的令牌测试）。
   鉴权测试里有一条"错令牌必须被拒"，其中一例写成
   `format!("{}0", &token[..63])` —— 意图是"长度对、只差最后一位"。
   但令牌末位**本来就是 `'0'` 的概率是 1/16**，此时这个"错令牌"**恰好等于真令牌**，
   于是断言随机地期望 401 却拿到 200：**约 6% 概率的门禁抖动**。
   它一开始没被发现，是因为单跑那个测试文件 15 次全绿；
   换到 `cargo test --workspace`（8 核上全量并行）才复现出来 —— 抖动本来就要靠
   重复+负载才逼得出来。

   修法有两层，第二层才是通用的：
   1. 末位改成与真实末位**确定不同**的字符（`if last == '0' { '1' } else { '0' }`）；
   2. **在循环里加自检 `assert_ne!(bogus, h.token)`** —— 断言"我构造的负向输入
      确实不是正向输入"。这条自检会让"用例本身失效"变成一次**清晰的失败**，
      而不是一次语焉不详的 401/200 不符。

   与第 3 条同族（负向用例自身出错），但更隐蔽：第 3 条是**必然**失败（立刻可见），
   这条是**概率性**失败（1/16，能潜伏很久）。**凡是负向用例的输入由被测对象的
   真值派生而来（截断、翻转、拼接），都必须断言它与真值不同。**

   附带一条流程教训：第一次跑门禁时我用 `bash scripts/verify.sh 2>&1 | tail -8`
   把失败输出**丢了**（`tail` 的退出码还是 0，通知里报"completed exit code 0"），
   于是只看到一句"有 1 组未通过"却无从下手。**跑门禁要留全量输出**；
   用管道截断输出时，退出码是管道末端的，不再代表门禁结果。

---

## 4.63 阶段 C：把自写实现换成成熟 crate —— 收益是正确性，不是行数

桌面版计划的阶段 C（4 份自写实现共约 1436 行）。本轮完成两项，**实测把计划里
"能省行数"的收益预估证伪了**。

### (a) `neo-capability/diff.rs` → `similar`（已提交 `6d1eb234`）

计划写"省约 200 行"。算法确实换掉了（自写 LCS → Myers/Patience），`similar` 的
**依赖闭包实测为零**（`cargo tree -p similar -e normal` 只有它自己；`Cargo.lock` 里
那条 `bstr` 是 default-features 下的可选边，实际未编译 —— 用 `target/debug/deps`
里没有 `libbstr` 产物核实过，这个核实动作值得记：`Cargo.lock` 出现一个名字
**不等于**它进了构建）。

纪律是"**换算法不换策略**"：`similar` 不提供任何上限保证，会老实展开整个 diff，
所以 `MAX_ALIGN_LINES` / `MAX_HUNKS` / `truncated` 全部留在本模块。替换过程中
发现并修掉两个**既有**缺陷：

1. **hunk 头行号错误**：旧实现把新旧起始行号都写成同一个值（`context_start(...)`），
   且把"改动行数"当成"hunk 宽度"。由 TUI diff 查看器按 hunk 头定位与跳转 → 跳错位置。
2. **摘要路径输出无界**：旧实现在超过 `MAX_ALIGN_LINES` 时会把**两侧全部行**原样
   吐出 —— 而那条路径恰恰只在超上限时才走。**等于"输出有界"在最需要它的场景外
   没有保护**。这条很值得记：一个上限常被验证（小输入有界），但它真正要保护的
   那条路径从未被测过。

自己踩到并修掉的一个坑：摘要的说明文字原本写在 `@@` 行上
（`…：-7000 行 / +7000 行`），而下游解析器按空白切这一行、把任何以 `+`/`-` 开头的
token 当行号 —— 新侧起始被读成 7000。`@@` 行现在只含 `-a,b +c,d`。

### (b) `neo-host-tui/markdown.rs` → `pulldown-cmark`

计划写"省掉自写子集"。**实测相反：实现从 288 行涨到 457 行**（不含测试）。
原因是要做到"先解析、再带着色调折行"，需要额外的协调代码。

**但换来的是真正确性** —— 实测复现了老实现的一个 bug：老流程是"先按宽度折行、
再对每个折好的片段跑行内解析"。当 `**加粗**` **跨越折行边界**时，两侧片段各自
都不含配对的 `**`，解析失败，**字面星号直接留在屏幕上**：

```text
这是
**一段很长的加粗文      ← 星号没被剥掉
字需要折行** 结束
```

新流程从根上消除。免费获得 `_强调_`、嵌套列表、链接文字、CommonMark 行内解析。
依赖代价极小：`pulldown-cmark` `default-features=false` 后新增只有 `bitflags`
与 `unicase`，`memchr` 本就在依赖树里。

一处**有意偏离 CommonMark**（写进了注释与测试）：软换行按换行处理，而非规范说的
空格。依据是本项目场景是"看 Agent 回复"而不是"读文章" —— 模型输出的单个换行
通常有意义（代码片段、错误输出、条目化内容），折叠成空格会让显示与原文不符
（`a\nb\nc` 显示成 "a b c"）。代价是被硬折行的长句会显示成多行，两害相权保原文。

### (c) 两条可复用的教训

1. **把"换 crate"的收益写成"少 N 行"是错的预估方式。** 收益在正确性与覆盖面，
   而适配层（把库的输出映射回自己的渲染模型）本身要花行数。diff 是净减、
   markdown 是净增 —— 用行数当卖点会让决策建立在错误的数字上。
2. **依赖体量必须实测，且按"闭包"而非"直接依赖"算。** 同为阶段 C 候选：
   `pulldown-cmark` 闭包 3 个 crate，`similar` 0 个，`syntect` **36 个**。相差
   一个量级。计划表里原本写的是各自的直接依赖数，据此判断会严重失真
   （同一个错误在阶段 B 的 egui 上出现过：声称 16/34，闭包实为约 165）。

### (d) 跨 crate 往返测试：两边单测都绿，拼起来会错

`unified_diff`（生产者）与 `diffview::parse`（消费者）**各自都有测试**，但都只对着
**自己假定的格式**：生产端改 hunk 头格式、消费端按空白切 token 找行号，两边单测
都会绿，拼起来却跳错行。新增 2 项往返测试钉住这条 —— 放在 **TUI 侧**（分层方向
L5→L3，`neo-capability` 不能依赖宿主）。

**通用规则：两个模块通过一个字符串/字节格式协作时，必须有一个测试是用真实生产者
喂真实消费者的**，不能只有各自的格式假设。

### (e) 抽取 `neo-text`：把"宿主中立的文本语义"沉到宿主之下

阶段 D 的前置。落点 `crates/neo-text`（L1 BASE，workspace 24 个 crate）：`Tone` +
`width` + `syntax` + `markdown`。用 `git mv` 搬（保留历史），TUI 侧
`pub use neo_text::{markdown, syntax, width, Tone};` —— **73 处 `width::*` 与
258 处 `Tone` 调用点一行未改**。这个"re-export 而不是改调用点"的选择是本轮
改动面小的直接原因，值得复用。

**为什么必须沉下去**：桌面原生 GUI 宿主要用同一份 Tone/Markdown，而架构守卫
A3 **禁止宿主互相依赖** —— 共享层不能在任何一个宿主里。

**`Tone` 的宿主中立化：删掉 `StarDim` / `StarBright`**。它们是终端星场的**装饰**，
不是文本语义（GUI 宿主没有星场，也不会去查 `theme.star_dim`）。一个"宿主中立"的
色调枚举里带着某个宿主的装饰，就是把宿主概念漏进了共享层 —— 恰恰是本 crate 要
消除的东西。改由调用方经 `Tone::Rgb` 传具体颜色（TUI 侧新增
`star_tone(theme, brightness)` 从自己主题取值）。
这是 §4.32「装饰不得进入语义」在**跨 crate 边界**上的又一次应用 ——
同一个原则在同一个项目里已经以三种形态出现过（进语义、进日志、进共享层）。

**顺带删掉死代码 `Grid::fill_stars`（16 行，无调用点）**。它的存在恰好说明为什么
该删：它和 `fill_background` 里的星场写入点**重复实现同一件事**，留着就是下一个
"改了这里忘了那里"的来源。死代码不只是浪费 —— 它会诱导下一个人去改错的那份。

**顺手更正一处不准确的自我描述**：TUI 的 `Cargo.toml` 原写"**零依赖**"，但文本语义
下沉后依赖树里必然多了 `pulldown-cmark`。改为"**终端控制零依赖**"并写明零依赖指的
是哪一层。**把"零依赖"当成整个依赖树的属性，是这类自我描述最容易失真的地方** ——
它原本指的是"不用 ratatui/crossterm、ANSI 自己写"。

**分层登记**：`check_architecture.py` 的 `LAYER` 登记 `neo-text: 1`（与 L1 PLATFORM
同级）。放 `LAYER` 而非 `SIDE` 是刻意的 —— `SIDE` 会**跳过 A1 方向校验**，而新 crate
正需要被检查。已用"注入违规依赖"验证守卫确实会拦
（`neo-text(L1) -> neo-core(L2)` → A1 向上依赖失败）。
**教训：新 crate 登记到哪张表，决定了它受不受检查** —— 图省事塞进"旁挂"就等于
放弃对它的依赖方向约束。

---

## 4.64 阶段 D 起步：桌面原生 GUI 宿主（egui → gpui）

> **本节是一份按时间顺序的日志**（(a) 最早、(aq) 最新）。
> **读结论请直接跳到下方"当前状态"** —— 开头几节描述的是 egui 时期，
> 而结尾的状态已经变了（这正是把日志按时间排的原因：能看出怎么变的）。

### 当前状态（2026-09-16 会话结束时）

- **`neo desktop` 默认是 GPUI**（(am) 转正），不再是 egui。
- **egui 已冻结**（只修 bug、不加功能），保留为 `--egui` 回退通道；
  **未删除**，前置条件见 (an)。
- 渲染缝有 **3 个真实消费者**（变更条 (p)、用量图 (ap)、分段进度 (aq)），
  组件测试 46 条。这是方案 Phase 4 "≥3 个自绘组件"的门槛。
- 可开源集 5 个 crate 已切 **Apache-2.0**（(ah)），可提取性守卫每次门禁都会
  **真的把目录复制到空仓编译**（(ab)）。
- **仍未实测**：Linux 构建（只能靠 CI 首跑）、真输入法 IME、fractional scaling
  —— 见 §8 缺口清单底部。

### 小节导读（按会话分批）

| 批次 | 小节 | 主线 |
|---|---|---|
| 1 | (a)–(p) | egui 宿主从零到 D2–D11 + 真机验收；渲染缝的**第一个**消费者 |
| 2 | (q)–(z) | D4 分组、gpui 真实输入框、D3 搜索、打包解开键盘验证、D7 补齐、事件差集、高 DPI、IME 用例、D1 状态圆点 |
| 3 | (aa)–(aq) | 可提取性/许可证守卫、RichText 与 storybook、Apache-2.0、自动跟随、模型选择器、帮助、侧栏折叠、**gpui 转正**、缝的第 2/3 个消费者 |

---

新建 `crates/neo-host-egui`（L5，workspace 第 25 个 crate）。当时 `neo desktop`
**默认**走它（后来换成 gpui，见 (am)）；`neo desktop --webview` 保留原 webview
路径（ADR-0006：不押注单一方案）。

### (a) 与 webview 版的结构差异（不是"换个渲染库"）

原生宿主**不经 HTTP、不开端口**：内核独占驱动线程，UI 直接收 `EventMsg`。
webview 版那整条 HTTP/SSE 链路存在的原因是"窗口是个浏览器"，原生宿主不需要它 ——
顺带也就没有"本地端口谁能访问"这个问题（§4.61 刚为它加了访问令牌）。

### (b) 驱动层：照 TUI 的模式，且两处**故意不同**

计划点名的几条都落实了：内核独占工作线程、每帧发 `Op::Pump`（不是 `UserTurn`）、
审批走 `ApproveStep`（不是 `Approve`）。与 TUI 版的两处差别是有意的：

1. **`drain()` 用 `try_recv`，一帧都不等**。TUI 版等 80ms 没问题（它有自己的泵循环），
   而 egui 是即时模式，`update()` 每帧都要立刻返回去绘制 —— 等 80ms 就是掉帧。
   测试直接**量了时间**（内核忙时 `drain` 返回耗时 < 50ms），不是只断言"返回空"。
2. **驱动逻辑在本 crate 内重写**，不能 `use neo_code_cli::tui_driver` ——
   架构守卫 A3 禁止宿主互相依赖。所以两个宿主**各有一份**同源测试（边界守卫、
   审批后恢复），这是刻意的重复而非遗漏。

`driving` 边界守卫是这层最要紧的：逐帧 Pump 意味着 UI 会持续往通道塞 Pump，
若某批出现边界事件，**更早排队的 Pump** 落地时轮已结束 —— 内核会对空闲状态再起一步，
那是一次**真实、要花钱的模型请求**。已用"去掉守卫"验证测试确实会失败。

### (c) `neo-text` 的第二个消费者（抽取它的理由兑现了）

GUI 用 `neo_text::markdown::blocks()` 而不是 `render()`，差别是实质的：
`render` 面向终端、按等宽列**折行**；egui 有自己的文本布局，两者都折一遍会
**折两次**（断点错位、缩进错乱）。所以新增 `blocks()`：只做解析与语义分层
（前缀缩进、项目符号、色调），换行交给宿主 —— 与 `render` 共用同一解析器，
内容与色调由测试保证一致，差别只在谁断行。

调色板同理：新增 `neo-text::palette`（`Tone → RGB`）供两宿主共用。**但要防漂移**：
品牌色现在同时存在于 TUI 的 `theme.rs` 与 `neo-text::palette`（后者宿主中立，
前者带星场/方角等终端专有字段，无法整体搬走）。两份值靠 TUI 侧一条**逐字段比对的
守卫测试**锁住 —— 改一边忘另一边当场失败。已用"改一个色值"验证守卫会拦。

### (d) 顺手修掉 markdown 的一个真 bug（上一轮软换行改动引入）

把软换行按换行处理后，`- a\n  b` 这种"一项被源码折行"会画成**两个项目符号**；
有序列表更糟：`1. a\n   b` → `1. a / 2. b`（逐行递增编号）。根因是符号归属弄错：
**符号属于"项"，不属于源码的每一行**。修法是 `item_started` 标记 —— 项内续行只
缩进对齐、不再出符号，编号也只在本项首行递增。

**教训：改一个"看起来很小"的渲染决策要回头测所有受影响的形态。** 软换行那一改
当时只测了段落，列表没覆盖到。

### (e) 观感验收：第一张截图就抓到一个机器测不出的缺陷

**已授权屏幕录制并完成真机截图验收**（此前那版写着"屏幕录制被拒、观感未验"，
已作废）。**第一张截图立刻发现**：`NEO` / `Default` / `WorkspaceWrite` 这些拉丁
文字正常，**所有中文都是 `□`**。egui 自带字体只覆盖 latin + cyrillic（官方文档明写），
而本项目界面文案是中文 —— **不装中文字体 = 界面不可用**。

**它躲过了此前所有机器检查**：编译通过、开窗成功、进程存活、驱动层三条回归测试
全绿、T6 契约通过。因为它不是逻辑错误，而是**字体缺字形**。唯一能发现它的手段
就是**看一眼屏幕**。

修法分两层：

1. `fonts.rs`：按**平台候选表**在运行时从系统加载中日韩字体（macOS:
   Hiragino Sans GB / PingFang / STHeiti；Windows: 微软雅黑；Linux: Noto CJK /
   文泉驿）。**不把字体打包进仓库** —— 候选都是 20 MB 量级（Hiragino 23 MB、
   STHeiti 55 MB），入库既让仓库膨胀，也牵扯系统字体的再分发授权。
   Proportional 与 Monospace **两个 family 都要装**，只装一个则另一半界面照样是方块。
2. **把"能不能显示中文"变成可机器判定的门禁**（`verify_cjk_renderable`）：
   用 `Context::run_ui` 在**无头环境**跑一帧装好字体，再用 `Fonts::has_glyph()`
   逐个查界面里真正会出现的汉字；并配一条**反例测试**（不调 `install` 时必定失败）
   证明自检有牙齿，不是永远返回 Ok 的摆设。
   **"只能靠人眼"不是可接受的终局** —— 能把人眼发现的问题转成机器门禁就该转。

踩到的 API 细节：`run_ui` 返回的 `FullOutput` 必须显式处理 `textures_delta`
（`epaint` 在析构时会 panic 提醒"有未处理的 delta"，它假定调用方是渲染后端）。
自检不渲染，主动 `clear()` 即可。

**验收结果**：中文与 D2–D7 全部正确渲染 —— 思考块（可折叠）、工具卡片（名字/状态/
exit/参数摘要/输出）、任务清单（✓ ▸ · 三态）、Markdown 正文、代码块语法高亮、
引用竖条、轮摘要、右侧 Goal 面板、底部输入区。代码块缩进正确。

### (f) 端到端界面测试：替代"手工点击"

验收时试过用 CUA 往窗口输文本，**进不去**：egui 是自绘画布，不通过 accessibility
暴露可写文本值 —— `set_value` 与 `type` 都报 `target_verification_status: mismatched`
（文字落到了当时的前台应用上）。app 级键盘也被拒（cargo 二进制 `bundle_id` 为 null，
无法解析成唯一 app）。

**手工点击本来就不是可重复的验证手段**，所以补 `tests/end_to_end.rs`：起**真内核**
（生产装配函数 `neo_exec::build_kernel`，含 compactor 与 goal orchestrator 的接线）、
接真驱动线程与 `Transcript`，断言 D2–D7 的内容确实出现。

这一步里有三个"测试自己写错、差点变成假绿"的坑：

1. **自己拼 `Kernel::new` 会漏接线**：第一版手拼内核、没装 goal orchestrator，
   `GoalSet` 直接报 `GoalUnavailable`。改用生产的 `build_kernel` 后，测试验的是
   **真实装配**，而不是"只在测试里存在的内核"。
2. **只发一次 `Op::Pump` 会空等**：`Pump` 只推进一步，多步轮次需要多次；真人界面
   每帧发一个。第一版只发一次，那个用例因此**跑满 20 秒超时**（其余用例 0.2–0.7 秒）。
   改成每轮都发后，整个文件 **0.04 秒**。
   **慢测试通常是"没在模拟真实节奏"，不是被测代码慢。**
3. **`if has_diff` 式的软断言等于没断言**：D5 原文是"若内核发了 `PatchProposed`
   就检查界面显示"，但用例用的工具名 `write` 在生产工具集里**不存在**
   （只有 bash / apply_patch / request_user_input / todo_write），
   于是那条断言永远不执行。改用真实的 `apply_patch`（唯一会产 diff 的工具）
   并改成**硬断言**，再用"把 diff 显示摘掉"验证它会失败。

顺手发现：`bash` 的参数名是 `cmd` 不是 `command`；传错键会让它走
"识别不出 → 保守判 Write"的分支并触发审批 —— 这个保守策略本身是对的
（宁可多问一次，不可漏放一次写）。

### (g) 一处配置决定要说明

`egui` feature 进了 `default`。理由：`neo desktop` 按计划就该是原生 GUI，若它不在
默认里，用户不装只会得到一句"未编译"—— 默认行为与文档不符。且 `neo-host-egui`
是 workspace 成员，`cargo check --workspace` **无论如何都会编译它**，所以放进默认
**不新增 Linux 构建面**，只是让 CLI 的接线分支也进 CI（否则那段
`#[cfg(feature = "egui")]` 无人编译，正是"腐烂"的成因）。
想省编译的用户仍可 `--no-default-features` 或 `--no-default-features -F desktop`。

还有一条**冒烟钩子**：`NEO_GUI_PROMPT=<任务>` 会在启动首帧自动提交一次任务 ——
因为 egui 画布无法自动输入，这是"启动 + 提交 + 渲染"一次跑完、供截图验证的路径
（与 AGENTS.md 里 `PROTEUS_CODE_SMOKE` / `PROTEUS_CODE_QUERY` 的约定同源）。

### (h) 真机上手用一遍，又抓到两个"只有动手才会暴露"的问题

授权辅助功能后做了完整的真机流程验证（打字 → 回车 → 审批 → 落盘）。两个修复
都不是"看代码能发现的"，一个是安全语义问题。

**1）审批前的 diff 可能与实际改动不是同一个文件（严重）**

现象：`neo desktop --workspace /tmp/x` 审批写文件时，界面**完全不显示 diff**，
用户仍被要求批准一次真实写入 —— 正是 preview 机制要消灭的"盲批"。

定位路径值得记：先看会话日志确认 `patch_proposed` **压根没发出**（不是界面的问题），
再往下追到 `ApplyPatchTool::preview` 读的是**进程当前目录**下的相对路径，而
`execute` 走 `ToolCtx::resolve` 按**工作区**解析。两者在 `--workspace <非进程目录>`
时必然指向不同文件 —— 那次 CWD 是仓库根、工作区是 /tmp/x，仓库根恰好有个同名
文件**且内容相同**，于是算出"空 diff" → 不给预览。

**这不只是显示问题，是安全语义问题**：用户照着预览批准，改的却是另一个文件。
修法是把 `cwd` 传进 `Tool::preview`（签名变为 `preview(&self, args, cwd)`），
与 `execute` 共用同一套解析规则。补 4 条回归测试，用"还原为按 CWD 读"验证过
**3 条会失败**；真机复验日志里出现了 `patch_proposed`。

**SPI 门禁正确拦下了这个签名变更**（S2c 指纹漂移：`ToolTransport` 口径已变），
强制"显式确认这是有意变更"并留可审计痕迹 —— 这正是它该做的，不是碍事。

**2）输入框不自动聚焦**

打开窗口后必须先用鼠标点一下输入框才能打字。对聊天式界面这很别扭，而且它
**卡住了真机键盘验证本身**（egui 画布不接受自动化注入文本，唯一活路是真键盘，
而真键盘需要焦点 —— 一个鸡生蛋问题）。修法：首帧请求焦点 + 从"审批阻塞"恢复时
再抢一次（只在恢复那一刻，不是每帧，否则用户没法把焦点移到别处）。
顺带把回车提交收到 composer 的 `lost_focus` 路径（原先全局与输入框两处都判，
一次回车可能提交两次）。

### (i) 把界面行为变成可无头测试的断言

`App::draw` 从 `eframe::Frame` 解耦（trait 方法只转发），于是能在 CI 里用
`Context::run_ui` **跑真帧**。新增 4 条：输入框首帧自动聚焦、回车提交并清空、
待审批时不被聚焦且提交被忽略、四种面板状态跑帧不 panic。自动聚焦那条用
"去掉 `request_focus`"验证过会失败。

**测试台自己的坑也要记**：第一版每帧新建 `Context`，而焦点存在 Context 的 memory
里 —— 于是"上一帧有焦点、这一帧回车触发 `lost_focus`"永远不成立，测试红了
一度怪到产品头上。**真实应用只有一个 `Context`，测试必须照做。**
（这也解释了为什么真机是通的而单测最初是红的 —— 这种"测试台与真实运行环境不一致"
造成的假失败，跟假通过一样会误导人。）

### (j) D11 模式切换：计划判断过时 + egui 焦点导航冲突 + 一个假通过

对标 `desktop-parity.md` 的 D11（执行模式切换）。三件事值得记。

**1）计划里"需新端点"的结论是错的 —— 落地时才发现**

原表写 D11"需新端点（运行时切换）"。实际上 `Op::ConfigureSession` **早已支持**
运行时改 `exec_mode`（`SessionPatch.exec_mode` 就在协议里，TUI 有 4 处在用）。
所以这项零后端改动就完成了，还顺手把模型 picker 也做了（同一个 Op 的 `model` 字段）。
**教训：差距清单里的"需要新能力"是**估算**，动手前先 grep 一遍协议层** ——
本次两处（D11 的端点、D3/D4 的"数据现成"）都因为便宜而白写了预期。

**2）egui 把 `Tab`/`Shift+Tab` 当焦点导航键，抢不过它就要用 `raw_input_hook`**

真机复现：`Shift+Tab` 切模式**成功**，但焦点从输入框跳到了右侧面板的 resize
手柄，**接着敲的字全部丢失**（输入框里只有 "aaa"，之后的 "bbb" 没了）。

根因：egui 在 `begin_pass` 里读按键做焦点导航，那时 `draw` 还没跑 —— 帧内再
`consume_key` 已经晚了。唯一的拦截点是 eframe 的 `raw_input_hook`（官方文档明写
"可用于阻止特定快捷键被 egui 处理"），它在 `begin_pass` **之前**跑。

代价：界面里 Tab 焦点导航基本失效。这是**有意的取舍** —— 切模式是高频操作，
Tab 导航在这套界面里几乎用不到。

**3）一个假通过：断言"状态没有变化"的测试必须给足观察窗口**

第一版焦点保持测试**只跑一帧**就断言，结果**无论实现对错都通过** ——
因为 egui 的焦点转移是**延迟生效**的（`focus_direction` 在某帧设置，实际转移
发生在**下一个 pass** 控件注册时）。用"不摘除事件"（即旧行为）验证时照样绿。
改成跑完后续帧再断言后才真有牙齿（现在不摘除会失败，并报出焦点跑到了
`neo_goal.__resize`）。

**通用规则：跑帧类的测试，"没观察到变化"≠"变化不会发生"。**
凡是断言"某状态**保持**不变"的用例，都要问一句：观察窗口够不够长到让变化显现？
这与 §5 那些"假通过"是同一族问题 —— 测试绿了，但它测的不是你以为的那件事。

### (k) D9 命令面板：两个键位都要在 hook 里摘、以及"只测主效果"的假通过

对标 `desktop-parity.md` 的 D9（命令中心）。做 commands 一类（9 条命令）；
conversations / files 需要会话库与文件索引（D1 范畴），未做，标注在 parity 文档里。

**1）注册表只收录"GUI 真的能执行"的动作**

TUI 的命令表有 24 条，含主题/星场/logo 等**终端专有**动作 —— 原生窗口里没有
对应物。照抄会产生"命令列着但点了没反应"，那比没有更糟（用户以为坏了）。
配套一条测试**逐个执行全部命令并断言界面状态确实变了**，漏实现会红
（用"把 Help 改成空实现"验证过）。

**2）`Esc` 也必须进 `raw_input_hook`（与上一轮 Shift+Tab 同一根因）**

真机实测：只在帧内判 `Key::Escape` 时，egui 先把它当"结束文本编辑"消费掉了，
表现是**搜索框被清空、面板却还开着** —— 用户按 Esc 以为关了，实际没有。
放进 hook 摘除后正常。
**规律：egui 会优先消费的键（Tab / Shift+Tab / Esc 这类"界面导航键"）都必须
在 `raw_input_hook` 里拦，帧内判断一律晚一步。** 这条已经出现两次了。

**3）"只断言主效果"的假通过**

`escape_closes_the_palette_without_running_anything` 这条**原本是假通过**：
它只断言"面板关了"，但在旧实现下**也通过** —— 因为 egui 清空搜索后、面板仍旧
开着，而测试从不检查搜索串。是**真机截图**才暴露出差异（面板还开着、搜索空了）。
加强为"同时断言搜索串与关闭状态"后，它才会因旧实现而失败。

**通用规则：断言"某事发生了"的测试，也要断言"没有意外副作用"。**
只测主效果会漏掉"主效果没达成 + 副作用发生了"这种组合 ——
而那恰恰是用户最能感知的坏行为。

**4）一个方法论提醒：AX 树可能滞后于渲染帧**

排查上面这条时，我一度从辅助功能快照读到"面板仍开着"，而同一时刻的截图显示
已经关了（也遇到过反向的情况）。**判定视觉状态以截图为准**，
辅助功能树适合读"有哪些控件、文本是什么"，不适合判断"这一帧画成什么样"。
——这与 §4.57 那条"机器测不了观感"是同一件事的两面。

### (l) D1 会话栏：分层搬迁、泛型化避免抄第二遍、以及"当前会话不在列表里"

对标 `desktop-parity.md` 的 D1。做了数据支持得住的部分（会话列表/切换/新建）；
其余子项（工作区分组、状态圆点、`+/-` 行数、视图切换、Archive）需要会话元数据里
有工作区与运行状态，当前 `SessionStore` 只有 id/标题/记录数 —— **列出来但显示不出来
比不列更糟**，故不做并逐项标注。

**1）共享契约必须下沉，且搬迁成本比预想低**

`SessionControl` 长在 `neo-host-tui` 里。桌面 GUI 也要会话管理，A3 禁止宿主互相
依赖 → 契约搬到 `neo-session`（SIDE 层，只依赖 neo-protocol），TUI 改 `re-export`，
**13 处调用点与 300+ 测试一行未改**。
**"re-export 而不是改调用点"这个手法在本项目已经用了第二次**（上次是 `neo-text`），
它把"移动代码"的代价从"改 N 处"降到"改 1 处"。

**2）把两份同源逻辑合成一份：泛型化端口**

会话切换 = "让内核换会话并重建上下文" + "把历史转回事件流给宿主"，逻辑宿主无关。
但两个宿主的**内核句柄是不同具体类型**（驱动层各自实现），直接复用会卡在类型上。
解法是抽一个两行端口 `KernelAccess`（`with_kernel` 在内核上跑闭包），两端各实现
一次，`Sessions<H>` 一份逻辑同时服务两个宿主。
不这样做，那 120 行边界处理要抄两遍 —— **抄一遍就意味着将来修一处忘一处**。
这是"两个宿主、一份逻辑"的通用解法：**在两侧各自适配最小的端口，把逻辑放中间**。

**3）"当前会话不在列表里"：惰性创建带来的界面空洞**

`SessionStore::new_id()` 刻意不建文件（首次写入才惰性创建，避免"新建了却没用"
留空文件）。于是刚点过"新建"的会话不在 `list()` 里 —— 真机实测：界面回了
"已新建会话 X"，**侧栏却仍显示"暂无会话"**，用户看不到也点不到自己刚建的那个。
修法：当前会话即使不在列表里也补进去（它必然存在，只是还没落盘）。

**这是一个可复用的模式：列表来自"落盘状态"，而界面还要显示"尚未落盘的新对象"。**
凡是"惰性创建 + 列表"的组合都要问一遍：刚建的那个在列表里吗？

**4）静默无反应被自己的测试抓住**

`/new` 在"未接会话库"的装配下**什么也不做**。抓到它的是
`every_command_has_an_observable_effect` —— 那条"断言每个命令都要有可观察效果"
的测试。改为如实反馈"本装配未接会话库"。
**静默无反应正是"用户以为坏了"的典型**；有反馈哪怕只是说明做不了，也比没有强。

### (m) D8 命令台：计划判断又过时一次、三个真机缺陷、以及"我写的测试其实没抓住它"

对标 D8（底部终端面板）。**第三次发现计划判断过时**：D8 原标"需新功能"，
实际 `Op::Shell` 早已存在，且内核把它交给**与模型工具调用同一条**执行路径 ——
零后端改动。（前两次是 D11 的"需新端点"、D9 的"纯前端"。）

面板命名为"**命令台**"而非"终端"，是**如实呈现能力边界**：ZCode 用 `node-pty`
起真 PTY（可跑 vim/htop 这类全屏交互程序），我们**每条命令一个进程、无 PTY**。
叫"终端"会让人期待 vim 能用。

**1）`Op::Shell` 的调用 id 会重复（内核缺陷）**

id 曾嵌 `turn_counter`，而 shell 在**任何轮次之外**、不递增那个计数器 ——
连敲两条 `!cmd` 都得到 `shell-0`。id 的用途正是标识一次调用（工具结果按 id 配对）。
改用独立的 `shell_counter`；回归测试验证旧行为确实产出
`["shell-0","shell-0","shell-0"]`。另补一条"shell 不得占用轮次号"的测试 ——
轮次号是回放与审批 id 的确定性来源。

**2）覆盖层关闭后焦点丢失（界面）**

`Cmd+K` 开面板 → `Esc` 关掉，之后两个输入框**都没有焦点**，键盘输入无处可去
（表现为"打字没反应"）。修法：关闭覆盖层时把焦点还给输入框。

**3）两个控件争抢焦点（界面，最隐蔽）**

修完 2 之后我用**两个独立布尔**表达焦点意图（composer 一个、terminal 一个），
而它们是**竞争关系**、胜负取决于面板绘制顺序 —— 真机表现：从命令台敲的命令
被当成任务发给模型，白花一次模型请求。
改成**单一焦点请求**（`Option<FocusTarget>`）：每帧至多一个控件抢焦点，
谁最后设置谁生效，与顺序无关。

还有一处顺序陷阱：命令面板的关闭处理无条件"把焦点还给 composer"，
而 `/terminal` 这类命令**自己指定**焦点目标，于是**后执行的 close 把它的请求覆盖掉**
（顺序：先 `run_action`、后 `close`）。改为"只在动作没指定焦点时才归还"。

**4）最值得记的一条：我为 3 写的测试其实没抓住它**

第一版测试是绿的，我一度以为覆盖到了。回头做 teeth check（用修之前的行为跑）
才发现它**照样绿** —— 两个原因：

- **绕过了用户真实路径**：直接调 `run_action(ToggleTerminal)` + `close()`，
  而覆盖 bug 恰恰就在"面板执行 terminal 命令 → 面板关闭归还焦点"那段代码里。
  没走那段代码，bug 不出现。
- **断言了间接指标**：断言"焦点 id 是终端输入框"，而非用户真正感知的结果
  （命令有没有被发给模型）。而且焦点请求**下一帧**才生效，我却执行完立刻断言。

新测试：用**回车事件**驱动面板自己的处理（把那两段代码都走到），
并断言**发出去的 Op** —— 必须是 `Op::Shell`、**绝不能有** `Op::BeginTurn`。
这正是真机验证用的判据（会话日志里是 `op {"shell":...}` 而非 `begin_turn`）。

**通用规则：为某个 bug 写的测试，必须先用"修之前的行为"跑一遍确认它会红。**
写完就绿不等于有覆盖 —— 它可能测的是另一条路径。这与 §5「假通过」同族，
但更隐蔽：§5 那些是"门禁自己失效"，这次是"测试自己跑偏"。

### (n) gpui 落进 NEO：Phase −1 实测推翻方案的三处论断

用户决定把 `docs/gpui-方案` 作为 NEO 的桌面 UI，且其中的层将来要**整目录开源**
（渲染框架 + 组件库）。我先跑了方案自己规定的 Phase −1（"必须实际运行，不要假设"）。

**实测结论：GO。**

| 项 | 结果 |
|---|---|
| 编译 | ✅ `gpui-kit 0.6.1`，冷 265s / 增量 151s |
| 需要完整 Xcode | ✅ **不需要** |
| CJK 渲染 | ✅ **全绿且零字体配置**（中/日/韩 + emoji + 中英混排） |
| 冷启动 | ✅ 509–560ms（预算 <800ms） |
| release 体积 | ✅ 8.9MB（预算 <30MB） |

**上一轮否决 gpui 的原因已消失**：那道门是上游 `gpui 0.2.2` 的 build.rs 调
`xcrun metal`；而 `gpui-pre-macos` **无 build.rs、无 `.metal` 源**（着色器预编译进库）。
**同一框架的不同发行线，构建成本完全不同** —— 这条值得记：上次的"No-Go"只对
那一条发行线成立。

**CJK 零配置这一点甚至优于 egui**（egui 需显式加载字体，见 §4.64(e)，
那是我上一轮真机踩出来的 bug）。

### 方案里被实测推翻的三处

1. **P2 与 A1 自相矛盾**：方案主张"用 gpui-ce"，但它钦定的组件库
   `gpui-kit 0.6.1` 实际依赖 **`gpui-pre 0.3.x` + `gpui_platform`**，不是 gpui-ce。
   而 `gpui_platform` **不在 crates.io** —— 手工声明会直接报错。
   **正确姿势是只声明 `gpui-kit` 一个依赖**（它带齐全部且版本自洽）。
   讽刺的是：方案原本设想的写法会破坏它自己最强调的单一 pin。
2. **"禁 `use gpui::`"与"直接用 gpui-component"冲突**：后者返回 `impl IntoElement`，
   是 gpui 类型。改成可机器判定的版本 —— **不得声明 gpui 依赖，只能经门面取用**
   （`check_ui_layering.py` 的 U2）。
3. **Fontique 接入不必要**：CJK 零配置已正常，删掉该任务。

### 三个"代码上看不出来"的缺陷（真机截图逐个抓出）

1. **主题模式没切 → 白底白字**。`gpui_component::init` 装浅色主题，而 NEO 调色板是
   深色的。只改语义色不切模式 = 近白字画近白底：窗口能开、布局对、中文正常，
   **但看不清**。所有自动化检查都会绿 —— 这类"看起来成功了"最危险。
2. **改了没人读的那一层**（更深）。第一版修法是 `Theme::change(Dark)` 后逐字段设
   `theme.primary = ...`。**还是白的**。读源码才明白主题有**两层**：
   `colors`（语义色）与 `tokens`（由前者**派生**），token 在 `apply_config()` 里
   一次性算出，而 `Root` 读的是 `tokens.background` —— **第二层**。
   正确顺序四步：切模式 → 覆盖 colors → **重建 tokens** → `sync_base`。
   第三、四步最容易漏，且从代码上看"语义色已经改了"。
3. **我自己拿 `Tone::None` 当背景色**。它的语义是"未指定色调"，调色板把
   `None | Text` 兜底到 `text`（近白）。现在 `neo-ui` 提供显式 `base_bg()`：
   **名字本身说明它是背景**，不靠猜语义；并加测试钉住。

**共同教训：渲染类问题里，"设置了一个值"与"那个值被读了"是两件事。**
本轮的 bug 全部属于这一类 —— 代码看着做了正确的事，但设的那一层没人读，
或用了语义不符的值。**只有真机截图能发现**，这与 §4.64(e) 的豆腐块同源。

### 结构整理（判据清晰，值得复用）

转录模型（事件流 → 显示块）下沉到 `neo-driver`：两个 GUI 宿主共用，A3 禁止宿主
互依赖。**它进 driver 而不是 UI 栈，判据是"能不能脱离内核独立发布"** ——
它认识 `EventMsg`（依赖内核轴），而 UI 栈被 U1 要求不得依赖内核轴。
**能独立开源的进 UI 栈，不能的进 driver。** 移动后 egui 宿主 77 测试一行未改。

### (o) gpui 阶段 2：补齐面板时先删掉两处"两个来源"

把数据已就绪的界面补到 gpui 宿主（D1 会话栏 / D7 目标面板 / D8 命令台 /
D9 命令面板 / D11 模式与模型切换）。但这一轮真正的收获是**先修了两处重复**。

**1）待审批状态存了两份**

`Transcript` 消费 `ApprovalRequest` 时会设 `transcript.pending`；而我在 gpui 视图里
又造了个 `PendingApproval` 结构。同一件事两个来源 —— 修一处忘一处就出问题。
**共享模型里已经有的状态，宿主不该再存一遍。**

**2）命令表长在 egui 宿主里**

`commands.rs` 引用 `Op`，因此**依赖内核轴**。按本项目已确立的判据
「能脱离内核独立发布的进 UI 栈，不能的进 driver」，它应当属于 `neo-driver`。
留在原处的话 gpui 只能复制一份 —— 那会让"有哪些命令"出现两个来源，
而两份措辞必然漂移（用户看到两套说法）。

**判据本身在这两轮里被用了三次，已经稳定**：
> 一段逻辑要下沉到哪个 crate，看它**依赖什么**：
> 依赖内核轴（认识 `Op`/`EventMsg`）→ `neo-driver`；
> 能脱离内核独立存在 → UI 栈（`neo-ui-*`，将来可整目录开源）。
转录模型、命令表都按这条落位。

**3）`Cmd+K` 被宿主应用抢走：环境干扰 ≠ 产品缺陷**

试图用 osascript 发 `Cmd+K` 验证命令面板时，**被 ZCode 自己的命令面板吃掉了**
（截图里那个"搜索操作、任务或文件"是 ZCode 的）。这不是 gpui 或本实现的 bug，
而是**验证环境的干扰**。

判定方法值得记：**看日志/看谁的面板出现了**。截图明确了那是 ZCode 的面板，
于是不必去查自己的实现。随后加了 `NEO_GUI_PANEL` 环境变量（启动即展开某个面板）
绕开键盘路径 —— 与 `NEO_GUI_PROMPT` 同源：**画布无法靠自动化工具输入或触发，
面板可见性只能由环境变量驱动**。这是验收手段，不是产品特性（代码里已注明）。

**4）面板是纯绘制，靠截图；逻辑要抽出来测**

`next_model`（模型循环）是真逻辑，抽成自由函数并补 3 条测试（回绕 / 单模型无操作 /
当前模型不在列表）。循环写错的症状在真机上只表现为"模型名没变"，很难归因 ——
这类"看得见但说不清"的行为最值得单测。

### (p) 渲染缝的第一个真实消费者：把"信仰"变成"设计"

补齐 gpui 的 D3（思考块折叠）与 diff 变更条。后者是**本项目的渲染缝第一次被真的用上**。

**1）折叠语义：两个意图要叠加，不能互相覆盖**

共享模型里本来就有 `collapsed_reasoning`（逐块），但 gpui 渲染完全忽略了它。
接上时判据是 **折叠 = 全局开关关掉 或 这一块被单独折叠** ——
全局关掉是"我现在不想看思考"，逐块折叠是"这一块太长"。
混成一个变量的表现是"关掉全局后逐块状态丢失"（或反之）。
**同一份状态在共享层，两个宿主必须读出同一套语义**。

**2）为什么让变更条走渲染缝（而不是直接调 gpui 绘制）**

方案自己那条纪律：「只有一个实现的抽象是**信仰**，两个实现的抽象才是**设计**」。
此前渲染缝只有定义与单测、**零真实消费者** —— 那正是信仰。
变更条给了它第一个消费者。

选它的理由：diff 的**文字**用文本渲染是对的（要可读、可选中），
但"改动集中在哪里"用文字表达不了（200 行里改 3 处，滚动时看不出来）。
那是真正的**图形**，正是"只有自绘表面才走缝"的适用场景。
直接调 `window.paint_quad` 也能画出来 —— 但那样这条缝永远验证不了。

**3）分工：翻译是纯数据（可测），绘制要 Window（靠真机）**

把 `GpuiPaint` 从"只有颜色数组"改成真能绘制（`quads` + `draw(origin, window)`）。
关键是分工：`paint()` 只做**坐标与颜色的翻译**（纯数据，所以能单测
"比例/下限/裁剪/颜色同源/端到端过缝"共 8 条），
绘制留给 `draw()` 在 gpui 的 `canvas` 回调里调（那里才有 `Window`）。
**把"可测的部分"与"必须靠眼睛的部分"切开**，是这类绘制代码能被测覆盖的前提。

**4）真机抓到的布局错误（又一次是"尺寸对了但用途没了"）**

第一版把变更条放进了 diff 的**标题行**，于是 `h_full()` 只等于**一行文本高** ——
变更条被压成 4×14px 的小块。而它**唯一的用途**就是表达分布，
那个尺寸等于没用（逻辑全对、单测全绿、真机上一眼看出是废的）。
改为与 diff **正文**并排、占满正文高度后正确。

**"尺寸"这类问题只有看才知道对不对** —— 与 §4.64(e) 的豆腐块、§4.64(n) 的主题
是同一族：**逻辑正确 ≠ 视觉可用**。本项目的做法是每轮至少一次真机截图。

**5）一处刻意的下限及其代价**

每格高度**至少 1 逻辑像素**：否则长 diff 里的小改动会被取整抹掉，
而"有改动却看不见"比"少显示一格"更糟（前者让用户以为没改动）。
代价是极端情况下色带总高超出表面 —— 所以必须压配平的裁剪兜住，
否则会画到相邻元素上（有测试守这两条）。
### (q) D4 工具分组：一个真缺陷藏在一个"演示桩"里，以及三处只有真机能看见的事

**1）分组判定必须在共享层，壳在渲染层**

`tool_runs` / `run_all_succeeded` / `run_in_progress` 是**纯逻辑**（对 `&[Block]`），
放 `neo-driver`；"折叠成一行、点开逐张展开"是纯展示决策，留宿主。
两个宿主必须是同一套判定 —— 否则同一个转录在 egui 与 gpui 里分组不同，
那是最难向用户解释的一类不一致。

组头色调取**组内最坏的**（有失败就是 ✗，哪怕另外 4 个成功）：
一组绿字里藏着一个失败，用户必然漏看。这是信息层次里最容易骗人的一处。

**2）"永远执行中"的卡片：缺陷在桩里，不在界面里**

真机第一版：三条调用全停在 ⏳。查会话日志才发现根因是
`tool_call()` 用 `format!("selftest-{name}")` 当 id —— 同名工具调用两次，
**id 相同**，于是三次 `ToolCallEnd` 反复认领最后一张卡，前两张永远不完成。

真实模型每次调用都给新 id，是**桩**没遵守这个契约。修两处：
- 桩加 `tool_call_n(seq, …)`，id 变 `selftest-{seq}-{name}`（演示桩想触发分组，
  就必须自己造出"同轮多次调用"这个条件，而它天然需要唯一 id）；
- 转录配对改成"**认领最早的未完成卡**"而不是"最后一个匹配的"。
  这是防御性的：provider 给了重复 id 时，界面不该出现永不结束的行
  （用户没有任何办法知道它其实早就跑完了）。

回归测试 `duplicate_ids_each_claim_their_own_card` 用旧行为跑是 **1/3 完成**，
有牙齿。

**3）三处只有真机能看见的**

- **尾部换行画成空行**：命令行输出几乎总以换行结尾，原样渲染就是卡片底部
  多一个空行，几张叠起来节奏全乱。`trim_end()` 只裁尾部（前导缩进是内容）。
- **折叠态与展开态都要看**：折叠态验"信息层次"，展开态验"卡片本身"。
- **自绘行不进无障碍树，且自动化点不动**：分组标题是 `div`，AX 树里看不到；
  而 gpui 窗口在 WindowServer 里 `bundle_id` 为空，坐标点击被拒
  （`no stable WindowServer app/window identity`）。于是照
  `NEO_GUI_PROMPT` 的先例加了 `NEO_GUI_EXPAND` —— 自绘表面的展开态只能这样验。

**4）等就绪不能靠 `sleep`（这次做成了条件等待）**

外部脚本没法问"这一轮跑完了吗"。做法：`NEO_GUI_SHOT=1` 时，在
**确实观察到运行中**之后、运行结束且无待审批时，把窗口标题改成
`NEO-SMOKE-READY`；脚本轮询标题（有总超时）。中间那一步"先确认运行过"
是必需的 —— 只判断"不在运行"的话，提交与 `TurnStarted` 之间那一帧会被误判成已完成。

**5）折叠状态按块下标记账 → 必须与 `clear_view` 同址**

`expanded_tool_runs` 起初放在宿主里；而 `clear_view` 会清块。清屏后块下标
从 0 重来，残留下标会与新块撞上，某个组莫名其妙是展开的（只在"清除后恰好
又生成了同下标的组"时出现，最难查）。移到 `Transcript` 里与
`collapsed_reasoning` 并列，`clear_view` 一并清（有测试钉住）。

---

---

### (r) gpui 宿主此前**无法输入任务** —— 比任何 D 项都硬的拦路石

**症状**：gpui 宿主的"输入区"一直是**纯展示的一行 `div`**（照 egui 的样子画的），
键盘打了字不进任何地方。于是它只能靠 `NEO_GUI_PROMPT` 喂任务 ——
那对人是不可用的，egui 侧却一直是真 `TextEdit`。

**这是"看起来对"骗过验收的又一例**：占位符文案、`发送` 按钮、审批阻塞提示
全都齐了，截图也挑不出毛病（§4.64(e) 豆腐块、§4.64(n) 主题、§4.64(p) 尺寸之后
的第四次同族）。**"能不能真的用它干活"与"看起来像不像"是两个验收条件**，
先前几轮只验了后者。

**接法**（组件库 `input::Input` + `InputState`）：
- `InputState::new` 要 `&mut Window`，而视图实体先于窗口存在 → **惰性创建**
  （第一次渲染时补上），存 `Option<Entity<InputState>>`。
- 事件靠 `cx.subscribe_in`；**`Subscription` 必须持有** —— 一 drop 就退订，
  症状是"打字没反应 / 回车不提交"。这是 gpui 里很容易漏的一条。
- 值同步：`InputEvent::Change` 时把 `state.read(cx).value()` 抄进普通
  `String` 镜像（`submit()` 与冒烟钩子都用它，不必到处传 `window`）。

**清空输入框必须只有一处**：起先回车在订阅里清、`发送` 按钮走 `submit()` 里清；
而 `submit()` 只清 `self.input`（不清 `InputState`）→ **点按钮提交后输入框里还留着
上一句**。用户会以为没发出去、再点一次，同一句话提交两遍。
抽出 `clear_input_box(window, cx)`，三条入口（回车 / 按钮 / 冒烟钩子）全走它，
`submit()` 自己不再清任何东西。

**真机验证的办法（键盘投不进这个窗口）**：
合成键盘事件到不了 gpui 窗口（`bundle_id` 为 null → WindowServer 路由不进去，
`key`/`left_click` 的坐标点击都被拒）。改用**无障碍元素级**手段：
1. `type` 打到 `textfield 任务输入` → AX 树里读回
   `任务输入 = 用真实输入框提交这条任务`，证明**值同步通了**；
2. `perform_action AXPress` 按 `发送` 按钮 → 会话日志里出现
   `user_submitted {text: "用真实输入框提交这条任务"}` + `agent_message_done`，
   证明**提交链路通了**（日志是比截图更硬的证据：截图只能证明画出来了）。

**仍未能自动化验证的一条**：回车提交。它走的是 `InputState` 内部的
`PressEnter` 事件，只能靠真键盘触发。已读源码确认单行输入框会 `cx.emit(PressEnter)`
且**不会**插入换行（`insert_newline = is_multi_line() && ...` → 单行为 false），
所以订阅能收到；但"按下回车真的提交了"这件事**没有机器证据**，
只有代码推理 —— 记在缺口里，不假装验过。

---

### (s) D3 思考轨迹搜索：三条"搜索必须诚实"的规则

**1）只搜思考块，不搜全文。** 工具输出动辄上万行，一搜就是全量扫描，
而用户要找的几乎总是"自己想不起来的某段推理"。范围收窄是有意为之，
不是没做完 —— 讲清楚比"什么都能搜但很慢"更好。

**2）命中块必须自动展开。** 折叠的块会把命中的字藏起来：用户看到
"命中 3 处"却在屏幕上找不到任何一处，只能怀疑功能坏了。
搜索结果的**数量**与可见的**内容**必须对得上。

**3）无命中要给明确提示，不能静默。** "搜了没反应"和"功能坏了"在用户那里
是同一件事。有查询就必有一行结果：命中 N 处（灰）或没有匹配（黄）。

**大小写不敏感，但…**：`to_lowercase()` 可能**改变字节长度**（如
`İ` → `i̇`），此时基于小写副本算出的区间会与原串**错位** ——
高亮会画到别的字上，比不亮更糟。做法：仅当小写副本与原串**字节长度相同**时
用它定位，否则退回原串精确匹配（宁可大小写敏感，也不给错位区间）。
区间另需落在 `char` 边界上（中文查询最容易踩），有测试专门钉住
`&text[range] == "缩进"`。

**低成本的验证方式**：`demo` 桩的推理文本里"缩进"出现两次、且句子够长，
正好当搜索的真机样本 —— 不必为搜索单独造桩。

---

### (t) 键盘投不进去不是 gpui 的问题，是**没打包**的问题

**症状**：整个 GUI 阶段，自动化始终无法向 gpui 窗口投递键盘 ——
`type` 报 "app_ref did not resolve to a unique live application"、
坐标点击报 "no stable WindowServer app/window identity"。
当时的结论是"gpui 窗口没有稳定身份，这条路走不通"，于是加了一堆
环境变量钩子（`NEO_GUI_PROMPT` / `NEO_GUI_PANEL` / `NEO_GUI_EXPAND`）
来绕开它。

**真正的原因**：直接执行 `./target/debug/neo` 的进程**没有 `bundle_id`**
（`list_apps` 里显示 `bundle_id: null`）。macOS 的自动化按应用身份路由事件，
没有身份就没有投递目标 —— 这与 gpui 毫无关系。

**修法**：`scripts/make-app.sh` 打成最小 .app，给
`CFBundleIdentifier = dev.neokernel.neo`，ad-hoc 签名。之后：
- `type` 走 `foreground_event` 成功投递；
- `key return` 真的触发了提交（会话日志 `user_submitted`）。

**教训**：把"工具链/环境限制"误判成"产品限制"，会让人去绕路（我加了三个
环境变量钩子），而绕路方案还会被写进文档当成事实。**遇到"自动化做不了"
先问一句"是我跑的方式不对吗"** —— 尤其当"产品真的不支持"这个结论
意味着用户体验有硬伤时（无法键盘输入对 GUI 是致命的）。

**副产品**：`NEO_GUI_FOCUS` 用 `FocusHandle::is_focused` 报告焦点状态。
为什么不用截图判：焦点唯一的可见证据是**光标**，而光标会闪 ——
单张截图 catch 到熄灭帧会得出"没聚焦"的错误结论，catch 到亮帧也证明不了
"一直聚焦"。读句柄是确定性的。**报告点必须放在消费意图之后**，
否则首帧会打印 false（那只是"还没处理"，不是"没聚焦"）。

**`InputState::focus_handle()` 有个坑**：它与同名**字段**并存，字段遮蔽方法，
所以必须经 `Focusable` trait 调用（`Focusable::focus_handle(&*state, cx)`）。

---

### (u) D7 "只做了一半"的三处 —— 面板指了条死路、刹车缺失、以及目标不会自己往前走

**查法**：对比两个宿主发出的 `Op` 集合（`grep -oE "Op::[A-Za-z]+"` 各自取
`sort -u` 再求差），比逐个凭感觉找可靠得多。差集直接暴露了 `GoalAdvance`。
**这是本类问题值得固化的检查手段**：两个宿主消费同一份协议，
理应发出同一套 Op —— 差集就是"功能缺口"的清单。
 —— 面板指了条死路，控制按钮缺失

对比 egui 宿主的字段表才发现 gpui 侧 D7 缺两块，**而两块都是"看起来完成了"的**：

**1）没有设定目标的入口。** 面板上写着「未设定（用 `/goal` 设定）」——
那是我照着 egui 的文案抄的，可 `/goal` 是 **TUI 的命令行语法**，
GUI 里根本没有输入它的地方。用户被告知了一条**走不通的路**，
比什么都不说更糟（他会先试几次，再怀疑是自己不会用）。

**2）没有暂停/恢复/清除。** 目标一旦立起来就**持续自动推进、花真钱**，
而 gpui 侧只能靠重开窗口才停得下来。缺的不是装饰按钮，是唯一的刹车。

**3）不定期发 `Op::GoalAdvance`（最隐蔽的一处）。** 这是"由看到最新快照的
一方驱动"约定（与 egui/Web 同构）里的那一环。缺了它的表现是：目标设定成功、
子任务清单正确显示、按钮都能按，**但永远只有第一个子任务会跑**。
界面上没有任何一处是错的，只是停在那里不动 ——
"看起来完全正常，只是不工作"是最难靠肉眼发现的一类缺陷。
补上后真机验证：子任务 1 走完规划→执行→审查→复盘四轮，自动进入子任务 2。

**教训**：D 项的"完成"要以**用户能不能走完那条路**为准，不是"界面上有没有
对应的文字/卡片"。我之前的验收只看了"目标卡片画出来没有"。
**照抄另一个宿主的文案时要连它的前提一起想** —— egui 那句文案成立，
是因为 egui 侧真有 `/goal` 之前的输入路径（它有 goal 输入框）。

**修法**：`TextareaState`（多行，目标天然多行）作输入 + `Op::GoalSet`；
控制按钮走 `goal_action(Op)` 而不是宿主自己改 `goal`（宿主自改会与内核
快照漂移）。真机验证方式是**读会话日志**：`goal_set` → `goal_updated`
（内核拆出子任务）→ `goal_pause` → `goal_resume` → `goal_cleared` 逐条对上，
且按钮文案跟随内核状态在"暂停/恢复"间切换（文案不跟随是另一个常见假象）。

---

### (v) 用"集合差"找静默缺口：内核发了、宿主没接的事件

`Op` 差集（§4.64(u)）之后，对**事件**做同样的事：列出协议里全部 `EventMsg`，
再剔除内核从不发出的历史/别名变体，剩下的差集就是"真的会来、但没人处理"的。

结果两条：`RefsResolved`、`InstructionsLoaded`。
后果是用户**看不到 `@文件` 到底读到没有**（引用了不存在的文件与引用成功
在界面上长得一样）、也看不到这次会话受哪些 `AGENTS.md` 约束、
更看不到指令是否被 32 KiB 截断。而 TUI 宿主一直都有这两行 ——
**GUI 用户的信息比终端用户还少**，这是"能力回退"，不是"还没做"。

落点选**共享转录层**（不是各宿主各加）：两个 GUI 宿主一次到位，
且文案与 TUI 同源（各写各的必然漂移出"同一条引用在两处说法不同"）。
三处刻意的取舍都写了测试：
- 正文**不进**转录（几百行文件正文会把对话挤没，那是模型上下文不是对话）；
- 引用摘要**每行一条**（合并成一行就分不出是哪条没读到）；
- 截断**必须显式告警**（静默截断会让用户以为模型看到了完整约定，
  而他的规则可能正好在被切掉的那部分里）。

**排查手段小结**：两个宿主消费同一份协议，理应发出同一套 `Op`、
处理同一套 `EventMsg` —— 所以**差集就是缺口清单**，比逐个凭感觉找可靠得多，
而且能发现"界面上看不出来"的那类（如不工作但长得正常的 `GoalAdvance`）。

---

### (w) 高 DPI 实测：2x 与真 1x 各验一次（含"怎么才切得动"）

**结论**：两档文字都清晰、中文正常、无 1px 错位或重影（同区域放大对比过）。
真 1x 下窗口 1080×752 点 == 1080×752 像素（点与像素 1:1），
2x 下同区域是 2160×1504 —— 渲染确实走的是原生矢量而非位图放大。

**踩过的坑：显示模式切换撑不过一个进程的退出。**
`CGDisplaySetDisplayMode` 在**调用它的那个进程退出时**被系统还原 ——
所以"切完就退出、下一条命令再截图"永远截到 2x（我这样白试了两轮，
两张图尺寸一样才发现）。正确做法是**让切换进程存活到截图完成**
（临时写了个 `hold-mode` 小程序，切完 sleep 到超时）。这是"我跑的方式不对"
而不是产品问题的又一例（同 §4.64(t) 的打包问题）。

**诚实边界**：本机是 Retina 内置屏，只有 1x / 2x 两档。
方案要求的"100/125/150/200% 四档"在 macOS 上落不成四档 ——
fractional scaling（如 125%）走的是"以更高分辨率渲染再缩放"，
与整数倍是**不同机制**，不能拿 1x/2x 的结果推断它。
这条仍记在缺口里，不假装覆盖。

---

### (x) IME 回归用例：先解决"能不能自动化"，用例本身只有三条

**先试路，再写测**。IME 的困难不在断言，在**根本没法驱动** —— 它由系统输入法
进程调用我们，脚本模拟不了。找到的入口是 gpui 公开的 `EntityInputHandler`：
`replace_and_mark_text_in_range` 就是 preedit 的入口，而组件库自己的测试
就是这么驱动的（读它的源码学来的，不是猜的）。

**踩到的三个坑**（都是"跑的方式不对"，不是产品问题）：
1. `#[gpui::test]` 宏展开时写死 `gpui::` 路径 → 测试文件里需要
   `use neo_ui_kit::gpui;` 做别名。**这不是新增依赖**（U2 守的是
   Cargo.toml 里的声明），门面 crate 内部同样这么做。
2. 组件库需要全局 `Theme` → 每个测试先 `cx.update(neo_ui_kit::init)`，
   否则 panic "no state of type Theme exists"。
3. **`pub use gpui_kit::test;` 会让本 crate 编译不过** —— 那个模块里有一个
   glob 引入的 GPUI `test` 宏，再导出会遮蔽 Rust 的 `#[test]`，
   报 "recursion limit reached while expanding `#[test]`"。
   上游注释其实提醒过，我没先读。改 `pub use ... as kit_test;` 解决。

**只写三条**（写多了是浪费 —— 契约面就这么大）：
1. preedit 删除后重输不崩且文本干净（`n→ni→你` → unmark → clean → 重输）；
2. 连续合成按顺序拼接（中文输入的常态）；
3. **测试基建自证**：能按 id 观察到真渲染出来的输入框。
   第三条的必要性：若连"找到渲染出来的输入框"都做不到，
   前两条即便全绿也可能是**在测空气**。用故意引入错误的方式验过牙齿。

**诚实边界**（写在测试文件头部，不让读者高估它）：
这里验的是**组件契约**（输入法调我们那几个回调时我们是否按契约应答），
**不是真输入法**。真输入法还可能在奇怪时机连发 unmark / 重发同一 range，
那些只有真机才遇得到。它的价值在于：gpui-kit 升级若悄悄改了
`replace_and_mark_text_in_range` 的语义，这里会红。

---

### (y) 我自己引入的一个缺陷：测试基建编进了生产构建

给 IME 用例开 `test-support` 时，我把它写在了**根 Cargo.toml 的 pin** 上：
`gpui-kit = { version = "0.6", features = ["test-support"] }`。
测试确实过了，但那条写法会让 `gpui-kit` 的测试基建（无头窗口、
事件派发、proptest）**编进所有构建，包括 release 二进制**。
测试代码出现在发布产物里是纯粹的浪费，而且没人会注意到。

**修法**：正确的分层是
- 根 pin 只写版本，**不写 feature**（并留注释说明为什么不要写）；
- `neo-ui-kit` 加一个默认关闭的 `test-support` feature 转发给 gpui-kit，
  并把 `pub use gpui_kit::test` 一并 `#[cfg]` 门控；
- 只有需要它的测试在**自己的 `[dev-dependencies]`** 里打开。

**验证方式是反证**：把 dev-dep 的 feature 去掉，测试**必须编译失败**
（实测 `unresolved import neo_ui_kit::kit_test`）；加回来才通过。
这同时证明了"测试之所以能过是因为基建真的开着"，
而不是靠别的路径碰巧通过 —— 否则"生产构建干净"这个结论本身就不可信。

**教训**：往 `[workspace.dependencies]` 里加 feature 会**影响全仓所有构建**，
而 `[dependencies]` / `[dev-dependencies]` 里加只影响一处。
"改一行、测试过了"不是终点 —— 要问**这一行被谁读到了**。

---

### (z) Linux 构建：本地做不到，但把"能本地查的那部分"查了

**试过交叉检查**：`rustup target add x86_64-unknown-linux-gnu` +
`cargo check --target x86_64-unknown-linux-gnu` → 失败在
`cc-rs: failed to find tool "x86_64-linux-gnu-gcc"`。
这是**环境限制**（macOS 上没有 Linux C 工具链），不是代码问题 ——
只要依赖树里有任何一个走 cc-rs 的 crate，这条路就过不去。
**结论：Linux 只能靠 CI 首跑验**，别再把时间花在本地模拟上。

**但有一类 Linux 崩溃能在本地查**：我们自己的 crate 里有没有
"只对 macOS 成立"的假设。查法：
```
grep -rn 'cfg(target_os\|cfg(unix)\|cfg(target_family' crates/*/src/ --include="*.rs"
```
结果（**都是有意的三平台分支，不是遗漏**）：
- `neo-sandbox` / `neo-sandbox-local`：三平台各有实现（Linux 受限档位
  fail-closed，这是设计）；
- `neo-host-egui/src/fonts.rs`：macOS / Windows / `all(unix, not(macos))` 三份
  字体候选表（该宿主即将删除）；
- `neo-web/auth.rs`、`neo-providers`、`neo-sandbox-local` 的 `cfg(unix)`：
  读 `/dev/urandom` 之类的 unix 专有路径，有非 unix 回退。

**UI 栈（`neo-ui-kit` / `neo-ui-render` / `neo-ui-behavior` / `neo-ui` /
`neo-host-gpui`）里平台相关 cfg 数量为 0** —— 这正是它们应当的状态
（中性绘制描述 + 后端翻译）。这也是 Linux 风险最集中的地方本来就在
**依赖层**（gpui-pre-linux、x11rb、wayland dlopen）而非我们的代码里的证据。

---

### (aa) D1 状态/改动行数：原判"需要扩展元数据"是**过时的**

D1 那一行一直写着"需要会话元数据里有工作区与运行状态，当前 `SessionStore`
只存 id/标题/记录数，故不做"。**这个判断是错的** —— 我去读了一份真实日志：

```
op    user_turn
event turn_started          ← 开了头
event tool_call_end
event files_changed         ← 改动行数就在这里
event turn_complete         ← 收了尾
```

状态与改动行数**日志里本来就有**，只是 `SessionStore` 没读。
判"需要新元数据"之前先翻一份真日志，成本极低，而错判的代价是整项被搁置。

**实现要点**：
- **单遍扫描**（`read_summary`）而不是每个字段扫一遍：会话列表每次打开都要
  列全部会话，日志可能几万行。
- **改动行数取最后一个 `files_changed`，不是逐条相加** —— 内核的改动事件是
  **覆盖语义**（它自己持有累计状态），相加会把同一文件重复计入。
  有测试专门钉这一条（两次快照 3/1 → 5/2，应得 5/2 而不是 8/3）。
- **状态推断有优先级**：收尾了就不标失败（中间某个工具报错 ≠ 会话失败）。
  有测试钉住。
- **悬空的 `turn_started` 是"未完成"，不是"运行中"**。那是磁盘上的历史，
  进程早就不在了 —— 显示成"运行中"是编的。这条写进了枚举的文档注释，
  因为它太容易顺手写错。

**颜色不能是唯一载体**：TUI 里用**文字**（失败/未完成），
因为终端主题可能吃掉颜色；GUI 里用圆点符号区分形状（● 实心 / ◐ 半环），
不只靠颜色 —— 色盲用户与灰度截图下都要能分辨。

**顺手抓到一处布局缺陷**：侧栏 220px 会把 `+3 -1` 里最右的 `-1` 裁掉，
而"改动行数"恰恰是这个列表**新加**的信息 —— 新功能自己不可见。
真机截图才看出来（单测与编译都不会报）。改为 260px + 行内截断。

**类型去重**：`SessionState` 一开始在 `neo-session`（trait 定义处）与
`neo-session-store` 各定义了一份，编译直接报
`expected neo_session::SessionState, found neo_session_store::SessionState`。
**同一概念只能有一份定义**，另一处 re-export。这个错误很"响"（编译期就炸），
比它在运行期悄悄分叉好得多。

---

### (ab) 可提取性守卫：把"能开源"从声明变成每次可验的事实

方案 Phase 3 的 DoD 里有一条 **"把三个目录复制到空仓能独立编译（实际演练一次）"**
一直没做。我先手工做了一次 —— 5 个 crate（含 `neo-text`）复制到空目录、
手写最小 workspace 根、`cargo check` + `cargo test` 全过。**这条从声明变成了事实。**

然后把它固化成 `check_extractable.py` 进每次门禁。设计上有一处关键取舍：

**用 `cargo metadata --no-deps` 而不是 `cargo check`。** 实测前者对两类失败
（路径依赖逃出、workspace 继承键缺失）**都会直接报错，耗时 0.1 秒**；
后者要编 gpui-kit，1.5 分钟。**一个能跑进每次门禁的 0.1 秒检查，
比一个"应该跑但太慢所以没人跑"的 1.5 分钟检查有用得多。**
完整的 `cargo check` + `cargo test` 留在 `--full`（发布前 / CI 跑，
实测 115 个测试通过）。

**它补的是 U1–U4 守不住的那类问题**：分层守卫检查的是"依赖声明是否合法"
（不许依赖内核轴、不许两个 crate 各 pin 一份 gpui），它拦不住
"声明完全合法、搬走却编不过"。三类：
1. **路径依赖逃出提取集** —— 合法性由分层守卫的理由判定，但"搬走会断"
   是另一件事；
2. **workspace 继承键在新仓根里不存在** —— 主仓里永远看不出来；
3. **提取集不闭合**（引用了本仓独有的私有 crate）。

三者的共同点：**只在别人的机器上才会炸**。所以"我们在 CI 里编过了"
给不出任何保证。

**清单只有一处定义**：可开源集从 `check_ui_layering.py` **读**（`import`），
不在这里抄一遍。抄一遍的下场是两份清单漂移 —— 加了新 crate 只改一处，
守卫要么漏检、要么报一个不存在的失败。

**牙齿验证**：分别注入"路径依赖逃出"（应报 E2）与"workspace 键不存在"
（应报 E4），都正确变红；另测了一个**不该报**的用例（`serde` 在主仓根里有
版本 → 新仓能照抄 → 不该报），确认没有过度报警。

---

### (ac) README 写成文档测试：一次抓出 7 处我凭记忆写错的 API

Phase 3 要求每个待开源 crate 自带 README。我写完第一个 crate 的 README 后
意识到一个问题：**README 里的示例是最容易腐烂的文档** —— 没人编译它们。

做法：把 README 用 `#![doc = include_str!("../README.md")]` 整个注入 crate
文档。于是示例进入 `cargo test` 的文档测试。效果立竿见影 —— 我写的示例里
**7 处 API 是错的**（凭记忆写的）：

1. `neo_ui_behavior::Verdict` 没从 crate 根导出（它是 `verdict()` 的**返回类型**，
   不导出则用户无法 `match` 它）—— **真实的 API 缺陷**；
2. `neo_text::Lang` 同上（`syntax` 子模块 pub，但没 re-export）；
3. `Tone::Strong` 这个变体**根本不存在**（真实是 `Primary`）；
4. `Scene.ops` 是私有字段（有公开的 `ops()` 方法）；
5. `Rect::new` 收 4 个参数不是 2 个；
6. `KeyArbiter` 的方法名与返回值全写错了（真实是 `claim`/`verdict`，
   返回 `App`/`Framework`/`Ignore` 而非我编的 `Owned`/`PassThrough`）；
7. `neo-ui` 的示例把唯一入口 `apply_neo_theme` **注释掉了**，于是
   "测试通过"掩盖了"唯一的入口没被示例覆盖"。

**教训**：API 文档的示例**必须可编译**，否则它记录的是作者的记忆而不是代码。
前两个还暴露了真实的可用性缺陷（常用类型没从根导出）—— 这类问题靠读代码
不容易发现，因为"能不能 `use` 到"只有写调用方代码时才感觉得到。

**顺带记下文档测试的两条经验**：
- 会启动事件循环的示例（`application().run(...)`）**必须标 ```` ```rust,ignore ````**，
  否则文档测试会真的把图形栈跑起来并挂住（实测卡了 60 秒以上）；
- 纯文本示意图（错误信息、ASCII 框图）要标 ```` ```text ````，否则被当成 Rust 编译。

---

### (ad) 第一个自研组件：把两处手写渲染合成一个

宿主里有**两处**几乎一样的手写渲染：`styled_line`（Markdown 片段着色）与
`reasoning_highlighted`（搜索命中高亮）。都是"给一段文字的若干字节区间指定
绘制样式"，各写了一遍偏移累加与区间处理。

合成 `neo_ui::RichText` 后，职责变得清楚：
- **调用方只给内容**（片段+色调、命中区间）；
- 归并、色调翻译、区间语义三件事在组件里做一次。

**最有价值的一处发现**：底层文本系统的 `compute_runs` 是**按顺序消费**高亮
区间、并把"上一个的结束"当作"下一个的开始" —— 它**假设区间有序且不重叠**。
也就是说，把 span 与 mark 两个来源直接倒进去，重叠处会算错长度。这个约束
看不见、也没有编译错误，只有真机上"文字少了一段"或高亮错位。
组件的 `runs()` 负责把它切成**有序不重叠的最小段**，且同时保住两个属性
（前景色来自 span、背景色来自 mark）—— 有测试钉住"首尾相接、覆盖全文、
长度守恒"，因为缺一段就会丢字。

**一处刻意的取舍**：命中的段落**保留**原语义前景色，只加背景色。
用固定前景色覆盖会让搜索抹掉"这段是强调/代码/注释"的信息层次。

**验证方式**：宿主改用组件后真机截图对比，画面与替换前一致（语法高亮、
清单、引用都在）—— 重构不该改变可见结果。

### (ae) 我的演练声明比验证范围大：`cargo check --workspace` 跳过 examples

写 storybook 之后再跑演练，发现一件事：`cargo check --workspace`
**不编译 examples**（要 `--all-targets`）。而 examples 恰恰是"这个 crate
拿出去能不能用"的最直接证据 —— 展示台就是 example。

也就是说在改之前，演练报绿只证明了"库代码能编译"，**没证明展示台能被带走**。
这是"声明比验证范围大"的典型：守卫的名字与输出说的是"复制到空仓能编译"，
实际只覆盖了一部分 target。

修法：E7 改成 `cargo check --workspace --all-targets`，并在注释里写明
**为什么这不是可选的保守选择**。改完实测：示例在空仓里确实编译通过
（先手工验证了一次，再落到脚本里）。

**教训**：写"能编译"这类断言时，要问**cargo 默认包含了哪些 target**。
默认不包含的例子：examples、benches、部分 test target。默认与直觉不一致，
而它决定你的守卫是"覆盖了"还是"看起来覆盖了"。

### (af) storybook：它的价值一半在"能独立跑"，一半在"只列真实存在的东西"

展示台有两重身份（调试工具 + 开源门面），但真正的纪律是两条：

1. **它必须只依赖待开源的 crate**。若展示台需要引用宿主或内核才能跑起来，
   说明组件与业务耦合了 —— 而它现在只依赖 `neo-ui` + `neo-ui-kit`，
   并经可提取性守卫验证过能在空仓里编译。
2. **只列真实存在的组件**。没有占位条目、不预告"即将支持"。
   一旦掺入愿望清单，读者就无法分辨哪一项是真的，展示台从"可用的东西"
   变成"路线图"。当前只有 6 项（其中 4 项是同一个组件的不同能力），
   **条目少是事实，不粉饰**。

**真机抓到两处展示台自身的问题**（都不是组件缺陷）：
- **色块"隐形"**：`panel_bg` 色块与卡片底色**同值**（都是 `#201e2a`），
  于是色块看起来没画出来。我先怀疑是颜色取值错了，**用像素采样量了一次**
  （`base_bg` = `#000000`、`panel_bg` = `#201e29`，两个都正确），
  才定位到是排布问题。修法是给色块加边框 —— 让"这是一块样品"靠边框成立，
  而不是靠与容器底色有差异。**"看起来不对"时先量一次，能省掉一轮瞎猜。**
- **内容不可滚动**：条目一多，最下面几项（调色板全览、组件库组件）
  落到窗口外且页面滚不动 —— 而展示台的价值就是"全都能看到"。
  补上滚动容器。

---

### (ag) 许可证合规：把"我们只用宽松许可"变成机器可判

方案 Phase 3 要求每个 crate 带 LICENSE，Phase 2 要求 `cargo deny check licenses`。
两件事都做了，但过程里发现一个**必须留痕的判断**：

**方案要求 Apache-2.0，本仓根是 MIT，我没有擅自改。** 方案给的理由是
"避免 GPL 式 LGPL 纠结"，但没有论证为什么不用 MIT —— 两者都宽松、都允许商用，
差别在 Apache-2.0 有**明确的专利授权**条款。这是法律性拍板项（涉及专利风险
与品牌策略），不该由我按"方案里写了"就改掉已有仓库的许可证。
记为待用户决定项，当前保持 MIT。

**但有一条是硬性义务、与选哪个许可证无关**：使用 Apache-2.0 的依赖
（`gpui-kit` 及其树）必须保留其版权声明。这条现在还没做（NOTICE），
因为完整的 NOTICE 要覆盖上千个包 —— 应当由生成器产出而不是手工堆。

**已做的**：`check_licenses.py` 进每次门禁 + `deny.toml` 供 CI 用完整能力。

判据不是"法律上能不能用"，而是"会不会给使用者带来义务或风险"：
宽松许可放行、MPL-2.0 放行（文件级）、GPL/AGPL 拒绝。

**最容易搞错的是 SPDX 语义**：
- `A OR B` → **任一**满足即可，所以 `Apache-2.0 OR GPL-2.0-only` 是**宽松的**
  （使用者自己选 Apache）；
- `A AND B` → 全部都要满足；
- `A WITH E` → 整体是一个标识符（`Apache-2.0 WITH LLVM-exception`）；
- **老式斜杠写法** `MIT/Apache-2.0`、`Apache-2.0/MIT` 与 `MIT OR Apache-2.0` 同义。

我第一版实现的正则用了前瞻断言，把斜杠漏掉了，**59 个正常依赖被误判为违规**。
修正后 1052 个包全部通过。**这类"检查器自己写错"的问题很危险**：
它不会报错，只会给出一个错误的结论，而人看到"59 个违规"的第一反应是去改依赖
而不是怀疑检查器。**修检查器时也要给它做牙齿测试**（我把白名单里的
Apache-2.0 去掉，确认它正确报警）。

---

### (ah) 许可证落地：可开源集切到 Apache-2.0（用户拍板），以及三处只有认真做才会撞到的细节

用户拍板"可开源部分用 Apache-2.0"，于是提取集 5 个 crate 从"继承根的 MIT"
改为显式 Apache-2.0。留下的技术结论：

**1）混合许可是可行的，但只能靠"显式声明"。** Cargo 不支持"继承 workspace
的 license 但覆盖成别的" —— 要改就只能不继承、写明 `license = "Apache-2.0"`。
实测确认：同一个 workspace 里 `a` 继承 MIT、`b` 显式 Apache-2.0 完全合法。
于是仓库里现在是两种许可（提取集 Apache-2.0 / 宿主与内核 MIT），
而 `check_extractable.py` 的"继承键"收集逻辑**自适应地**不再要求 `license`
（因为它扫的是"crate 里有没有 `xxx.workspace = true`"）—— 这一步没有手工调整，是对的。

**2）许可证原文绝不凭记忆敲。** 从 Rust 工具链自带的
`LICENSE-APACHE` 取（Rust 项目本身就是 Apache-2.0，那份是标准全文），
并校验结构标志（9 个条款齐全、含 APPENDIX）后才复制。
**校验时不要断言 URL 是 `http://`**：Rust 项目做过 http→https 迁移，
把 URL 写进断言只会让校验变脆（我第一版就因此报错，而错的是断言不是文本）。

**3）NOTICE 义务：实测不触发，但必须留痕说明"查过"。** Apache-2.0 §4(d)
要求"若原作品带 NOTICE 则保留其归属声明"。我逐个检查了提取集里 38 个
必须采用 Apache-2.0 的依赖 —— **没有一个带 NOTICE 文件**，所以义务不触发。
关键是**把它写进产物**（THIRD-PARTY-LICENSES.md 的对应章节），
让读者知道"我们查过且结论是不需要"，而不是"我们漏了"。

**4）算 NOTICE 范围要按"实际会采用哪个许可"，不是"表达式里含 Apache"。**
初算得到 620 个包，而其中绝大多数是 `MIT OR Apache-2.0` —— 那种情况**我们选
MIT**，不产生 Apache 义务。收紧到"所有可选组合都含 Apache-2.0"后是 38 个。
这个区别很实在：620 行清单没人会读，38 行的表才有用。

**5）三处"检查器自己坏了"**（这是本轮最值得记的教训，同 (ac)(ag) 同一族）：
- `check_licenses.py` 里我插入的代码用了尚未定义的 `failures`，于是它
  **先打印 PASS 再崩溃**。我第一轮牙齿测试只 `grep` 输出、没看退出码，
  差点把"崩溃"当成"通过"。**牙齿测试必须检查退出码，不能只看输出文本。**
- 生成器的 `find_registry_dir` 按"最后一个 `-`"切包名，遇到带 build metadata
  的版本（`0.4.0+sdk-1.4.341.0`）就匹配不上，把已下载的 spirv 报成"未能检查"。
- `check_licenses.py` 的 SPDX 正则漏了老式斜杠写法（见 (ag)），59 个包误判。

三处的共同点：**检查器出错时不会报错，只会给出一个错误结论**，
而人看到结论的第一反应是相信它、去改被检查的对象。

**6）清单必须能自检是否过期。** 加了 `--check` 模式进门禁：依赖一变
（跑 `cargo metadata` 对比），清单过期就红。没有这一步，THIRD-PARTY-LICENSES.md
会在第一次 `cargo add` 之后悄悄变成一份错的信息。

### (ai) 自动跟随：一个"纯逻辑测试全绿但真机永不生效"的符号错误

补的是 gpui 相对 egui 的一处**真实回退**：egui 转录区有 `stick_to_bottom(true)`
（流式输出时视图跟着走），gpui 侧没有。现象是内容长起来之后，用户得手动往下拖
才能看到新出的字。

**1）语义不是"总是滚到底"，而是"只在用户本来就在底部时才跟随"。**
前者会毁掉读历史的能力：流式输出持续几秒到几分钟，那段时间里用户往上翻
会被不断拽回去。这个判断与渲染框架无关，所以放 `neo-ui-behavior::scroll`，
做成**可注入位置的纯函数**。

**2）关键的技术障碍：组件库的 `overflow_y_scrollbar()` 拿不到滚动句柄。**
它内部自己创建并持有 `ScrollHandle`。要用 `track_scroll(&h)` +
`overflow_y_scroll()` + `vertical_scrollbar(&h)` 三项组合自己持有
（组件库内部也是这个组合，区别只在句柄归谁）。

**3）真机才抓到的符号错误（本轮最值钱的一条）：**
gpui 的 `ScrollHandle::offset()` 返回**负值**（往下滚为负，到底时
`offset == -max_offset`），而 `max_offset()` 是正的。我的判定写成
`offset >= max_offset - tol` ——于是把原始值直接喂进去，条件**恒为假，
自动跟随永远不生效**。

而 9 条纯逻辑单测**全绿**：它们构造 `ScrollPos::new(1000.0, 1000.0)`，
两个输入都是正的，符号问题在那一层根本不存在。

修法不是在调用点写个负号（那会留下"以后再有人接线时再踩一次"的坑），
而是让**符号约定只在一处表达**：`ScrollPos::from_raw(raw_offset, raw_max)`
负责翻正。调用方不可能搞错，因为类型要求它经过这里。

**验证方式**：加了 `NEO_GUI_SCROLL` 诊断（与 `NEO_GUI_FOCUS` 同族），
把**框架的真实返回值**打出来：

```
scroll raw_offset=-8px raw_max=8px -> offset=8 max=8 at_bottom=true follow=true
```

`raw_offset=-8` 是负的、`raw_max=8` 是正的 —— 这就是符号约定的现场证据。
若没走 `from_raw`，`offset` 会是 -8，判定恒假。**这类错误只有打印真实返回
值才能确认**：截图看不出（静止画面）、单测看不出（输入是自己造的）。

**4）我还砍掉了一段自己写的过度设计。** 初版加了 `FollowTailWithGrace`
（"抑制窗口"，防后端滚动动画的中间帧被误判成用户滚离）。读源码发现
`scroll_to_bottom()` 是**立即跳转、没有动画**（直接把 `scroll_offset.y`
设为 `-scroll_max.y`），于是那段抑制窗口没有存在理由 —— 而且它自身有个
bug（跟随期间每帧 `mark_followed` 会让窗口永久生效，用户往上翻再也停不下来）。
**先读实现再写容错**：凭"可能后端有动画"的猜测加复杂度，会同时引入无用代码
和新的失败面。

**5）视觉验证的局限与价值。** 用合成滚轮向上滚 → 截图 → 等 2 秒再截图：
两张**完全一致**，证明没有被拽回底部。这是视觉上能验的部分。
但"跟随是否生效"在静止截图里只能靠"内容有没有显示到末尾"间接判断 ——
所以真正的确定性证据是上面那条日志。

---

### (aj) 模型选择器：把"左右盲切"换成"列出全部可选"

gpui 侧第二处真实回退：egui 的模型是**下拉框**（列出全部 + 悬停显示说明 +
桩标"（桩）"），gpui 侧只是一个左右切换的按钮 —— **看不到有哪些模型可选，
更看不出哪个是桩**。用户切到 `mock` 会以为模型坏了（它回固定文本）。

**1）装配层丢信息。** 根因在装配层：给 gpui 传的是 `Vec<String>`（只有名字），
egui 拿的是 `Vec<(String, String, bool)>`（名字/说明/是否生产可用）。
"传什么"决定了"能显示什么"—— 只传名字的宿主**根本没有信息可标**。

**2）加具名类型 `ModelChoice`。** `(String, String, bool)` 在调用处只能写
`models[0].2`，读的人得回翻定义才知道那是"是否可用于真实任务"。
改成具名结构 + `display_name()`（桩自动加"（桩）"后缀），
放在共享层 —— 两个 GUI 宿主 + TUI 展示同一份清单，"桩要标出来"这条规则
必须一致，各宿主自己拼会漂移。

**3）两个入口做同一件事、行为不同，比只有一个入口更糟。** 原来
`A::ShowModels`（命令面板里的动作）是"循环到下一个"，我加了选择器之后
它就成了第二个入口。统一到选择器：盲切想切到第 3 个要按 2 次，而且途中
会经过桩模型 —— 真按下去就切过去了。

**4）`next_model` 及其三条测试随之删除。** 这是个好信号：功能被更好的
交互取代时，旧实现应该**消失**而不是留着（留着就是"两处实现同一个行为，
改一处忘一处"）。删掉后那个 `#[cfg(test)] mod tests` 变成空的，也一并删了。

**真机验证**：选择器列出 5 个模型、桩全部标"（桩）"、各自带说明、当前项有
● 高亮；点选"深度求索"后会话日志出现
`configure_session {model: "深度求索"}` + `model_switched`，状态栏同步变化，
提示"已切换到 深度求索"。**日志是比截图更硬的证据**（截图只能证明画出来了，
日志证明内核真的收到了）。

---

### (ak) 帮助面板：顺便抓出共享命令表里一条"猜实现"的描述

egui 有帮助面板（快捷键 + 11 条命令），gpui 只有一行提示（会被下一条提示冲掉，
用户来不及看完）。补成面板。

**关键做法：命令清单从共享表读**（`neo_driver::commands::COMMANDS`），
不手写。手写清单必然在加命令时忘记更新 —— 而"列了但按不出来"与
"能按但没列"都是**错误信息**，比没有清单更糟。真机验证：面板显示
"命令（12 条）"，与表里的实际条数一致。

**顺带抓到一处**：共享命令表里 `/model` 的描述写的是
"切换模型（**状态栏的模型下拉**）"。而两个宿主都没有"下拉" ——
egui 侧是组合框、gpui 侧是选择器面板（前一个提交刚做的）。
这个描述是**对实现的猜测**，而命令表是三个宿主共享的，
写字面控件名等于在三个地方说了三种都不完全对的话。
改为"打开模型选择器"（描述行为，不描述控件）。

**教训**：共享层的文案不要描述**具体控件形态**，只描述**用户可见的行为**。
控件形态是宿主的选择，会变；共享文案的生命周期比它长。这与
`Tone`（语义角色）不写具体色值是同一个道理 —— 不同层次的抽象要各守本分。

---

### (al) 侧栏折叠：以及"点击投不进窗口"时怎么拿到确定性证据

补上 gpui 相对 egui 最后一项差异：egui 的侧栏可折叠（`sidebar_open`），
gpui 侧是固定面板。补了状态字段 + 状态栏可点开关（与 `/sessions` 命令
同一个 Action，两个入口等价）。

**验证过程值得记**：自绘元素不在无障碍树里，只能坐标点击；而坐标点击
反复失败 —— 排查后发现是**窗口被其它应用抢到前台**，我点到了别的窗口上。
（截图截到的是 ZCode 而不是 NEO，这正是"点到别处去了"的现场。）

**改用程序化验证**：加 `NEO_GUI_SIDEBAR=off` 启动钩子（与既有
`NEO_GUI_PANEL` / `NEO_GUI_EXPAND` 同族 —— 自绘界面收不到合成输入时，
用环境变量驱动一次以便核对），然后**直接采样像素**：

- 展开态面板区：`#0e0e0e`（面板底）+ `#8a8a93`（文字）
- 折叠态面板区：`#000000`（纯页面底，245000 像素全同色）

这比"看一眼截图说变了"可靠：**它是可判定的**（同色占比），
不依赖我对图像的目测。

**顺带一个教训**：采样区域的坐标要除以设备像素比。我第一版用逻辑点
（窗口 1080×752）去 `crop` 图像（2160×1504），采到的是完全无关的位置 ——
两张图都"全黑"，差点得出"折叠没生效"的错误结论。
**图像坐标与逻辑坐标是两套坐标系**，混用会得到看似合理但完全错误的测量。

---

### (am) gpui 转正：`neo desktop` 默认换宿主

阶段 3 的核心动作。切换前逐项核对 egui 的能力（不靠印象）：
帮助面板、侧栏折叠、退出、命令面板、会话栏、命令台、模式切换、模型切换 ——
**只有侧栏折叠缺**，前一个提交补掉了。gpui 另在三处领先（思考轨迹搜索、
工具调用分组、自动跟随语义更精细）。

**切换的四个动作**：
1. `gpui` 进 `default` feature（此前刻意不进 —— 那条注释"能力还远未对齐"
   已经过时，连同注释一起改了）；
2. CLI 分支：默认走 gpui，加 `--egui` 作回退；
3. `--gpui` **保留但变成无操作**，不删 —— 脚本与文档里用过它，
   删掉会让 `neo desktop --gpui` 报"未知参数"，而它现在正是默认，
   报错会让人以为这个选项坏了；
4. README 三处描述（"egui 是默认"、"gpui 刻意不进 default"）同步更新。
   **文档与实际不符比没有文档更糟** —— 那会让人按错的方式使用。

**egui 仍留在 default feature 里**：它还没删，`--egui` 是回退通道 ——
留一条退路比"切了就回不去"稳妥。删它是阶段 3 的下一步。

**验证方式**：**读启动横幅**（`[neo] 桌面窗口（…）`），三条路径各跑一次：
- 不带标志 → `GPUI；--egui 可切回旧实现`
- `--egui`   → `egui 旧实现；它已冻结，只修 bug`
- `--webview`→ `webview）http://127.0.0.1:…`

**为什么用启动横幅而不是截图**：这轮验证里我反复被"窗口被其它应用抢到前台"
干扰 —— 截图截到的是别的应用，坐标点击也点到别的窗口上，白费了好几轮。
而启动横幅是**进程自己打印的**，不受前台归属影响，一次就是确定的答案。
**验证手段要选那些不受环境竞争影响的。**

---

### (an) 删 egui：我**没有**做，以及为什么这一次该停下来问

阶段 3 的清单里还写着"egui 删除"。我没有执行，理由不是懒：

**1）它与我在同一个提交里刚写的理由直接矛盾。** 转正提交里我写的是
"egui 仍留在 default 里：`--egui` 是回退通道 —— 留一条退路比'切了就回不去'
稳妥"。转正后立刻把退路拆掉，等于那句话没有意义。

**2）没有任何真实使用数据支持"gpui 可以独立了"。** 我对 gpui 的验证都是
**我自己在真机上按脚本走的**（打一段字、点一次模型、滚一次页）。
那证明了"这些路径能用"，**没有**证明"连续用几个小时不会出问题"。
egui 是唯一有完整真机验证历史的实现（D2–D11 全流程 + 授权后的视觉验收）。
删掉它，下一个问题出现时就没有对照物了（"是 gpui 的问题还是内核的问题"
会变成猜）。

**3）破坏性且不可逆。** 删除代码不像改配置 —— 恢复要翻 git。项目自己的
Phase 4 触发条件写的是"主应用已稳定上线 ≥ 6 个月"，那条纪律对删除同样适用。

**所以我把"什么时候可以删"写成可判定的**（而不是"等它稳定"这种没法执行的
描述），放在缺口清单里：三条全满足才删。这是我能做的推进 ——
**把感觉变成一个可以逐条打勾的清单**。

**顺带验了这次改动的直接风险**：`gpui` 进 default 之后，
`--no-default-features` 是否还能构建（发布流程的 Linux 版用它）。
本地跑通 —— 这条不验的话，"发布时才发现精简版编不过"是典型的时间点错配。

---

### (ao) 转正后必须补的两件事：默认路径的完整验证 + Linux 运行时库

换默认宿主不是改一行 feature 就完了 —— 它把**从未走过的组合**变成了默认路径。
我补了两件事：

**1）默认宿主上的完整闭环验证。** 此前 gpui 的验证是分开做的（打字、审批各验过），
但"默认模式下 selftest 触发审批 → 显示 diff → 点允许 → 文件落盘"这条**完整**链路
只在 egui 上走过。补验结果：状态栏"待审批"、`apply_patch` 卡片、**diff 带绿色 + 行**、
审批对话框（`类别: write` + 三档按钮）、输入区被阻塞，点"允许"后文件真实落盘
（48 字节、内容正确）、审批按钮消失、输入框恢复可编辑、标题变为 READY。
**证据以会话日志为准**（`tool_call_end exit_code=0` + 文件实际存在），
截图只作辅助。

**2）Linux 运行时库：一个"编译能过、启动才炸"的坑。** 查 `xkbcommon` 的实现：
它**没有 build.rs**，通过 `xkbcommon-dl` 做 **dlopen（运行时加载）**。
也就是说 Linux 上编译含 gpui 的版本**不需要** `libxkbcommon-dev`，
但运行时缺库会直接：

```
Library libxkbcommon.so could not be loaded.
```

这是"编译期与运行期的依赖不一致"的典型 —— 而我的 README 原本写着
"Linux 产物是精简版（不含桌面窗口，因它需要 libwebkit2gtk）"，
**在 gpui 转正后这句话不完整了**（现在有第二组系统库需求，且机制不同：
webkit 是构建期 pkg-config，xkbcommon 是运行期 dlopen）。已改写成
两组需求分开说明，并写明"Linux 桌面产物尚未验证"。

**教训**：换默认值时要问"**谁的计算前提变了**" —— feature 变了、文档没变、
CI 没变、发布流程没变，而它们都依赖那个默认值。

---

### (ap) 渲染缝的第二个消费者：用量条形图（以及三处只有真机/真数据能发现的）

方案 Phase 4 的触发条件是"`GpuiBackend` 之上有 ≥3 个自绘组件稳定运行"。
加了第二个：**用量条形图**（缝的第一个是变更条）。选它的理由是数据在手边
（每轮 token）且"趋势"用文字表达不了 —— 一条曲线比几十个数字更快看出
"某一轮陡增"。

**1）三处靠"真数据/真机"才发现的问题：**

**(a) 所有桩都不报 token，图表离线永远画不出来。** `TurnComplete` 的
`input_tokens` 恒为 0，而我的组件对"全零"有意不画（避免一排齐平空柱子
造成误解）—— 于是侧栏只有"用量（1 轮）"标题和一片空白，**看起来像坏了**。
加了两件事：一个报 token 的演示桩（`--provider usage`），以及
"本次未报告 token 用量（桩 provider 不报）"的说明文案。

**(b) 我以为"每次模型调用 = 一轮"，错了。** 桩脚本写 5 步想看到 5 根柱子，
结果 5 步的 token **全加在一起**、只出一根 —— 读内核源码才确认：
**一轮 = 一次 `BeginTurn` 到它结束**，`TurnComplete` 报的是**整轮累计**
（`usage_in/out` 在整轮里累加）。而"每步不带工具调用"会让内核认为任务已完成、
只跑第一步（另一个方向的错）。两处都试过，注释里写清了。
**要真的多轮必须由外部驱动**（目标模式每子任务一轮，或用户多次提交）。

**(c) 单根柱子占满整行宽度，看起来像横向色带而不是图表。** 柱子宽度原本是
"按数量均分可用宽度"，于是只有一轮时 `bar_w = width` —— 真机截图看出来的。
那一刻"越高=越多"这个读法**不成立**（它同时占满了宽）。
加了 `MAX_BAR_W = 12`：每轮都是一根**竖条**，读法才成立。

**2）修 (c) 时我的测试挂了 —— 而错的是测试。** 它按"x < 宽度一半"给两根
柱子分组，加了宽度上限后两根都挤在左侧、被算成同一根。**测试的分组依据
依赖了一个我正在改掉的假设**。改成按横坐标归组（每根柱子 x 唯一），
并补了一条"单根不占满整行"的测试钉住新行为。
**测试失败时先问"是代码错了还是测试的假设过期了"** —— 这次是后者。

**3）确定性证据（像素采样，不靠目测）：**
- 段色 `#91d1f8`（输入，22656 px）/ `#da7ff2`（输出，11328 px）→ 比例 2:1，
  与设定的 1800:900 一致；
- 柱子横跨 x 14→37 = 24 图像像素 = **12 逻辑像素**，正是 `MAX_BAR_W`，
  证明宽度上限生效（而不是"看着变窄了"）。

**这条经验与渲染缝相关**：缝的价值在于"自绘表面的绘制逻辑可以无头测试"
（组件 13 条测试覆盖比例/下限/裁剪/边界），而**视觉可读性仍然只能靠真机看**
（(a)(c) 都是）。两者不可互相替代。

---

### (aq) 渲染缝第三个消费者（分段进度条）+ 两个只有真机能发现的布局缺陷

**第三个自绘组件：分段进度条**（每个子任务一段，段内表达走到第几阶段）。
加上变更条、用量图，缝现在有 **3 个真实消费者** —— 正好达到方案 Phase 4
"≥3 个自绘组件"的触发条件（那是删 egui 之后才谈的事，但门槛本身满足了）。

写它时**我自己引入了一处违规**：测试里用了
`unsafe { std::mem::transmute }` 想绕开借用问题。项目是**零 unsafe** 铁律，
任何"为了测试方便"的理由都不成立（测试基建是唯一例外，且那条也只在内存
计数器里）。改成直接 `into_iter().map(|(r, _)| r)` 就完了 —— 我一开始写成
借用版，才需要那个 transmute。**绕路的复杂度往往是自己先写错造成的。**

**两个只有真机能发现的布局缺陷**（都与"内容变多"有关，而它们此前不出现
是因为内容少）：

**1）侧栏没有滚动容器。** 加完用量图 + 进度条之后，侧栏内容超出一屏，
超出部分**看不到也滚不到**（目标区块直接消失在窗口外）。
侧栏此前没有滚动是因为内容少到没暴露这个问题 —— 我的组件把它暴露了。

**2）flex 项的 `min-width: auto` 把侧栏挤出窗口。** 这个更隐蔽：
转录区是 `flex_1`，而 flex 项的默认最小宽度是"**内容宽度**" ——
转录里那些不换行的长行（`【目标 goal-1 · 子任务 2/2】请审查…`）把横向布局
撑开，**右侧栏被推到窗口之外**，连标题都看不见。
修法是给**每一层**相关容器加 `min_w(px(0.))`（`h_flex` + 内层 `v_flex`）。

**我第一版只加了外层**，真机上侧栏照样不见 —— 因为内层 `v_flex` 自己也是
flex 项，它也撑着。**"加了一层就好"这个判断必须真机验证**，而我当时差点
以为改完就对了（改完立刻编译通过、测试全绿、无 warning）。

**验证方法**：像素采样找 `Success` 色（`#6ee7b7`）的分布。宽窗口下先确认
三个组件都画出来（129 个绿像素 + 紫/蓝段），再缩回默认宽度确认侧栏仍在 ——
**"宽窗口能看见"与"默认窗口能看见"是两件事**，而我最初的"验证通过"是在
宽窗口下得到的（那时侧栏确实可见，只是被挤出了默认宽度）。

---

### (ar) CI 失败 "读不到日志" 本身是一个必须修的缺陷

首次推送（65 个提交）后 CI 在 Linux 上失败：**编译检查通过、全量测试失败**，
而本地全绿（896 个测试通过、退出码 0）—— 所以是 Linux 特有。

**我为此浪费了约六轮**，因为**读不到失败详情**：
1. `actions/runs/<id>/logs` → **403 "Must have admin rights"**（需要 admin）；
2. AGENTS.md 与 CI 注释里都写着"失败摘要写进 STEP SUMMARY，check-run output
   字段免认证可读" —— **这句是错的**：实测该 check-run 的 `output.summary`
   **为空**，摘要只出现在网页 GC 的 Step Summary 里，API 读不到；
3. 注解（check-run **annotations**）**才是**免认证可读的 —— 我正是靠它读到了
   Node 版本警告，这证明通道可用；
4. 匿名 API 有**每小时 60 次**限额，我前面查得太频繁，**把配额用完了**，
   于是连注解也读不到（要等一小时）。

**修法**（三次迭代才成）：
- 失败摘要**同时**输出为 `::error::` 注解；
- 第一版拼成**一条多行**注解 → API 只回显标题（"exit code 101"），内容看不到。
  改成**每条一个单行注解**（失败用例名/panic/编译错误/日志尾部兜底）；
- 新增 `if: failure()` 步骤把失败用例 `--exact` 逐条重跑并打印完整输出。

**这一轮排查里我推断错六次**（沙箱 fail-closed、Node 版本、字体候选、
NEO_HOME 污染、链接期缺库、bwrap 缺失），每次都以为是根因。而
**改可观测性的那一步才是有确定产出的** —— 它让"以后任何一次失败"都能定位。
**这也是一条效率教训**：读不到证据时，继续猜的期望收益是零；
先修通道才是正确的下一步。

（顺带发现一个真实缺口：`neo-sandbox` 的 Linux/Windows 后端**零测试覆盖**，
详见 (as)。那是排查副产物，与本次失败原因无关。）

---

### (as) 排查副产物：neo-sandbox 的两个平台后端零测试覆盖

查 CI 失败时顺手发现：`neo-sandbox` 的三个 `CommandWrapper` 实现
（macOS `Seatbelt` / Linux `LandlockBwrap` / Windows `RestrictedToken`）
是 `#[cfg]` **互斥**的 —— 在 macOS 上只能编译到 `Seatbelt`。
全局引用计数：`Seatbelt` 有（但都是注释里的提及），
**`LandlockBwrap` = 0、`RestrictedToken` = 0**。

也就是说"Linux 沙箱可用"这句话**从来没有测试支撑**，而它在 README 与
诚实边界里都被写着。这不是本次 CI 失败的原因，但它是同一类问题：
**"只在某个平台上编译的代码"天然缺少测试**，而平台相关的代码恰恰最容易错。

补法：**按平台写测试**（`#[cfg]` 保护），这样各自在对应平台的 CI 上被执行。
覆盖：受限档真的改写命令、全权限档原样返回、包装不丢原参数（丢一个标志
就可能让命令行为全变）、cwd 原样传递（改了会让相对路径指向别处）、
无后端时 fail-closed。牙齿验证过（故意丢参数立刻红）。

**另一个待确认项**：`neo-sandbox` 的 `wrap_or_fail` / `CommandSpec` 在全局
**没有任何调用方** —— SPI 定义存在但未接线。要么接线，要么承认它是给
未来平台预留的（并在文档里说明），否则它就是"定义了却不可达"那类问题的
又一个实例（§4.x 里已记过两次同类）。

---

### (at) 定位 Linux CI 失败时，我在"诊断通道"上连踩三个坑

**当前状态**：Linux CI 仍失败（`verify` 的"全量测试"步骤，exit 101），
**根因尚未定位**。但这个过程中修掉了三个**真实的 CI 缺陷**，
它们每一个都让"失败"变成不可见：

**(1) `run` 块默认 `bash -e` → 失败处理的代码从未执行。**
我加了三版失败诊断（STEP SUMMARY / 多行注解 / 单行注解 / 无条件元信息），
**一条都没出现**。最后发现：GitHub Actions 的 run 块以 `bash -e` 执行，
`cargo test | tee` 失败后 shell **立刻退出**，紧随其后的 `code=$?`、
摘要写入、注解输出**一行都不执行**。修法是显式 `set +e`。
**"错误处理的代码路径没人跑过"的又一个变体** —— 而且这次连它自己都没跑过。

**(2) 日志带 ANSI 颜色 → 行首锚点 `^error` 永远匹配不到。**
修好 (1) 后注解出现了，但"错误行"是空的。原因：cargo 默认输出带颜色，
行实际以 `\x1b[1m` 开头，而我去 ANSI 的 `sed` 排在 `grep` **之后** ——
顺序一换就什么都抓不到。修法：run 块加 `CARGO_TERM_COLOR: never`（根治，
日志对人也可读）+ 诊断里先去 ANSI 再 grep。

**(3) 多行注解会被 API 截断 → 只剩标题。**
第一版把整个摘要拼成**一条**多行注解，API 只回显了
"Process completed with exit code 101"，内容看不到。改为**每条一个单行**注解。

**这一轮的净损失**：十余次推断（沙箱 fail-closed、Node 版本、字体候选、
NEO_HOME 污染、链接期缺库、bwrap 缺失、build.rs 差异、ash 的 linked feature…）
**全部落空**，因为每次推断的依据都是"读代码猜"，而不是失败日志。
而三个通道缺陷又各自伪装成"没有信息"。

**教训**：加诊断代码时，**必须验证它真的能抓到东西** ——
否则"看不到"会被误读成"没有问题"。三次都是同一类错误：
**诊断工具自身的失败，伪装成被诊断对象的正常**。

**下一步（给接手者）**：日志只有 **516 行 / 40 KB**（诊断元信息读到的），
而 896 个测试正常跑完会有数万行 —— 所以这是**编译或链接期失败**，
不是断言失败。而 `cargo check --workspace --all-targets` 是**通过**的，
差异在"test 目标要链接而 check 不链接"。方向：查 gpui 依赖树里
**链接期**需要而 CI 未装的系统库（已排除：ash 的 linked feature 未启用、
xkbcommon 走 dlopen、fontconfig 已装 libfontconfig1-dev）。
**另外注意：上一个提交（2e619056）的 CI 是成功的**，所以这 65 个提交里
有真实回归 —— 二分提交范围比继续读代码更快。

---

### (au) 收尾：Linux CI 根因已定位修复 + 这一轮为什么慢（效率复盘）

**根因（已修，commit b123f843）**：

```
error: linking with `cc` failed: exit status: 1
  = note: rust-lld: error: unable to find library -lxkbcommon-x11
```

gpui 的 Linux 依赖树里有包在**链接期**要求 `-lxkbcommon-x11`，
而 `libxkbcommon-dev` 不含它 —— CI apt 列表缺 `libxkbcommon-x11-dev`。
`cargo check` 不做链接，所以"编译检查"步骤照样绿，症状只在 test 阶段出现。

**我对此方向的判断错过一次**：我查过 `xkbcommon-dl` 确认走 dlopen（对），
就据此排除了"缺系统库"，没注意到同一生态还有**链接期**需求。
**验证"某处不需要 X"推不出"整条依赖树不需要 X"**。

---

### (av) 昨天记的"根因"只对了一半：补库修掉了链接失败，Linux 仍然红

补上 `libxkbcommon-x11-dev` 后逻辑能链接了，但 **CI 还是失败**。上一轮把
"尾部全是 Compiling、没有 error 行"解释成超时/磁盘/链接缺库，都对不上真实形状：
本轮日志**有 5 条以上 `test result: ok` 行**（说明测试**跑起来了**）、末尾
`exit=101`、且**没有 error 行** —— 这是"某个测试用例断言失败"的形状，
不是编译/链接失败。**我上一轮把它归到"编译期"这一类，方向就是错的。**

#### 读不到真因的第二层原因：注解通道的 10 条上限

上一轮我加了三版 `::error::` 注解，却只看到噪声。真正原因有两个，叠加：

1. **每个 step 只保留前 10 条 error 注解**，其余**静默丢弃**；
2. 内联 grep 用了 `-i 'error|failed'`，于是 `Compiling thiserror`（含 `error`
   子串）与 `test result: ok. 0 failed`（含 `failed` 子串）**都命中**。

噪声排在最前 → 10 条配额被吃光 → 真正的 `FAILED` 一条都发不出去。
我用真实日志形状在本地复现出了 CI 还回的那 10 条（4 条 `Compiling` +
5 条 `test result: ok`），确认**不是"grep 没匹配上"**，是匹配得太宽。

修法（commit 32f228e4）：抽出 `scripts/ci-failure-digest.sh` ——
按价值排序（失败用例名 → panic 上下文 → 编译错误 → 链接期失败）、
模式**行首锚定且大小写敏感**、error/warning 各 10 条（两条独立配额）、
消息压成单行。关键是**它可用日志在本地回放**，不必"推一次 CI 才知道写没写对"；
并带 `--selftest`（进 `verify.sh` 第 6 段）—— 把模式改回旧的 `-i` 写法会立刻变红。

#### 真实根因：用例拿"平台相关的沙箱能力"当前提

修好读取通道后，注解一次就点名了：

```
失败用例: tool_cards_carry_name_args_and_output
panic: left: Some(-1)  right: Some(0)   (crates/neo-host-egui/tests/end_to_end.rs:191)
```

`tool_cards_carry_name_args_and_output` 用 `ExecMode::Default`（→ 沙箱档
`WorkspaceWrite`）**真的跑** `bash echo`。而 `LocalSandbox::supports` 里
受限档是 `cfg!(target_os = "macos")` —— **Linux 上 fail-closed**（尚无
Landlock+bwrap 实现），命令根本不执行，`exit_code` 因此是 `-1` 而不是 `0`。

**本机复现（这是关键的一步）**：临时把 `supports()` 的受限档改成 `false`
（即模拟 Linux 的 fail-closed），`cargo test -p neo-host-egui --test end_to_end`
立刻得到 **1 failed / 5 passed**，失败点正是 `Some(-1)` vs `Some(0)` ——
与 CI 首次给出的**逐字一致**。有了这个，修复就不再是猜。

修法（commit ef1c3c5e）：该用例改用 `FullAccess` 档（沙箱档
`DangerFullAccess` 三平台恒可用），**不跳过 Linux**（跳过会让 CI 失去意义）。
用例的主语是界面数据通路（卡片带不带名字/参数/输出），与沙箱档位无关；
拿一个平台相关的产品能力当测试前提，本身就是错的。其余用 `Default` 档的用例
（如"审批前 diff"）不受影响 —— 它们在**执行前**就挂起审批。

#### 两条可复用结论

- **"日志里没有 error 行"不等于"编译期失败"**：先看有没有 `test result: ok`
  行 —— 有就说明测试跑起来了，那是**用例失败**，该找 `FAILED`/`panic`。
  上一轮我据"尾部只有 Compiling"就归到编译期，白追了一整轮。
- **平台相关的产品能力不能当测试前提**：`Default` 档在 macOS 可用、
  在 Linux fail-closed —— 用它当"能跑命令"的前提，等于把 CI 变成平台彩票。
  要么用三平台都成立的档位，要么显式按平台分派（像
  `neo-sandbox-local/src/lib.rs` 的测试那样 `#[cfg]` 分开写）。

---

### (aw) gpui composer 多行：补功能时抓到一个"回车留下换行"的真缺陷

缺口表里"gpui 宿主无多行输入（`Shift+Enter` 什么都不做）"本轮补掉了。
换 `TextareaState` + `submit_on_enter(true)` + `auto_grow(1, 6)` 即可，
但**真正花时间的是一个读文档看不出来的缺陷**。

#### 组件契约说要"提交"，可平台又把它当文本送了一遍

上游 `enter()` 的逻辑很清楚：多行 + `submit_on_enter` 时，非 Shift 的 Enter
走提交分支（不插换行），并且**两种情况都** `cx.emit(PressEnter { shift })`。
看起来配好 `submit_on_enter(true)` 就完事了。实测却在输入框里留下了一个 `\n`。

原因在**组件之外**：`dispatch_keystroke` 在事件未被 `stop_propagation` 时，
会把 `keystroke.key_char` 交给输入处理器 —— 而 Enter 的 `key_char`
**平台层给的就是 `"\n"`**（macOS 见 `gpui-pre-macos/src/events.rs`；测试里
`with_simulated_ime` 同款）。组件在提交分支里调了 `cx.propagate()`，
于是"提交"与"插入换行"**同时发生**。

**发现方式**：无头探针刺订阅 `InputEvent`，看到
`[Change, PressEnter(shift=false), Change value="任务\n"]` ——
提交事件**在**、换行也**在**。这一条序列直接把范围锁死在"事件之后还有一次文本插入"，
比读代码猜快得多。

**修法**：在 `composer_input` 里对非 Shift 的 Enter `stop_propagation()`。
`PressEnter` 在此之前已派发，故提交不受影响；`dispatch_keystroke` 因此跳过
key_char 分支。Shift+Enter 不拦（它本就该换行，且组件那条分支不 propagate）。

#### 三条做法值得复用

1. **行为默认值不能当契约**：`submit_on_enter` 的**默认是 `false`**，
   而它在我们这里是**必须为 true** 的语义（否则 Enter 也换行 = 永远提交不了）。
   所以配了回归用例专门钉这一条，而不是相信"组件会做对"。
2. **生产接线抽成一个函数给测试复用**（`composer_input` / `new_composer_state`）：
   测试若自己拼一份等价元素，产品把 `stop_propagation` 删掉它也照样绿。
   实测牙齿：删 `submit_on_enter` → 用例红；删 `stop_propagation` → 用例红。
3. **无头用例 + 真机各验一次，各管一段**：
   无头钉住可判定的语义（4 条用例）；真机（.app + 合成按键）钉住整条链路 ——
   会话日志里出现 `{"begin_turn":{"text":"first\nsecond"}}` 与 `user_submitted`，
   这才是"多行文本真的进了内核"的证据。**注意**：合成键盘要求 `.app`
   （裸二进制 `bundle_id` 为 null，见 §4.64(t)），所以真机验证必须先跑
   `scripts/make-app.sh`。

---

### (ax) 渲染缝的第二个后端：把"只有一个实现是信仰"兑现成"两个实现是设计"

用户指出"渲染引擎和组件库没大进展"—— 这个判断是对的，而且是可量化的：
`neo-ui-render` **只有一个 `impl RenderBackend for`**（GpuiBackend），
而该 crate 的头部自己就写着方案那句
**"只有一个实现的抽象是信仰，两个实现的抽象才是设计"**。
本仓的 AGENTS.md 也把"每个 SPI 必须 ≥2 真实后端 + conformance"列为硬约束 ——
**这条缝自己没达标**（且没登记为 SPI，所以门禁没卡它，属于门禁盲区）。

#### 做了什么

补上 `HeadlessBackend`（无 GPU、可在 CI 跑），并写 `tests/conformance.rs`
用**同一份用例**跑两个后端（6 条契约）。关键是两个后端的能力矩阵**故意相反**：

| | GpuiBackend | HeadlessBackend |
|---|---|---|
| 输出 | gpui 绘制参数 | 显示列表 + **SVG 导出** |
| 需要 GPU | 是 | **否** |
| 文字 | **画不出** | **画得出** |

"两者能力不同"这件事本身，就是 `supported()` 该属于**后端**而不是中立层的证明 ——
放进中立层就必然要按某一个后端写死。

顺带把两处"静默"变成"可断言"：
- 新增 `unsupported_ops()`：把"场景里有后端画不出的指令"变成可查询的事实；
- `StrokeRect` 从**静默不画**改为真的画（除零线宽），修掉一处真实的静默丢弃。

#### conformance 当场抓出**两个**我自己写的缺陷（这是第二个实现的价值）

1. **颜色占位规则不一致**：gpui 后端对 `PopClip` 留占位（0），我给无头后端漏了 ——
   于是同一个场景，两个后端的颜色序列**长度不同**。
   **两个后端对同一契约有两种实现，正是这条缝存在的意义所在**，而它被跨后端
   比对当场抓住（`left.len()=7 / right.len()=8`）。
2. 修 #1 时我在 `paint()` 末尾加的 `debug_assert_eq!(colors.len(), scene.len())`
   **又一次抓住自己**：`PopClip` 分支当时只 `{}`、忘了推进位。

这两个缺陷都**不会有任何编译错误**，真机上只表现为"颜色莫名错位"。
**这就是"第二个实现"能买到的东西 —— 不是多一个后端，是多一套发现问题的机制。**

#### 诚实边界（别把这一步读大了）

- 无头后端是**对照物 / 测试替身**，**不是**第二个生产渲染路径。
  方案 Phase 4 要的 VelloBackend 仍未做，触发条件明确（主应用稳定 ≥6 个月 +
  ≥3 个自绘组件 + 专职人力）。
- 与本仓既有约定一致：`neo-mock` 的 noop/mock 同样算各 SPI 的实现
  （Clipboard 的 System+Noop、Sandbox 的 Noop+Leaky 都是这样）。
- **组件库层面的真相**：`neo-ui` 是**主题**不是组件库（它自己的 README 就这么写），
  通用控件来自 `neo-ui-kit`（上游），自研的只有 RichText + 3 个自绘消费者
  （变更条 / 用量图 / 分段进度）。所以"组件库没进展"这个观察，
  正确的表述是：**我们的自研面本来就窄**（方案 P0 明说"自研组件应为 0–5 个"），
  缺的不是数量，是这条缝此前**没被验证过**。
- 已登记进 `check_spi_conformance.py`（SPI 7 → 8，上限 8 已满）。
  ⚠️ **SPI 上限到顶了**：下次再想登记新 seam，必须先论证旧的该合并或降级，
  否则就是在走 DSH "seam 无序增生"的老路。

---

### (ay) 让缝真的被用起来：自绘 diff 行背景带（第 4 个消费者）

§4.64(ax) 收尾时给的判断是"下一步不是加第三个后端，而是让缝真的被组件用起来"，
方案 P2 点名的 `DiffView` 就是那个方向。本轮做了它的第一层：**行背景带**。

#### 补的是一个具体的可读性缺口（不是"再加一个组件"）

两个宿主的 diff 正文此前都是**逐行彩色文字** —— 绿字=新增、红字=删除，
但底子与其它所有内容一样。问题是：**逐行彩色只标注了单行语义，没有给出
"改动落在哪几段"的形状**，而看 diff 时的第一个问题恰恰是那个形状。
GitHub / Zed / ZCode 都有行背景带，这就是让 diff 能**扫读**的那一层。

#### 分工：缝画底、宿主画字

底带是矩形填充（缝最擅长的），文字**留在 element 树里**（保住 gpui 的文本整形、
字形缓存与 element diff —— crate 头部的纪律）。这也解释了为什么
`GpuiBackend` 画不出文字却不影响本组件：底带只用 `FillRect` + 裁剪。

#### 对齐是**构造保证**的，不靠调间距（这条最容易做错）

底带的 y 由 `i * line_height` 给出；文本容器设 `.line_height(同一个值)`，
且每行 `.whitespace_nowrap()` —— 于是"一行文字"恰好占 `line_height` 高，
与带子严丝合缝。`line_height` 取自 `Window::line_height()`（**当前文本样式的
真实行高**，不是魔数），两边同源。

**为什么强调这个**：若靠"看起来差不多"去调间距，换字号/换字体立刻错开，
而**错位的背景带比不画更糟** —— 它会把"改动"标到错误的行上，用户据此判断
改了哪一行。所以单测直接钉死几何：第 i 条带子必须落在 `[i*lh, (i+1)*lh)`。

另一个细节：`whitespace_nowrap()` 不能省。折行会让"一行文字"占两行高，
底带立刻错位一半 —— 长行该由裁切/横向滚动处理，不是靠折行。

#### 真机验证（按窗口 ID 截图，不抢用户焦点）

用户明确要求"别把窗口抢到前台，我在做别的事"。用 `screencapture -l <windowid>`
按窗口 ID 截取 —— **不需要激活窗口**，因此配合的是那次实测的另一个教训：
先有"不干扰用户"的验证手段，才谈得上频繁验证。

测得的对齐精度：红底带 **130pt 覆盖 5 个删除行 = 每行 26pt**，与行高完全一致；
hunk 头淡蓝、新增行绿、上下文行无底。

**又一次踩到"不要用眼睛判断"的坑**：第一轮我用 MCP 截图 + `screencapture`
分别取图，两张图的坐标系不同（一个 1280 宽、一个 4480 宽），我按 1280 的坐标
去裁 4480 的图，裁出来是无关区域，差点据此得出"底带没画出来"。这与 §4.64(al)
那次"图像坐标与逻辑坐标混用"是**同一个坑**，第二次踩。

---

### (az) diff 未改区块折叠：一个"实现了但休眠"的功能，以及为什么必须说清楚

继续 §4.64(ay) 的方向（让缝/UI 真的可用），做 diff 的**折叠未改区块**。
功能本身不大，但这一轮最有价值的产出是**一个实测结论**，它推翻了"做完即可用"的默认假设。

#### 实测：对我们自己生成的 diff，折叠**永远不会触发**

`neo-capability::diff::unified_diff` 用 `similar` 的 `context_radius(CONTEXT)`，
而 **`CONTEXT = 3`** —— 每个 hunk 最多 3 行上下文。于是连续上下文最长 3 行，
而折叠阈值是 6：**永不触发**。

我是用探针实测出来的（构造 60 行文件、只改中间一行，再数最长连续上下文），
不是读代码猜的。这一条如果没测，我会把"实现了折叠"当成"折叠可用了"报出去 ——
**功能在、路径通、界面不报错、但用户永远看不到它**，属于最难被发现的一类错误。

#### 处理方式：把它标注成"休眠基建"，并用测试钉住这个事实

三种处理方式里我选了第三种：
1. 调低阈值让它"看起来在工作" —— **错**：折 3 行要多出一个把手，净收益为负，
   而且会把"上下文"这个理解改动所必需的东西藏掉；
2. 把代码删掉等以后再加 —— 过：逻辑（分类、阈值、边界）现在写正好，
   而且它是并排视图/宽松上下文的前置；
3. **保留 + 明确标注休眠 + 测试钉住触发条件** —— 采用。

具体做了三件事：
- 模块头部写清"当前没有触发条件"，并列出三种会立刻激活它的情形
  （上下文半径调大 / 喂进完整上下文 diff / 并排视图）；
- 加测试 `our_own_diff_shape_does_not_fold`：若有人调小阈值让它"看起来在工作"，
  这条会红 —— 那是需要停下来重新权衡的信号，不是修测试；
- changeset 与 parity 文档都写明"对我们自己的 diff 休眠"，不写成"已实现折叠"。

#### 顺带做对的一件事：`DiffBand` 从"二分"补成"完整分类"

折叠需要区分"未改的上下文"（可折）与"文件头 / 截断说明"（**不可折**）。
而宿主此前的分类是 `_ => Context` —— 把 `Header` 与 `Meta` 混进了上下文。
折叠若按"是上下文就折"，会把**文件头**（不知道这段属于哪个文件）与
**"…另有 N 处改动未展示"**（不知道 diff 被截断过）一起藏掉。

所以 `DiffBand` 补成完整枚举（+`Header` / `Meta` / `Fold`）。
**这类"为了新功能必须把旧的有损抽象补全"的情形，是重构最正当的时机** ——
不是为了整洁，是因为有损抽象在新需求下会变成正确性 bug。

#### 分层与可测性：把宿主接线也抽成纯函数

`neo-ui-behavior` 只能依赖 `neo-ui-render`（守卫 U3），所以折叠建立在**中立
`DiffBand`** 上，不认识 diff 语法 —— 语法分类留在宿主。
宿主那一步（分类 + 折叠 + 底带重算）抽成纯函数 `diff_display_rows`，
并断言硬不变量：**底带必须与显示行同长**（折叠改变显示行数，底带沿用原行数
就会错位）。这样"接线对不对"不必靠真机截图 —— 3 条无头测试就覆盖了
"真的折了 / 展开态铺开 / 底带同长"。

#### 方法上留下的两条

1. **"实现了"不等于"生效了"**：任何新增路径都要问"**触发条件在当前数据下
   成立吗**"。这次靠一个 20 行的探针测出来；不测就只能靠假设。
2. **休眠的功能要显式标注 + 用测试钉住**，否则下一个人（或下一轮的自己）
   会把它误当成已生效，或者为了让演示好看而调参数破坏净收益。

#### 顺带查清了一件事：为什么这个 diff 不长（即折叠休眠的上游原因）

休眠的直接原因是 `CONTEXT = 3` —— **没有注释说明为什么取 3**，是个未经审视的
unix 默认。查清了三件事，供将来决定"要不要让它活起来"：

| 问题 | 结论 |
|---|---|
| 它进模型上下文吗（改了会不会加 token 成本）？ | **不进**。`PatchProposed` 只走事件流给宿主渲染；模型看到的是 `ToolResult`（工具自己 report 的内容），与 `/review` 一样是本地操作 |
| 改动波及面？ | **只有审批预览这一条路**（`tool.preview()`）。生产调用方仅 `neo-capability/src/lib.rs:189`；其余全是测试 |
| 现有测试是否依赖具体行数？ | 几乎不 —— 只有一个 `assert!(d.contains(" one"))`（宽容断言），不钉行数 |

**所以"把审批预览的上下文从 3 提到 10"是一次低风险改动**，且能让折叠真正生效。
但我**没有在本轮顺手改**，两条理由：
1. 那会改变 `TUI` 宿主里 diffview 的观感（共享同一个生产者），属**产品决定**
   （审批时想给用户看多宽的上下文），不该由一个"补一个 UI 组件"的任务顺带定；
2. 折叠本身对**完整上下文的 diff**（用户粘贴的 `git diff -U100`、将来并排视图）
   已经可用，不必为了"让新功能显得在工作"去动共享数据形状。

已把这条判断写进 changeset 与 parity 文档，供用户拍板。

---

### (ba) diff 行内强调：三个实测抓到的坑，以及"粗体在中文上等于没做"

继续 D5，做行内强调（把真正变化的字再点亮一次）。功能不大，但**三个坑都是
实测抓到的，且都不是编译错误**：

1. **`similar::from_chars` 返回 char 下标、不是字节偏移**
   （实测 `-中文旧内容`：6 char / 16 字节）。直接当字节用会切在字符中间，
   `&str` 切片甚至直接 panic。故建 char→byte 映射。**这类"下标语义"错误
   在纯 ASCII 测试下完全测不出来** —— 我用探针先确认了语义再写代码。
2. **`\ No newline at end of file` 会打断 Del/Add 配对**。我们自己的
   `apply_patch` 常产出它（旧文件无尾换行时），而它在 `diff_line_kind` 里是
   `Meta` —— 卡在 Del 块与 Add 块**之间**，于是"两段紧邻"的判定失败，
   行内强调在**整文件重写**（最常见的一类改动）上永不生效。
   屏幕上只表现为"没有强调"，看不出是 bug —— 属于最该防的那类静默失效。
   **发现方式：真机看了一次截图，发现该强调的地方没有强调**。
3. **粗体在中文上约等于没做**。第一版用 `FontWeight::BOLD`，真机看不出差别；
   原因不是实现错，而是 **CJK 字形普遍没有真正的粗体字面**，字体系统要么
   synthesize（很轻微）要么直接取常规字面。这与 §4.64(e) 豆腐块**同源**：
   都是"中文在字体层被特殊对待"。改用"更浓的同色系底"（实测 29 → 89，
   差 60 个色阶），与字体无关，而且**可用像素断言**（粗体只能靠"看起来粗了
   一点"，无法判定）。

#### 顺带一个渲染缺陷，同样是真机才看见

强调片段最初直接塞进外层 `v_flex`，于是**每一段各占一行** —— 真机上表现为
"行首的 `-` 单独占一行、内容另起一行"。片段是**同一行**的内容，必须放在
**横向**容器里。这类"flex 方向选错"的问题在代码里读不出来（`div()` 不带方向
语义），只有看图才知道。

#### 方法上的一条

**"我加了某个强调手段" ≠ "它在目标字形上看得出"**。粗体这个选择在英文语境下
完全合理（我凭习惯选的），但本项目的用户大量用中文。**凡是与字体/字形相关的
视觉手段，都要在中文内容上实测一次** —— 这也是本项目第三次被同一类问题咬
（豆腐块 → egui 默认字体不含 CJK；→ 粗体无 CJK 字面）。

---

## 效率复盘：这一轮跑了 2 小时+，主要成本是我自己造成的

用户明确指出效率太低。按 `ai-efficiency-rules` 的六类违规逐条对照：

**(1) 无归因重试（最严重，占了大半时间）** —— 读不到 CI 日志后，
我连续做了**十余次推断**（沙箱 fail-closed / Node 版本 / 字体候选 / NEO_HOME
污染 / 链接期缺库 / bwrap 缺失 / build.rs 差异 / ash 的 linked feature /
磁盘 / 超时…），每次都是"读代码猜 + 推翻"。**正确顺序是反的**：
应当**先修可观测性（一次），再读真实日志（一次）**，而不是在无证据时反复猜。
两次就够的事，我做成了十余次。

**(2) 重复拉取 + 固定盲等** —— 匿名 GitHub API 每小时 60 次，
我反复查询把它耗光，然后 `sleep` 等一小时配额恢复（等了 3 轮）。
这不是"等待就绪"，是**我自己制造的等待**。正确做法：一次拿到 run id，
等 CI 完成后一次读注解；或直接用 `curl` 轮询时**记账并留余量**。

**(3) 该并行却串行** —— 修 CI 诊断的三处缺陷（`bash -e` 吞异常、ANSI 让
`^error` 失配、多行注解被截断）本该**一次性全想到**（都是"CI 日志可读性"
这个已知问题面），我分了三轮推送→等 CI→再修。每轮 10+ 分钟。

**(4) 输出爆炸** —— 这个失败我写了 5 个 commit + 4 节记忆（(ar)(as)(at)(au)），
远超"一个真 root cause + 一条教训"所需的篇幅。

**共同根因**：**没在第一步判断"我现在有没有证据"**。
证据缺失时，唯一有价值的动作是**修取证通道**，其余都是空转。

**下次遇到读不到 CI 结果时，正确的一步**（写在最前面，别再推导）：
1. 先确认 `gh` 或 token —— 没有就**立刻**决定用注解通道；
2. 注解通道要能读到东西，需要：`set +e`（否则失败处理不执行）、
   `CARGO_TERM_COLOR: never`（否则行首锚点失配）、**单行**注解（多行被截断）、
   **模式行首锚定且大小写敏感**（`-i 'error|failed'` 会被 `Compiling thiserror`
   与 `test result: ok. 0 failed` 吃光 **10 条**注解配额 —— 见 §4.64(av)）；
3. 这三条现在都在 `.github/workflows/rust.yml` 里，直接用，不要重新发明。
   摘要逻辑已抽成 `scripts/ci-failure-digest.sh`，**可以先本地回放日志**，
   不要用"推一轮 CI 看注解对不对"来迭代（那是 10 分钟/轮的空转）。

**匿名 GitHub API 每小时 60 次**，而轮询 run 状态是最容易把它耗光的动作。
要等 CI 时：**用 `/rate_limit` 探测**（它不消耗配额）而不是持续 `curl` run；
或先算好"预计完成时间"再一次性读取。为等配额而 `sleep` 一小时是本末倒置。

---

## 6. 旧实现（legacy/）里哪些经验仍然有效

重写时**别重复这些坑**，也**别以为重写能消掉它们**：

| 经验 | 重写后 |
|---|---|
| `file:`/symlink 解析陷阱、profile/pnpm/bundle 层叠、Node ≥22 要求、bundle 改动须重启 | ✅ **消失**（编译期静态链接、单二进制） |
| Electron ~78 MB 壳 | ✅ 消失（系统 webview ~5–10 MB） |
| **DOM/CSS 复杂度**（哈希类名不可选、`backdrop-filter` 会为 fixed 后代创建包含块、`isolation` 会创建层叠上下文） | ❌ **仍在**（webview 就是浏览器） |
| **OS 沙箱三平台差异**（Seatbelt / Landlock+bwrap / ACL 不同构） | ❌ **仍在**，且是 NEO 最重的部分 |

---

## 7. 调试与验证

```bash
cargo test --workspace      # 806 测试（内核 / 内存有界性 / SPI / 宿主 / TUI / Web HTTP / 原生 GUI）
bash scripts/verify.sh      # 全套门禁（Rust 测试 + 零 warning + 6 个 Python 守卫 + 执行效率）
```

**验证套件在 `docs/neo-plan/05-验证/`**（Python + golden 用例），
`scripts/verify.sh` 会自动带上它并设 `NEO_ROOT`。

**写测试时的两条纪律**：
1. **每个 SPI 都配一个「坏后端」**作为负向用例被试 —— 抓不住反例的套件没有牙齿。
2. **性能与有界性要实测**，不能写形容词。`crates/neo-core/tests/memory.rs`
   用计数 `GlobalAlloc` 量化分配；上限类断言必须给出具体字节数。

---

## 8. 当前缺口（诚实清单）

| 缺口 | 影响 |
|---|---|
| ~~**gpui 宿主「回车提交」无机器证据**~~ **已解决** | 根因不是 gpui 而是**没打包**：直接跑裸二进制时进程 `bundle_id` 为 null，WindowServer 拿不到稳定应用身份 → 合成键盘事件投不进去。加了 `scripts/make-app.sh`（最小 .app + `CFBundleIdentifier` + ad-hoc 签名）后，`type`/`key return` 都能投递，会话日志出现 `user_submitted` —— 回车提交现在是**有机器证据**的结论（见 §4.64(t)） |
| ~~**gpui 无自动跟随**~~ **已补** | 语义是"只在底部才跟随"（`neo-ui-behavior::scroll`），真机验过：往上翻后等 2 秒画面不变（没被拽回）。真机诊断 `NEO_GUI_SCROLL` 可打印真实滚动值 |
| **egui 未删（阶段 3 剩余）** | gpui 已转正为默认，但 egui 保留作回退通道。**删除的前置条件**（三条全满足才删）：① 至少一个发布周期里 gpui 作为默认无 P0/P1 问题；② 有人在真实工作中用它连续数小时（当前只有我按脚本走的验证，证明"路径可用"不等于"长时间稳定"）；③ 确认 egui 无 gpui 缺失且需要的独有能力（现查为：无）。**不擅自删的另一理由**：保留对照物才能判断新问题是 gpui 的还是内核的 |
| ~~**gpui 侧栏不可折叠**~~ **已补** | egui 有 `sidebar_open`，gpui 是固定面板。补了状态 + 状态栏开关（与 `/sessions` 同一个 Action）。程序化验证：折叠后面板区像素全为页面底色 |
| ~~**gpui 模型选择器缺信息**~~ **已补** | 此前是左右盲切按钮（看不到有哪些可选、看不出哪个是桩）。改为选择器面板：列出全部 + 说明 + 桩标"（桩）"+ 当前项高亮。装配层改传 `ModelChoice`（此前只传名字，宿主没有信息可标）；`A::ShowModels` 与它统一，旧的 `next_model` 已删 |
| ~~**gpui 宿主无多行输入**~~ **已补** | 已换成 `TextareaState`：**Enter 提交、Shift+Enter 换行**（`submit_on_enter(true)`），高度 `auto_grow(1, 6)`。补的过程中抓到一个**只有端到端才看得见**的真缺陷：平台会把 Enter 当文本再送一遍（macOS 平台层给 Enter 的 `key_char` 是 `"\n"`），而组件在提交分支里 `cx.propagate()` —— 于是**回车既提交、又留下一个换行**。修法：冒泡时 `stop_propagation()`（提交事件在那之前已派发）。真机证据：会话日志 `{"begin_turn":{"text":"first\nsecond"}}` + `user_submitted`，按回车后输入框无残留换行。见 §4.64(aw) |
| ~~**许可证选择待拍板**~~ **已定** | 用户拍板：可开源集（5 个 crate）用 **Apache-2.0**（含明确专利授权），宿主与内核保持 MIT。已落地：显式 license 字段 + 标准全文 LICENSE + README 说明；提取集 38 个 Apache-2.0 依赖**都不带 NOTICE**，故 §4(d) 义务不触发（已写进 THIRD-PARTY-LICENSES.md）。仍未做：`cargo-deny` 需在 CI 安装后跑全量（本地脚本已覆盖主要能力） |
| **NOTICE 依赖上游包内容** | §4(d) 义务的判定依据是"上游是否随包发布 NOTICE"。本脚本查的是本地 registry 目录 —— 若某包在上游带 NOTICE 而随包未分发，会漏判。彻底做法是用 `cargo-about` 读包元数据。当前实测 38 个包全无 NOTICE，风险低但非零 |
| ~~**渲染缝只有一个实现（假 SPI）**~~ **已补第二后端** | `neo-ui-render` 此前只有 `GpuiBackend` 一个 `impl`，正是 crate 头部自己引用的"只有一个实现的抽象是信仰"。本轮补 `HeadlessBackend`（无 GPU / 可跑 CI）+ `tests/conformance.rs`（同一份用例跑两个后端，6 条契约），并登记进 SPI 门禁（7 → 8）。**仍未做**：方案 Phase 4 要的第二个**生产**后端（`VelloBackend`：vello + wgpu）—— 它有明确触发条件（主应用稳定 ≥6 个月 + ≥3 个自绘组件稳定运行 + 专职人力，预期 12–18 个月）。**别把无头后端读成"引擎能开源了"**。见 §4.64(ax) |
| **SPI 数量已达上限 8** | `check_spi_conformance.py` 的 `MAX_SPI = 8` 现已用满。再想登记新 seam 必须先论证旧的可合并/降级，否则就是走 DSH "seam 无序增生"的老路（该上限本就是为防这个设的） |
| **Linux 桌面宿主的运行时库** | gpui 在 Linux 靠 **dlopen** 加载 `libxkbcommon` / `libwayland` / `libX11` —— **编译不需要**这些包，但运行时缺了会直接 panic（`Library libxkbcommon.so could not be loaded.`）。预编译 Linux 产物不含桌面（精简版）故不受影响；自行 `cargo install` 的 Linux 用户需要先装它们。README 已写明 |
| ~~**Linux 构建未验证**~~ **CI 已能跑通测试** | 本地交叉检查不可行（macOS 无 Linux C 工具链，`cc-rs` 直接报缺 `x86_64-linux-gnu-gcc`），只能靠 CI。已定位并修掉两个 Linux-only 拦路石：① 链接期缺 `libxkbcommon-x11-dev`（`cargo check` 不链接故只在 test 阶段暴露）；② 用例拿 `ExecMode::Default`（→ 受限沙箱档，Linux fail-closed）当"能跑命令"的前提。后者已在本机用"模拟 Linux fail-closed"复现并修（§4.64(av)） |
| **IME 只验了组件契约** | 已有 3 条自动化回归用例（preedit 删除后重输、连续合成、基建自证），钉住我们依赖的组件契约。但**真输入法**（装中文输入法敲键、候选框跟光标）未验：本用例不驱动系统输入法进程，真机还可能在奇怪时机连发 unmark |
| **高 DPI 只验了整数倍两档** | 1x / 2x 实测通过（清晰、中文正常、无错位）。但 macOS 的 fractional scaling（125%/150%）是"高分辨率渲染再缩放"的**不同机制**，本机没有对应的可用模式，无法验证 —— 不能由整数倍结果推断 |
| **Desktop 打包：有脚本，未产品化** | `scripts/make-app.sh` 已能产出可用 .app（Info.plist + `CFBundleIdentifier` + ad-hoc 签名）—— 它同时是**GUI 键盘自动化验证的前提**（裸二进制 `bundle_id` 为 null，合成键盘投不进去，见 §4.64(t)）。**仍未做**：图标、Apple 签名与公证（正式分发必需）；Windows 无对应脚本 |
| **Linux / Windows 沙箱未实现** | 受限档位在这些平台 **fail-closed**（拒绝执行），不会静默降级放行 |
| **MCP 未接 prompts** | 两种传输 + tools + resources 已就绪；提示模板未接入（设计决定:与 $skill 同构,触发条件见 4.5x） |
| **Subagent 已接入(白名单版)** | Markdown 定义(.neo/agents)→ 白名单裁剪的子内核(审批 Never、沙箱继承、过程不进主转录),真机验证主模型委派 `agent_scout` 完成。按行拆解之外的 agent 调度(并行、深嵌套)未做 |
| **MCP 工具无结构化 diff 预览** | 外部工具内容由服务器决定，客户端生成不了可信 diff —— 审批时看不到"将改什么"，只有参数摘要 |
| **技能只在启动时加载** | 引用是热路径、扫盘是冷路径，故装配时加载一次；会话中途新增技能需重启才可见（MCP 服务器同理：改 `mcp.json` 需重启） |
| **AGENTS.md 超限不生成摘要** | 规划里是"让模型生成握手摘要"，当前只做诚实截断（截到 32 KiB 并标注） |
| **Goal 审查的模型自评是半程** | 模型能显式叫停（审查答复含「审查未通过」→ 回退重做），但"沉默"视为通过（离线桩友好、避免含糊模型烧光重试预算）；挂钟停止条件未实现（会破坏回放确定性） |
| **`#session` / `/command` 引用不注入文本** | 刻意如此：它们是宿主动作而非模型上下文。宿主未实现时这两类引用是空操作 |
| ~~**Web 宿主无鉴权**~~ **已加访问令牌** | 已实现（§4.61）：除内置页面外一切端点都要令牌，令牌经 fragment 下发、页面自动接上。剩下的是**边界**而非缺口：这是单进程生命周期的一次性令牌（无用户/角色），且 `--addr` 对外监听时流量仍是明文 HTTP（已有显式警告，需 TLS 反代） |
| **发行物未做代码签名** | `install.sh` 会校验 SHA-256（防损坏），但 macOS 产物未做 Apple 签名/公证、Windows 无 Authenticode。用户首次运行可能遇到 Gatekeeper 拦截；正式对外分发需签名证书（见 §0） |
| **Windows 无预编译产物** | `release.yml` 只出 macOS（双架构）+ Linux x86_64；且 Windows 沙箱未实现，故未纳入。Windows 用户当前需 `cargo install`（受限档位仍 fail-closed） |
| **npm 包尚未真正发布** | 包装层、平台包、`publish-npm` job 均已就绪并本地端到端验证（真实 `npm install -g` 跑通）。发布需仓库 secret `NPM_TOKEN`，打 tag 即自动发（见 §0） |
| **crates.io 未发布（刻意不做）** | 元数据齐备、`cargo package` 全部通过，但决定不发布：`cargo install --git` 已覆盖该用户群，而 23 个包需长期版本同步。理由见 §0(a2) |
| **`changeset release` 每次红（设置问题，非代码）** | 机器人分支能推（`origin/changeset-release/main` 存在且为 `chore(release): v0.2.0`），但 `gh pr create` 失败、Actions 里零 PR —— 根因是仓库设置「Allow GitHub Actions to create and approve pull requests」未勾选（GitHub 默认关闭，`pull-requests: write` 也放行不了）。**改代码无解**，需在 Settings → Actions → General 勾上（组织仓库还要 org 级放行）。它让 `changeset release` 长期显示红色，别误判成代码回归；`rust`/`efficiency` 是绿的。已写进 `docs/RELEASE.md` §5 |

**下一步优先级建议**：Desktop 的 webview 窗口层（需要平台图形栈与 GUI 验证，动手前先做窗口壳的设计决定）。

---

### (bb) D12 文件树：补上"解析的逆"，以及两个实测才暴露的默认值陷阱

parity 里唯一还是 ❌ 的 D12，本轮做了第一层：**文件树**。

#### 做之前先发现一个真缺口：`@file` 引用**只有解析、没有格式化**

`parse_refs` / `parse_file_ref` 早就有了，但反向是空的 —— 于是"把一条文件引用
写进输入框"这件事**没有任何正确做法**，调用方只能 `format!("@{path}")`，
而**路径含空格时这就错了**（`@my file.txt` 会被切成 `@my` + 普通词 `file.txt`，
引用静默消失）。文件树"点一下加进上下文"正是这个场景，所以先补
`format_file_ref`（引号规则与解析严格对偶，往返用例钉住）。

**教训**：一个"只有解析没有格式化"的 API 是一半的 API。做新功能时如果发现
要用的基础件缺了反向操作，**先补基础件**，而不是在调用点手拼一个近似正确的
字符串 —— 手拼的那份必然在边界（空格/引号/非 ASCII）上错，且错得静默。

#### 两个默认值陷阱（都是实测才暴露，都不是编译错误）

1. **`ignore::WalkBuilder::require_git` 默认为 `true`** —— 语义是"只有身处
   git 仓库内才应用 .gitignore"。对 `git` 是对的，对**文件树**是错的：
   非 git 目录（用户的工作区可能只是普通文件夹）会**完全不忽略**，
   `node_modules/` 照列。我的临时目录测试就是这么红的（没有 `.git`）。
   修：显式 `.require_git(false)`。
   → **通用教训：一个库的默认值是"为它最常见的用户"选的，不一定是为你选的。**
   用它之前要问"这个默认在我的场景下对吗"，尤其当我的场景（文件树）
   与它的原生场景（git 工具）相近但不同。
2. **`NEO_GUI_PANEL=files`（启动即打开）走构造函数、不经过 `run_action`** ——
   于是我写的惰性扫描（在 toggle 里调 `ensure_files`）从未触发，
   面板永远停在"正在扫描工作区…"。**这是看真机截图发现的**：
   静态读代码时"打开面板 → 扫描"看起来是通的。
   修：让"面板可见 ⇒ 索引就绪"成为**自足条件**（渲染时保证），
   而不是给每个开启入口都记得调一次（典型的"改一处忘一处"）。

#### 验证方式（延续"不抢用户焦点"的约定）

- **与 `git ls-files` 交叉核对扫描准确性**：本仓扫出 342 个文件，
  git 跟踪的非隐藏文件 341 个，唯一差异是当时未跟踪的新文件 ——
  **既无泄漏（没漏掉被忽略的）也未过度排除**。这比"看一眼树觉得对"强得多，
  而且它同时验证了忽略规则的两个方向。
- 真机按窗口 ID 截图确认：树渲染正确、`debug.log` 与 `target/` 被排除、
  `.gitignore` 自身不可见。

#### 无障碍改动顺带解开了"点击路径无法验证"的死结

给文件行加 `role(Button)` + `aria_label` 时，理由写的是"读屏用户拿不到文件名" ——
它随后带来一个**意外收益**：**可访问 = 可验证**。

- 加之前：AX 树里整棵文件树**一条都没有**（自绘文本行默认不进无障碍树），
  于是"点一下文件"这条路径只能靠纯函数组合间接验证。
- 加之后：`button 加入引用 src/main.rs` 出现在 AX 树里，于是可以直接
  `AXPress` **语义点击**（不需要坐标、不需要窗口在前台）——
  这正好绕开了"点击要抢焦点"与"不抢焦点"的冲突。

于是本轮**真机验完了整条链路**（全程前台没被抢）：
1. 点 `加入引用 src/main.rs` → 输入框读到 `@src/main.rs`；
2. 点含空格的 `加入引用 my file.txt` → 输入框读到 `@"my file.txt"`
   （**引号正确**，这正是手拼 `@{}` 会错的那一格）；
3. 再点 `plain.rs` → 读到 `@"my file.txt" @plain.rs`（**追加而非覆盖**）；
4. 回车提交 → 会话日志：
   `"refs": [{"target": "my file.txt"}, {"target": "plain.rs"}]` +
   `refs_resolved: ["📄 my file.txt 已注入", "📄 plain.rs 已注入"]`。

**教训（值得复用）**：给自绘控件补无障碍不只是"合规"或"照顾读屏用户"——
它同时把**程序化验证的通道**打开了。自绘 UI 的默认状态是"既不可访问也不可
测试"，补 a11y 是一石二鸟。反过来，之前遇到"点击投不进去"（§4.64(al)(t)）
时的正解可能也不是环境变量钩子，而是**先把控件做成可访问的**。

---

### (bc) 审批预览上下文 3 → 10：顺手让一个"休眠功能"活过来，并给它加了守卫

用户拍板把 `neo-capability::diff::CONTEXT` 从 3 提到 10。这是 §4.64(az) 里
那个待决问题，本轮落地。

#### 为什么 3 是错的（不是"够用"，是"没想过"）

原注释只写"每个 hunk 保留的上下文行数"——**一个数字，零理由**。
3 是 git 默认值，对"补丁邮件逐行读"够用；但审批预览是**判断依据**，
3 行常常交代不清（前面是什么函数、变量从哪来）。改成 10 后：
- 审批时看到更多上下文（产品意图）；
- **折叠未改区块真正生效**（阈值 6 < 半径 10）—— 那个功能此前实现了却永不触发。

**顺带一个正面副作用**：半径变大让相距 < 2×半径 的改动合并进同一个 hunk，
`@@` 头反而变少。所以这次改动是"更长的上下文 + 更少的 hunk 头"，不是单纯变长。

#### 本轮最有价值的产出不是改值，而是**给耦合加了守卫**

`CONTEXT` 与折叠阈值（6）之间的耦合，此前**没有任何东西守着**。谁把 CONTEXT
调回 ≤6，折叠会**再次静默失效** —— 表现只是"没有折叠把手"，不报错、不崩溃。
这正是 §4.64(az) 记的那类错误，而我当时只写进了文档（文档不执行）。

所以加了两条断言（`neo-host-gpui` 的测试里）：
1. `fold_threshold_stays_below_the_context_radius` —— 断言**参数关系**，
   报错信息直接给出可操作建议（调大 CONTEXT 或调小阈值）；
2. `a_real_approval_preview_actually_folds` —— 用**真实生产者**
   （`unified_diff`）产出的预览，端到端断言"真的折了"。
   两条互补：前者管参数、后者管接线（参数对但忘了把 bands 传进 fold_rows，
   只有后者会红）。

**牙齿已验证**：把 CONTEXT 改回 3，第 1 条立刻红。**"文档里写了"不等于
"被守护了"** —— 能执行的约束才叫约束。

#### 顺带修掉两处"钉死实现细节"的测试

`hunk_header_tracks_old_and_new_line_numbers_separately`（capability）与 TUI 的
跨 crate 往返用例，都把"两个改动相距 20 行"**写死**。半径变 10 后两个改动被合并
进同一个 hunk，于是断言"应有两个 hunk"失败。

**判断**：这两条测试的**意图**（守"hunk 头两侧起始行号必须错开"这个旧 bug）
是正确的，钉死的只是手段。所以改为**由 `CONTEXT` 推出距离**
（`far = 10 + 2*CONTEXT`），并断言**关系**（`ns2 == os2 + 1`、`os1 == ns1`）
而非绝对数字。它们要守的回归仍在，而以后调半径不再打断它们。

#### 顺带补了一个"观察窗口"

其余演示桩都产不出**带上下文**的 diff（`selftest` 整文件重写 → 全是增减行；
`multitool` 只有 bash 调用）。于是"审批时到底能看到多少上下文"此前**没有可
离线复现的观察路径** —— 这个改动是"改了但看不到"。故新增 `--provider edit`：
改大文件里的一行，产出真正的部分修改 diff。

**真机验证**（按窗口 ID 截图，未抢焦点）：
- hunk 头 `@@ -10,21 +10,21 @@` —— 半径 10 的确切形状；
- 25 行 diff → 折成 14 行显示；`⋯ 未改 4 行`（第 13–16 行）与
  `⋯ 未改 7 行`（第 24–30 行）与算法**逐项对得上**
  （前导段两侧各留 3 行——一侧贴 hunk 头、一侧贴改动；尾段只留贴改动的 3 行）；
- 行内强调同时生效（`行（已改）` 压了更浓的底）。

**教训**：一个"看起来够用"的常量，往往只是"没人问过为什么"。而问过之后，
第一件该做的事是**把新建立的依赖关系写成可执行的断言**，否则下一个改动又会
把它悄悄破坏掉。

---

### (bd) D12 内置浏览器 + 一个连踩四次的"长行越界"

D12 第二层：**文件预览**（点文件看内容）。面板改成两栏（列表 + 预览），
点击语义从"加入引用"改成"预览"，引用变成预览栏里的独立按钮 ——
此前想看内容只能先把整个文件注入模型上下文，既花钱又绕。

两件值得记的事：**两道独立的安全闸**，以及**一个连踩四次的布局坑**。

#### 安全：两道闸各自挡一种绕过（实测确认过，不是推测）

预览不走沙箱（内容只给人看、不进模型上下文），所以工作区限定必须自己做。
写了两道：
1. **入口检查**：`rel` 含 `..`（`Component::ParentDir`）或是绝对路径 → 直接拒；
2. **`is_within`**：`canonicalize` 之后比前缀。

我原以为第 2 道是"主闸"、第 1 道是"友好提示"。**牙齿验证时发现相反**：
把 `is_within` 关掉，**只有软链那条测试红**，`..` 那条照样绿 ——
因为含 `..` 的输入在第 1 道就被拦了，`is_within` 根本没被走到。
两道闸挡的是**不同的攻击面**：可疑输入 vs **看起来完全正常**的软链路径
（`link/secret.txt`）。已把这个发现写进测试注释 —— 否则后人很可能
把其中一道当"冗余"删掉。

`is_within` 在 `neo-platform`（L1）**本地实现了一份**，而不是复用
`neo-sandbox-local`（L3）的：向上依赖会被架构守卫拒（那条守卫是对的 ——
平台层不该知道沙箱实现）。代价是同一语义两份实现，所以逐句对齐了那边的做法
并在两处都写了注释说明**为什么**是两份。

#### 布局：长行裁切连踩四次（前三版真机上都越界）

| 尝试 | 做法 | 结果 |
|---|---|---|
| 1 | 抄 diff 正文的 `whitespace_nowrap` | 长行**横向冲出面板**、盖到主界面 |
| 2 | 改成折行 | **仍越界** —— 一长串无空白字符（`xxxx…`、压缩 JS）**没有折行断点** |
| 3 | `text_ellipsis()` | **仍越界** —— 它**只设 `text_overflow` 样式**，不含 `overflow_hidden` |
| 4 | `truncate()` + **整条 flex 链 `min_w(0)`** | 成功（`…` 恰好落在面板边界） |

**两次"抄错"的教训**：
- **抄一个写法前要问它在原处是"契约要求"还是"顺手写的"**。`nowrap` 在 diff
  正文里是**契约要求**（不折行才能与底带逐行对齐），在文件预览里毫无必要 ——
  我照抄了形式，没抄它的理由。
- **`text_ellipsis` 与 `truncate` 是一对陷阱**：名字听起来前者更"专门"，
  实际它只设样式；**三者合一的那个叫 `truncate()`**。

**`min_w(0)` 那一步才是根因**：flex item 的 `min-width` 默认是 `auto`
（= 不小于内容），于是 `flex_1` 的"可收缩"**形同虚设** —— 内容多宽它就要多宽，
一路把父容器撑破，`overflow_hidden` 也就无从裁起。
**每一层要收缩的容器都得显式给 `min_w(0)`**，只给最里层不够。
这条在本仓是重复出现的模式（转录区也写过 `min_w(px(0.))`）——
**"flex 收缩不生效"先查 `min_w(0)`**。

**客观验证**：不靠"看起来对齐了"，而是量"长行最右像素 x=1879" =
面板右边界（940 逻辑点 × 2）——**数值恰好等于边界**才说明裁在正地方。

#### 顺带：数据层不再写"已截断"标注

原先我在 2000 字符处截断并追加"（本行过长，已截断）"。实测**那段文字会被
渲染层的省略号裁在可视区之外**（2000 字符远超面板宽度）—— 加了谁也看不见，
纯死重。改为**数据层管封顶、渲染层管提示**（`…`），各做各的不重复。

---

### (be) 自绘 UI 的"粗糙"：先审计成可核对的清单，再改收益最大的两处

用户反馈："自绘组件 UI 比较粗糙，不是 zed 或大厂那种能直接上生产的标准。"
这是**主观感受**，而主观反馈最容易被敷衍（"我调了几个间距"然后说改好了）。
所以我先把它变成**可核对的数字**：

| 项 | 实测（可查） | 意味着 |
|---|---|---|
| 圆角 | 渲染缝 `Op` **没有半径概念**，后端一律 `Corners::default()` | 所有自绘元素都是硬边直角 |
| 悬停反馈 | 宿主 22 个 `on_click`，`hover(` **0 次** | "能点"却看不出能点 |
| 字号层级 | `font_weight`/`text_size` **0 次** | 标题与正文同字号，无信息层次 |
| 列表项形态 | 文件行是裸文字：贴边、无内边距、无行高、无选中态 | 不像列表，像日志 |

**这份清单本身就是产出** —— 它把"粗糙"拆成可以逐项做完、逐项验收的东西，
而不是一个模糊的质量抱怨。

#### 本轮改的两处（收益最大的）

1. **渲染缝支持圆角**：`Op::FillRect/StrokeRect` 增 `radius`，两个后端都如实
   传达（SVG 写 `rx`）。半径**由调用方从设计系统取**（`neo_ui::RADIUS = 6.0`，
   即主题 `radius` medium）—— 与颜色同一条路径：**中立层不能读主题，
   所以设计系统的值必须由上层传入**。
2. **文件行改为真正的列表项**：内边距 + 行高 + 选中底色 + 悬停底色。

#### 三个值得记住的判断

- **默认 0（直角）是刻意的**。给"所有矩形加圆角"才是那个看起来对、实际错的
  做法：diff 底色带是**整行通铺**的面，圆角会让相邻行之间出现缝隙 ——
  看起来像"改动的行数不对"。所以 `radius` 是"要圆角的人显式要"的属性，
  而不是默认继承。**组件的默认值应当由"这个形状是什么"决定，不是由"新特性
  默认开启"决定。**
- **圆角一律先夹到半宽/半高再入场景**。gpui 自己会夹、SVG 的 `rx` 不夹 ——
  若在场景里不夹，"中立场景"就同时有两种解释，两个后端会给出不同结果。
  **跨后端一致性的责任在场景本身，不在后端各自"聪明地处理"。**
- **悬停取值跟设计系统的习惯走**（`muted` 半透明），不自己另挑一个颜色 ——
  否则悬停色会与组件库的控件不一致，而那正是"拼凑感"的来源。

#### 诚实边界：这一步只走了一小段

圆角与列表项只是清单里的**两项半**（字号层级、其余 21 个可点元素的悬停态、
间距节奏、暗色下的对比度校验都还没做）。**"上生产标准"是一次系统工程，
不是一次补丁** —— 本轮的价值在于**把标准写成了清单并开了头**，
而不是声称已经达到。剩余项见 changeset 与本节的表。

---

### (bf) UI 精细度第二步：排版层级做成"可断言的数据"，而不是逐处挑字号

继续 §4.64(be) 那份"粗糙清单"，本轮做**字号层级**与**间距 scale**
（我判断这两项对观感的贡献比圆角更大）。

#### 审计出的第二个硬数字

宿主里 `font_weight` / `text_size` 出现 **0 次**；间距里有**离 scale 的原始值**
（`.py(px(3.))`）—— 而设计系统的间距档是 2/4/8/12/16/24/32，**根本没有 3**。
两个数字都指向同一个病：**样式是随手写的，没有从设计系统取值**。

#### 关键做法：把角色做成**数据**，于是"层级存在"可被断言

第一版我把四个角色的样式**写死在函数里**（`section_title()` 内部 `.text_sm().font_weight(SEMIBOLD)`）。
它是对的，但**不可测**：若哪天有人把四个角色改成同一组样式，界面上不会报错 ——
只是"又变平了"（用户看得出、测试看不出）。

所以改成数据驱动：`Role { size, bold, tone }` + 四个 `pub const`，
`apply()` 是唯一的套用点。于是测试可以直接断言**关系**：
标题与正文不同字号、标题更粗、三种颜色互不相同、四个角色两两不等。
**牙齿已验证**：把 `SECTION_TITLE` 抹平成正文，测试立刻红。

**通用教训**：把"样式决定"从函数体搬到数据，收益不只是整洁 —— 它让
**设计决策变成可断言的命题**。凡是"看起来应该分层 / 应该区分"的观感要求，
都值得问一句："它能被写成断言吗？" 写不出来，就说明它还只是个愿望。

#### 两个具体判断

1. **标题取"小一号"而不是"大一号"**。工具型界面（dense tool UI）的惯例是
   正文当主角、字号偏小以换信息密度，标题靠**字重与颜色**区分（Zed / VS Code 都这样）。
   把标题放大到 20px 会让面板显空旷、挤压内容区。**"层级"不等于"更大"**。
2. **层级不只靠字重**（承接 §4.64(ba) 的教训）：CJK 常无真粗体字面，
   `SEMIBOLD` 在中文上可能看不出差别。所以标题**同时**用了更亮的语义色、
   说明文字**同时**用了弱化色 —— 两条路径叠加，任一条在某字体上失效仍可分辨。
   **在"可能失效的机制"上不要只押一条路径。**

#### 诚实边界（清单上还剩什么）

本轮做完：圆角、悬停反馈、排版层级、间距 scale。**仍未做**：
- ~~**暗色下的对比度校验**~~ **已补**：实测 `Text` 17.9 / `Muted` 6.1 /
  `Info` 12.6（黑底）与 14.0 / 4.8 / 9.8（面板底），全部 ≥4.5:1（AA）。
  并**固化成守卫** `text_and_muted_stay_readable_on_both_backgrounds`
  —— 牙齿已验证：把 `muted` 调暗到 0x3a 立刻红。"我算过一次"不等于"以后不会被破坏"；
- 焦点环（键盘可达性的可见反馈）；
- 过渡动画（hover/面板开关目前是瞬变，Zed 那类观感有短过渡）；
- 间距节奏的**整体复查**（我只清了"离 scale 的值"，没有重新审视各处该用哪一档）。

---

### (bg) UI 精细度第三步：焦点指示 + 入场动画，以及"能改签名让事情可证"

做完 §4.64(bf) 里剩下的两项：**焦点环**与**过渡动画**。

#### 焦点指示：一个"看不见的状态"

输入区是 `.appearance(false)`（无边框，为与转录区连续排版），于是**聚焦与不聚焦
长得一模一样** —— 唯一差别是光标在闪，而光标会在截图里熄灭（本文件另一处注释
早就记着这件事，但没人把它当成缺陷修）。对以打字为核心的界面，
"看不出焦点在哪"是实打实的可用性问题。

修法：聚焦时在输入区下方画一条品牌色细线（**底线**而非整圈边框 ——
套一圈会让它突然像网页表单，破坏"无框"的连续排版观感）。

**真机证据是数值**：聚焦时底部品牌色像素 **1988**，焦点移走后 **0**。
配合 `NEO_GUI_FOCUS` 钩子确认 `composer_focused` 在 true/false 间正确切换。
—— "焦点可见"这种观感要求，一样可以被量出来并写进证据。

#### 入场动画：只给面板加，不给列表行加

设计系统里 `with_animation` **只用在弹层**（dialog / popover / sheet /
notification），列表行没有入场动画。所以覆盖式面板（文件树 / 帮助 / 模型选择 /
命令面板）接入场动画（150ms 下滑 + 淡入，与弹层 `DROPDOWN_ENTER_DURATION` 同值），
而列表行**不动**。**跟设计系统的习惯走** —— 与 §4.64(be) 同一个判断：
在别处也动的地方动，否则要么浮夸、要么与组件库不一致。

**自动尊重 `reduce_motion`**：走 `with_animation`（它在系统要求减少动效时渲染终态、
不排帧），**没有**自己写 `cx.spawn` + 定时器 —— 那会让这个系统级设置失效，
等于替动效敏感的用户做决定。

#### 最有价值的一条：**能改签名让事情可证，就别绕道去证**

验证"动画确实走了 gpui 的动画入口"时，我前两版判据**都失败**：
1. `include_str!("app.rs")` + 文本断言 → **自我命中**：断言里写的字面量
   本身就在那个文件里，于是它**必然失败**（我还因此多花了两轮）；
2. 比较两元素的 `Debug` 输出 → `Div` 与 `impl IntoElement` **都没有 `Debug`**。

最后改的是**产品代码的签名**：让 `panel_enter` 返回具体的
`AnimationElement<Div>`，于是"它走的是动画入口"成为**类型层面的事实** ——
测试只需 `assert_is_animation_element(&animated)`，而"自己写循环"根本不可能
产出这个类型。

**教训**：当一条断言难写时，先问"是不是接口让我难证？"。
**把接口改成可证的形状，比在测试里绕（字符串、Debug、反射）都可靠** ——
后者写出来的往往是"看起来很努力但抓不住东西"的测试。

#### 顺带：`InputEvent` 的匹配改成穷尽

加了 `Focus | Blur` 分支后，原先的 `_ => {}` 变成 **unreachable**（编译器警告）。
去掉通配分支后，`InputEvent` **新增变体会编译失败** —— 那正是我们要的提醒
（"有新事件了，你想好怎么处理了吗"）。加通配分支会把这条提醒静默吃掉。

#### 诚实边界

清单上还剩一条：**间距档位的整体复查** —— 我只清了"离 scale 的值"（§4.64(bf)），
没有重新审视各处**该用哪一档**（比如所有面板内边距都是 `p_3`，而按层级
外层该更大、行内该更小）。这需要逐屏看，不是一次性改动。

---

## 会话接续点（2026-09-17 收尾）

今天这一线（渲染缝 + diff + gpui composer）共 12 个提交，全部已推送，`rust` /
`efficiency` 两个 workflow 全绿（`changeset release` 仍红，见上表：是仓库设置）。
测试 922 → 967，本机 `scripts/verify.sh` 全部门禁通过，工作区干净、与远端同步。

### 明天可以直接接着做的三件事（按我的建议排序）

1. **D12 文件树 / 内置浏览器 / Repo Wiki** —— parity 清单里**唯一还是 ❌ 的项**，
   也是"从'能看'到'能干活'"差别最大的一项。**动手前需先定**文件索引的边界
   （扫盘范围 / 忽略规则 / 是否 watch 变更），那是新能力，不是纯接线。
2. **D5 剩余两项**：并排（side-by-side）视图、跳转上/下一处改动。
   并排视图还能**顺带让折叠活起来**（每侧上下文比单栏多）。
3. **删 egui（阶段 3 收尾）** —— 三条前置条件写在缺口表里，**需用户拍板**
   （不擅自删：保留对照物才能判断新问题是 gpui 的还是内核的）。

### ⏳ 有一件事**等用户决定**（别自己定）

**是否把审批预览的 diff 上下文从 `CONTEXT = 3` 提到 10？**

- 上下文：`neo-capability/src/diff.rs` 的 `CONTEXT = 3` **无注释说明取值理由**
  （未经审视的 unix 默认），它是 §4.64(az) 里"折叠休眠"的直接原因。
- 已量化风险：**不进模型上下文**（`PatchProposed` 只走事件流渲染，模型看到的是
  `ToolResult`，改它不加 token 成本）、**生产调用方只有审批预览一处**、
  现有测试几乎不依赖具体行数。
- **为什么没顺手动**：它会同时改变 **TUI 宿主 diffview** 的观感（共享同一生产者），
  属"审批时给用户看多宽的上下文"这个**产品决定**，不该由补 UI 组件的任务顺带定。

### 这一线的经验（详见 §4.64(ax)(ay)(az)(ba)）

- **"实现了" ≠ "生效了"**：折叠逻辑写完才发现对自家 diff 永不触发（每 hunk 只带
  3 行上下文）。**新增路径必须问"触发条件在当前数据下成立吗"**。
- **有损抽象在新需求下会变成正确性 bug**：`DiffBand` 原为"改动色 / 其它"二分，
  折叠需要区分"未改上下文"与"文件头 / 截断说明"，故补成完整分类。
- **字体相关的视觉手段必须在中文上实测**：粗体在 CJK 上无真字面（第三次被
  同类问题咬，前两次是 egui 字体与豆腐块）→ 改用底色（可像素断言）。
- **GUI 验证不抢用户焦点**：`screencapture -l <窗口ID>` 按窗口截图 + `open -g`
  后台启动 + `NEO_GUI_PROMPT` 自动提交，全程不需要键盘注入（已写成效率规范第零条）。
