[package]
name = "dsh-core"
version.workspace = true
edition.workspace = true
license.workspace = true

# L2 CORE: 禁止依赖 L3(capability) / L4(orchestration) / L5(host*)
[dependencies]
dsh-protocol = { path = "../dsh-protocol" }
dsh-sandbox = { path = "../dsh-sandbox" }
dsh-platform = { path = "../dsh-platform" }
dsh-session = { path = "../dsh-session" }
dsh-config = { path = "../dsh-config" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
dsh-mock = { path = "../dsh-mock" }
dsh-capability = { path = "../dsh-capability" }
