# 落地路线图（供 LLM Agent 逐阶段执行）

> 前置：先读 00-架构总纲.md 与 01-组件库规格.md
> 约定：每个 Phase 结束必须跑通「验收命令」且全绿，才允许进入下一 Phase。
> 所有 `<ns>` 替换为你的组织前缀。

---

## Phase -1 · 技术验证（1–2 周，只做一次，目的是避免押错注）

**目标**：在写下第一行产品代码前，用最小代价确认 GPUI-CE + GPUI Kit 这条路在你的目标平台上真的能跑。

### 任务
- [ ] 建一个临时仓库 `spike-gpui`，**不写任何业务代码**
- [ ] 用 `gpui-ce` + `gpui-component` 跑通 Hello World
- [ ] 三平台各跑一次：macOS / Windows / Linux(Wayland + X11)
- [ ] 验证四个高风险项（见下）

### 验证脚本（必须实际运行，不要假设）

```bash
# 1) 拉最新可用版本，勿硬编码文档里的数字
cargo search gpui-ce --limit 5
cargo search gpui-component --limit 5
cargo search gpui-kit --limit 5
```

`Cargo.toml`（spike）：
```toml
[dependencies]
gpui          = { package = "gpui-ce", version = "<实查最新>" }
gpui_platform = { git = "https://github.com/gpui-ce/gpui-ce" }
```

> 若 `gpui-component` 与 `gpui-ce` 版本错配，用社区推荐方式打 patch：
> ```toml
> [patch.crates-io]
> gpui = { git = "https://github.com/gpui-ce/gpui-ce", package = "gpui-ce" }
> ```

### 四个必须实测的高风险项

| # | 验证内容 | 通过标准 |
|---|---|---|
| 1 | **CJK 输入法** | 中文输入法候选框跟随光标；preedit 删除后重输不崩溃 |
| 2 | **CJK 渲染** | 中日韩文字 + emoji + 中英混排换行正确，无豆腐块 |
| 3 | **高 DPI** | 200% 缩放下无模糊、无错位 |
| 4 | **冷启动** | release 构建首帧 < 800ms |

**任何一项不过，立即回头评估 Iced / Slint，不要带病前进。** 这四项是后期无法补救的——它们深植在框架的文本与平台层。

### DoD
- 三平台 Hello World 均可运行
- 上表四项实测通过并有录屏/截图留档
- 产出一份 1 页结论：GO / NO-GO + 锁定版本号

---

## Phase 0 · 工程地基（2–3 周）

**目标**：把「单一 pin」「分层」「CI」这三件事在代码里固化下来。

### 任务
- [ ] 建 workspace，统一 `[workspace.package]` version / edition / lints
- [ ] `rust-toolchain.toml` 锁定 toolchain
- [ ] 建 `crates/<ns>-kit` 门面层（唯一允许依赖 GPUI 的地方）
- [ ] 建 `xtask`，实现三个校验子命令
- [ ] CI：build / test / clippy / fmt / deny / audit
- [ ] 根目录 `NOTICE` + `LICENSES/`（SPDX 归档）

### 必须实现的三个 xtask 校验

```bash
cargo xtask verify-pins      # 除 kit 外无 crate 直接依赖 gpui；Cargo.lock 中 gpui 只有一个版本
cargo xtask verify-layering  # behavior/ 与 ui/ 中不得出现 `use gpui::`
cargo xtask verify-assets    # 图标/字体/主题资源均已生成且无缺失引用
```

`verify-pins` 核心实现参考：
```bash
# 断言无重复 GPUI 实例（有输出即失败）
cargo tree -i gpui-ce --duplicates
```

### DoD
- [ ] `cargo xtask verify-pins && cargo xtask verify-layering` 均退出码 0
- [ ] CI 全绿（三平台 matrix）
- [ ] 空壳应用可启动（一个空白窗口 + 主题切换）

---

## Phase 1 · MVP（4–6 周）

**目标**：核心用户旅程可跑通，能内部试用。

### 任务
- [ ] `<ns>-core` 业务核心（**零 GUI 依赖**，纯逻辑，先写测试）
- [ ] 主窗口骨架：`Root` + `Sidebar` + `Dock` + `Tabs`
- [ ] 核心视图：用 P0 组件清单（见 01 文档第 4 节）组装
- [ ] 状态持久化（设置、窗口布局序列化）
- [ ] 基础日志（tracing + 落盘）

### 执行纪律
- 每个 PR 只做一件事，PR 描述必须写「改了哪一层」
- 业务名词**禁止**出现在 `<ns>-ui` 中（组件只认识 `Item`/`Row`/`Entry`）
- 每次视图改动都要更新 storybook 条目

### DoD
- [ ] 核心旅程端到端可走通（人工实测 + 录屏）
- [ ] `<ns>-core` 行覆盖率 ≥ 80%
- [ ] 窗口布局可持久化并在重启后还原
- [ ] 三平台均可启动并完成主流程

---

## Phase 2 · 商业可用化（4–8 周）

**目标**：从「能跑」到「能卖」。这一阶段占整个项目 40% 的时间，不要压缩。

### 2.1 组件达标
- [ ] P1 组件清单全部集成 + 品牌化
- [ ] **每个 P1 组件过 DoD 九宫格**（01 文档第 3 节），逐项打勾
- [ ] 补 IME 回归用例（中/日/韩三种输入法）

### 2.2 无障碍
- [ ] 全量组件补 role / label / state
- [ ] 键盘全可达 + 焦点环（亮暗主题对比度 ≥ 3:1）
- [ ] 读屏实测：VoiceOver(macOS) / NVDA(Windows) / Orca(Linux)

### 2.3 稳定性
- [ ] 接入 Sentry Rust SDK（`backtrace`/`contexts`/`panic` feature）+ minidump
- [ ] 自定义 panic hook：崩溃前 flush 日志与遥测
- [ ] 全局错误边界：单视图渲染失败降级为占位，不白屏
- [ ] 日志落盘 7 天 + 一键导出

### 2.4 分发（用 cargo-dist，不要自研）
```bash
cargo install cargo-dist
cargo dist init
cargo dist build
```
- [ ] macOS：Developer ID 签名 → `xcrun notarytool` 公证 → `xcrun stapler` 钉票 → `.dmg`
- [ ] Windows：Authenticode（EV 证书或 Azure Trusted Signing）→ `.msi`
- [ ] Linux：`.deb` / `.rpm` / `.AppImage`
- [ ] 自动更新：签名更新包 + CDN + stable/beta 双通道
- [ ] Homebrew tap / winget / Scoop（CI 自动提 PR 更新 SHA256）
- [ ] SBOM（CycloneDX）随产物发布

### 2.5 性能
- [ ] 内置帧时间直方图，跑满 01 文档第 7 节性能预算
- [ ] **空闲 CPU ≈ 0**（无动画不出帧）

### DoD
- [ ] 01 文档第 8 节 Release Gate 全项通过
- [ ] 三平台签名包可安装、可自动更新

---

## Phase 3 · 开源准备（与 Phase 2 并行，低成本）

**目标**：让「复制目录出去建新仓」成为真话。

### 任务
- [ ] `<ns>-render` 实现 `RenderBackend` + `GpuiBackend`（**只做一个实现**）
- [ ] 自绘组件（Canvas / DiffView / 消息流）改走渲染缝
- [ ] `<ns>-ui` 建 storybook gallery（含亮暗主题 × 2 档 DPI 截图）
- [ ] 每个待开源 crate 自带 README / LICENSE(Apache-2.0) / CHANGELOG
- [ ] 文档站：`cargo doc` 或 mdbook
- [ ] CONTRIBUTING.md + 行为准则

### 关键纪律
- 待开源 crate 的文档注释与错误信息，**一律用独立产品口吻**，不引用应用上下文
- 渲染缝只服务自绘表面，常规组件仍直连 gpui-component（见 01 文档 2.1）
- **不要提前写 VelloBackend**：只有一个实现的抽象是信仰，两个实现才是设计

### DoD
- [ ] `cargo xtask verify-layering` 绿
- [ ] storybook 可独立运行并展示全部 P1 组件
- [ ] 把 `render`/`behavior`/`ui` 三个目录复制到空仓能独立编译（实际演练一次）

---

## Phase 4 · 渲染引擎开源（Phase 2 上线后 6 个月起）

**目标**：让「渲染引擎」名副其实。

### 触发条件（全部满足才启动）
- 主应用已稳定上线 ≥ 6 个月
- `GpuiBackend` 之上已有 ≥ 3 个自绘组件稳定运行
- 有 ≥ 1 名专职人力可投入

### 任务
- [ ] 实现第二个 backend（`VelloBackend`：vello + wgpu）作为**抽象有效性证明**
- [ ] 若第二实现暴露 trait 设计缺陷 → 回头改 trait（这恰恰是第一版不做的原因）
- [ ] 基准测试套件：与 Skia / Direct2D 对比，公开发布
- [ ] 建独立仓库 + 官网 + 路线图 + RFC 流程
- [ ] 明确维护承诺（响应时间、版本策略、MSRV）

### 预期时间
**12–18 个月**。参照系：Vello 由 Raph Levien（Skia 老兵）带队，2022 做到 2026 仍是 alpha，blur/filter/字形缓存/显存策略四项仍未完成。请对这条时间线保持敬畏。

---

## 9. 时间线总览

| Phase | 周期 | 产出 | 可对外 |
|---|---|---|---|
| -1 验证 | 1–2 周 | GO/NO-GO 结论 | — |
| 0 地基 | 2–3 周 | workspace + CI + xtask | — |
| 1 MVP | 4–6 周 | 可走通主流程 | 内部试用 |
| 2 商业化 | 4–8 周 | 签名包 + 无障碍 + 崩溃上报 | **可发布** |
| 3 开源准备 | 并行 | 渲染缝 + storybook + 文档 | 仓库可公开 |
| 4 引擎开源 | 12–18 月 | 第二 backend + 基准 | 独立产品 |

**最快可发布：约 3–4 个月**（Phase -1 到 Phase 2 结束，假设 1–2 名熟练 Rust 工程师）。

---

## 10. 给执行 Agent 的十条铁律

1. **绝不**在 `<ns>-kit` 之外声明 `gpui` 依赖。
2. **绝不**在 `<ns>-behavior` / `<ns>-ui` 中出现 `use gpui::`。
3. **绝不**在 `<ns>-ui` 中出现业务名词。
4. **绝不**硬编码颜色，一律 `cx.theme()`。
5. 组件尺寸只用 `xs/sm/md/lg`。
6. 无状态组件一律 `RenderOnce`。
7. 每个组件配 story 条目 + 截图基线。
8. 输入类组件必须有 IME 回归用例，且不可 `#[ignore]`。
9. 升级 GPUI 版本必须走独立 PR + 截图基线比对，禁止夹带功能改动。
10. 拿不准放哪层的代码 → 放 L2（行为层）。表现错误好改，层次错误难救。
