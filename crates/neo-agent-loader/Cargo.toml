[package]
name = "neo-agent-loader"
version.workspace = true
edition.workspace = true
license.workspace = true

# L3 PROVIDER —— 子代理定义的发现与加载(Markdown + frontmatter)
[dependencies]
neo-core = { path = "../neo-core" }
