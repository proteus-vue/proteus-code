[package]
name = "neo-skill-loader"
version.workspace = true
edition.workspace = true
license.workspace = true

# L3 PROVIDER —— 技能目录发现与加载（把磁盘上的 SKILL.md 变成 SkillRegistry）
[dependencies]
neo-core = { path = "../neo-core" }
