[package]
name = "dsh-exec"
version.workspace = true
edition.workspace = true
license.workspace = true

# L5 HOST: 无头 / CI 宿主。不含业务逻辑。
[dependencies]
dsh-protocol = { path = "../dsh-protocol" }
dsh-core = { path = "../dsh-core" }
dsh-orchestration = { path = "../dsh-orchestration" }
