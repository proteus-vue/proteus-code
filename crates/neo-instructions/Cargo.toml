[package]
name = "neo-instructions"
version.workspace = true
edition.workspace = true
license.workspace = true

# L3 PROVIDER —— 项目指令（AGENTS.md）级联发现与加载
[dependencies]
neo-config = { path = "../neo-config" }
neo-core = { path = "../neo-core" }
