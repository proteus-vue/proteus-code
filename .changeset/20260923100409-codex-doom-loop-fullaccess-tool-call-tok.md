---
bump: minor
---

对齐 Codex 风格桌面宿主的内核护栏十条：连续相同调用提醒与 doom-loop 独立闸门（FullAccess 不静默放行、拒绝即停）、未执行 tool_call 非空占位、单轮工具预算提醒与熔断、会话 token 预算（session/configure.token_budget）、只读并行（上限 10，事件序不变）；app-server 新增 models/list 供桌面设置页选模型。
